# Python
__pycache__/
*.pyc
.venv/

# conversion outputs and debug artifacts
*.debug.json
*.debug.p*.png
verify/

# decks and documents are data, not source — never version them
*.pdf
*.pptx

# Office lock files
~$*

# Claude Code 的本機東西：權限允許清單（settings.json）與 worktree 工作副本。
# 換電腦是複製整個專案資料夾，所以它照樣帶得走，不必靠 git（與 MP4-2-SRT 一致）
.claude/

# pytest
.pytest_cache/

# 執行紀錄：GUI 每次啟動寫一個 logs\<時間>.log（保留 30 天），不是原始碼。
# 「啟動.vbs」不再落檔——它把錯誤直接跳訊息框（2026-08-24 使用者指示）。
logs/
*.log
