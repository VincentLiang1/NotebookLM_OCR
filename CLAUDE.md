# CLAUDE.md

本檔案為 Claude Code（claude.ai/code）在此倉庫工作時的指引。

## 專案簡介

`NotebookLM_PDF_2_PPT` — 將 NotebookLM 簡報 PDF 檔轉換成可編輯 PowerPoint 的工具（CLI `pdf2ppt.py` 為主，另有 Tkinter 圖形介面 `pdf2ppt_gui_2.py` 包在同一支 `cli.main()` 外面），每頁投影片以全解析度頁面渲染圖為背景，疊上實心填色矩形蓋住點陣文字，並承載可編輯、樣式比對過的文字（字級、粗體、文字色、蓋板色皆從影像估計）。

## 常用指令

```powershell
pip install -r requirements.txt        # rapidocr 首次執行會下載 ONNX 模型（需要一次網路）
python pdf2ppt.py "input.pdf"          # 完整轉換 -> input.pptx
python pdf2ppt.py in.pdf -o out.pptx --pages 1-2 --debug   # 快速迭代：指定頁 + 疊框 PNG/JSON
python tools/compare_pptx.py generated.pptx reference.pptx # 與參考簡報比對文字召回率
python pdf2ppt_gui_2.py                # 圖形介面（在專案根目錄執行）
```

⚠️ **GUI 的選項清單是手抄 `cli.py` 的 argparse 定義**（標籤、預設值、`--device` 的 choices、粗體模式的旗標對應）。在 `cli.py` 增刪旗標或改預設值時，`pdf2ppt_gui_2.py` 與 README 的選項表必須同一輪一起改 —— 已經漂移過一次（`--lang` 在 CLI 與 README 都有、GUI 完全沒有對應控制項，2026-08-16 補上）。

本專案沒有自動化測試；驗證方式是轉換範例 PDF 並與參考 PPTX 比對（見下方「驗證」）。

## 協作方式

以下**必須當場生效**，所以抄在這裡——完整理由、沿革與災情紀錄在 `docs/dev/collaboration.md`，只寫在那裡等於沒寫（要有人想到去讀才載得進來）。

- ⚠️ **使用者說「記住 / 記憶下來」= 寫進 repo，不是寫進 Claude Code 的記憶功能**（2026-08-17 自 `meeting-scribe` 移植）：記憶目錄在 `%USERPROFILE%\.claude\projects\<由專案路徑編出來的名字>\`、**在 repo 之外且 git 不追蹤**，而使用者換電腦的方式是**複製專案資料夾**——寫在那裡的東西一份都帶不走，光是把專案搬到別的資料夾，那個依路徑編出來的目錄名就對不上了。聽到時：**必須當場生效**的規則寫進**這一份**（只有 `CLAUDE.md` 每次自動載入）、領域細節寫 `docs/dev/` 並在這裡留一句指路；然後**一併提交推送**，並**在回覆裡說清楚寫進哪個檔**，好讓他知道換機器之後還在。⚠️ 同理，全域的 `C:\Users\vli\.claude\CLAUDE.md` 也帶不走——跨專案規則若對本專案是關鍵的，就在這裡再寫一份。沿革見 `docs/dev/collaboration.md` §1。
- **回覆使用者一律用繁體中文**（台灣用語），不必等他提醒；專有名詞、API 名稱、程式碼、路徑、log 原文照原樣保留。使用者的**內容領域也是繁中**——這正是 OCR 必須用 PP-OCRv5 **server** 辨識模型而非預設 v4 mobile 的理由。
- **git commit 訊息一律繁體中文**（使用者 2026-06-11 指示）：技術名詞、函式名、常數、px 數值保留原文；訊息內**避免雙引號**（PowerShell 5.1 會截斷），結尾加 `Co-Authored-By`。
- **修正完成並驗證後自動 commit + push，不要再問**（使用者 2026-06-11 指示）；**功能新增或修改時 `README.md` 要在同一輪同步更新後一起提交**（判準：使用者看得到的行為變了沒有）。⚠️ **`CLAUDE.md` 是被 git 追蹤的**——`.gitignore` 只擋 `*.pdf`／`*.pptx`／`verify/`／`*.debug.*` 那幾類，提交時不要漏掉它。
- ⚠️ **自己下 Bash 指令時別寫成權限閘門攔得下的形狀**（自 `MP4-2-SRT` 移植 2026-08-23；原始查證 2026-08-15 讀 `claude.exe`）。全域的 `%USERPROFILE%\.claude\CLAUDE.md` 也有一份，但**那個檔換電腦帶不走**，所以照本章開頭的原則在這裡再寫一次。踩到就**跳過 Auto Mode 的分類器直接中斷去問使用者**，而且加 `allow` 白名單救不了（判為 `too-complex` 的指令，規則比對一律回 `unverifiable`）。三條：**①** 不在指令裡 `cd`（Bash 工具的工作目錄本來就在專案根目錄），要別的路徑就寫絕對路徑；**②** 重導向目標寫死絕對路徑，不可用 `$VAR` 或 `$(...)` 組（會判成 `runtime-determined`）；**③** `cd` 與輸出重導向不可出現在同一條複合指令（`cd-compound-redirect`）。能不重導向就不重導向，讓輸出直接回來。⚠️ 連帶效應：**工作區未被信任時，`.claude/settings*.json` 的權限規則會被整批忽略**（Claude Code 2.1.232 起信任不從上層資料夾繼承進 repo），症狀就是「每個工具都重新問一次」。
- **改了行為要同步更新文件，而且與程式碼同一筆提交**（自 `MP4-2-SRT` 移植 2026-08-23）：事後補的文件跟事後補的測試同一種病，沒人會回頭做，而規格一旦有一條是舊的，讀的人分不出哪幾條還能信。**這個 repo 已經吃過虧**——2026-08-23 那輪 code review 一次抓出本檔有五處與程式不符或自相矛盾（投影半徑公式、`nat_close` 門檻、chromatic 退回的新條件、已解決卻仍寫著「需使用者裁示」的限制、同一段裡一句說移除一句說還在的閘門）。落點：

  | 改了什麼 | 要更新 |
  |---|---|
  | 使用者看得到的行為（CLI 旗標、輸出規則、丟棄規則、限制） | `README.md` 對應章節 **＋ `pdf2ppt_gui_2.py` 的選項清單**（它是手抄 argparse 的，漂移過一次） |
  | 通用規則、不變量、門檻的理由 | **本檔**的「架構」章（只有 `CLAUDE.md` 每次自動載入） |
  | 沿革、災情紀錄、完整推導 | `docs/dev/` 並在本檔留一句可 grep 的指路 |
  | 常數的值或理由 | **該常數上方的註解**（那是正典，本檔指過去而已） |
  | 量過而否決的一條路 | 該處註解 **＋ 本檔最後的「量過而否決的路」索引** |

  判斷方式：**如果這個改動會讓本檔某一句話變成假的，那句話就要一起改**。
- **改壞了重試，成功的那一刻就把失敗的殘骸一起清掉**（自 `MP4-2-SRT` 移植 2026-08-23）：定義了沒人用的常數與函式、被取代卻沒刪的分支、指路到不存在的檔案 —— **跟著「最後一次改對」的那一批進同一個 commit**，不是留到下次想到再清。理由是殘骸跟「刻意留的東西」長得一模一樣，而**分得出來的只有剛改完的那個人**。⚠️ 本 repo 的實例：2026-08-23 用 `_grow` 取代 `_dilate` 之後，`_dilate` 零呼叫者卻留在檔案裡，直到當天稍晚 `/simplify` 才發現。
- **中文輸出字型是 Microsoft YaHei，不要「好心」換成 Microsoft JhengHei**（使用者 2026-06-11 明確選定，先前為 Noto Sans TC）：他是繁中使用者，但看的是 PowerPoint 裡的渲染結果。⚠️ 這個選擇有下游後果——`style.py` 的 `_measure_em()` 固定載入 `msyh.ttc` 量寬度，整套寬度夾制、snap 與標題足跡門檻都是照 YaHei 字寬校準的，換字型不會跟著換度量衡。

## 架構

每頁的處理管線（由 `pdf2ppt/cli.py` 調度）：
渲染（`render.py`，PyMuPDF @200dpi，同一張渲染圖同時供 OCR 與投影片背景使用）→ OCR（`ocr.py`，RapidOCR PP-OCRv5 + server 辨識模型 — 繁體中文準確度遠勝預設的 v4 mobile；`--fast` 可切回）→ 樣式估計（`style.py`）→ 選擇性行合併（`blocks.py`，預設與 DeckEdit 相同、每個 OCR 行一個形狀）→ PPTX 輸出（`builder.py`）。

### 最高原則：所有調整必須具通用性（使用者指示 2026-06-13）

**每一條規則／修改都必須對「未來任意新的 NotebookLM 簡報 PDF」有意義，不能只為了通過手上這份 SOURCE.pdf。** 修正一個視覺問題時，要找出該問題背後**可一般化的成因與判別準則**（例如「線稿圖示被誤讀成單字」「背景有銳利明度階躍」「同段折行被 snap 雜訊拆階」），用通用條件去偵測與處置，並在本檔記錄其原理與反例。**嚴禁針對單一頁面/單一字串硬編碼**（如「若文字==某某 則…」「第 N 頁特別處理」）——那對新檔毫無價值。唯一例外是已知無誤殺空間的**辭典型修正**（如 `_CONFUSION_BIGRAMS` 只收非真實詞彙的穩定誤讀字對），且必須在表中註明已目檢確認。**若某修正本質上只能針對這份 PDF、無法一般化，必須先向使用者說明並取得確認，再決定是否寫入。** 通用規則一律寫進本檔成為不變量。

以下是經過校準才得到的關鍵不變量 — 不要隨意更動：

- **兩套座標系**：PDF 頁面可以是任何尺寸（範例：1376×768 pt = 19.1 吋寬），但投影片固定為 12192000 EMU（960 pt）寬。字級必須換算到「投影片」點數空間：`px_to_slide_pt = 960 / image_width`。
- **字級來自字墨而非 OCR 框**：RapidOCR 偵測框的留白不一致。`style.py` 量測框內緊貼字形墨水的高度，再除以 `CJK_INK_RATIO = 0.91`（用 PowerPoint COM 實際渲染輸出、回測墨水高度所得的實證值）。字墨列要**分群取最重的群**（空隙 > 8px 即切群；行內真實空隙如 i 的點 ≤ ~4px）— 跨進框內的外來墨水會灌高字墨帶、放大字級、讓蓋板塗掉鄰近內容：膠囊框線的圓角/反鋸齒列佔寬 < 0.85 混進字墨（images/ 案例：離主帶 16 空白列）；上一行的底線 `_` 與下一行的字頂也會壓進框內（graph.sh 案例：各離主帶 9 空白列，門檻曾是 0.12×框高=11.4 而漏切）。
- **密集粗體字墨帶不可被 `MAX_INK_ROW_FRAC` 切碎**（`style.py` 的緊字墨界計算，使用者回報 2026-06-14）：`MAX_INK_ROW_FRAC=0.85` 是用來剔除「整列幾乎全是墨」的 ribbon 背景列，但**密集粗體文字在其最擠的中段列也會超過 0.85**（p14 白色粗體「Redmine 工單開立與完成時間戳」峰值 0.90、「全力建構內部 Codebase Skill」0.94，真實心 ribbon 才 ~1.0），這些列被剔除後在字墨帶中間打洞、heaviest-group 分組把帶切成碎片、只剩一小片 → 量成 6–10pt（該行其實是填滿框的 24pt 大字）。**修法**：仍把近實心列從帶的 row set 剔除，但分組時**只在「真正空白縫（>8 列無任何字墨）」切組、橋接被 cap 剔除的近實心列**（span 含被橋接的密集列）。對正常行（無近實心列）與舊的鄰行分離案例（graph.sh 9 空白列、images/ 16 空白列）邏輯完全等價——它們本就由空白縫分隔。連帶修好 p13 金色塊（第二階段：試點 從 10.5pt 救回、漏偵測的「同步補強 DORA 地基」也回來了）。
- **同卡堆疊標籤統一字級**（`blocks.py` 的 `harmonize_stacked_overlap_size`，**在 `harmonize_font_sizes` 之前跑**，使用者回報 2026-06-14）：同一張卡片上下堆疊、原稿同字級的標籤行，pipeline 量成不同字級。**必須在字級調和前跑**，否則調和會把已修正的字級重新併回去。兩種失效形狀，皆拉到較小（可靠）字級，同卡閘門＝同粗體/文字色（≤45）/底色（≤30）＋x 重疊 ≥0.4×min 寬：
  - **(A) 偵測框 y 重疊**（>0.12×min 高）：上框吃進下行字頂、上行字墨帶捕捉鄰字而高估（p8「單一」疊「主專案」、原稿同 24 但單一量 28）。框 y 重疊本身就是異常訊號。
  - **(B) 長行被寬度夾、短行未夾**：較長的卡標籤被卡框寬度夾到小一階、較短的同卡標籤無寬度壓力保持灌高值（p14「啟動【技術解法】/【制度解法】」量 32/28，旁邊「全力建構…Codebase Skill」「重啟計量、拉動式排程」被夾到 24；p13 換行標題「第三階段：擴展與制度重構」的上半「第三階段：」短、未被夾保持 32，下半被夾 24）。訊號：兩行**自然字級相近**、且**較小字級那行的 font_pt < snap(其 est)**（被夾在自然字級之下）→ 把較大行拉到被夾尺寸。**自然字級用「字墨帶高」(ink_bottom−ink_top) 比較、不可用 est_pt**：寬度夾制會壓低 est_pt（兩個換行孿生變成 est 33 vs 26＝差 20%、誤判不同級），但字墨帶高是夾制前的原始量測、兩者只差 7%（p13 第三階段 122 vs 擴展 114）。字墨帶比較同時擋住「真正不同級的標題＋小副標」（原始帶差 >**10%**——門檻 2026-08-23 由 15% 收緊：p7 橘色註解框的標題「盲改 CSS3 次不中」帶 114 與其下的內文「AI 講得很有道理，但它沒去看」帶 97 只差 14.9%，擦邊配對後標題被拖下兩階；本形狀真正要救的折行對差距遠小，p13「第三階段：」122 vs「擴展與制度重構」114＝6.6%）。
  Gustafson 兩個「主專案」差 0 階不動。
- **拆開的標題碎片取「最多 CJK 字」碎片的字級**（`blocks.py` 的 `merge_row_title_fragments`，使用者回報 2026-06-14）：合併同列標題碎片時舊規則取**最大字級**，但**被全形括號（）撐高的短碎片會高估**（p6 標題被拆成「原廠宣傳：Greenfield」(真 28) +「（新專案）」(括號滿高量成 32)，取 max 就繼承了 32、與平行標題「綠界真實戰場：Legacy」28 不一致）。改取**CJK 字最多的碎片**（最可靠、最寬、最不可能是被夾小的那塊）之字級，平手取較寬者——對既有 p6「釐清…的規格體系」案例仍解析到 36（CJK 最多的尾段）。
- **行首高圖示要從字墨帶剔除**（`style.py`，使用者回報 2026-06-13）：偵測框吞進文字左側的高圖示會灌高字墨帶、放大字級。p6 檔案樹每個檔名左邊有文件圖示，index.md/log.md/CLAUDE.md/style_guide.md 把圖示框進去量成 24/20/18/24pt，而圖示落在框外的同款檔名（meeting_notes.md 等）正確量成 12pt。用 `char_boxes` 框出真字範圍：行首間隙（框左到第一字）內的字墨若明顯**比文字帶高**（h_lead > 1.25×h_text）即判圖示——把該段從字墨帶清掉（字級＋蓋板高都據此），並設 `cover_x0_px` 讓圖示**不被蓋掉**（點陣圖示保留、可編輯文字對齊原稿而非塞進圖示位）。char box 內縮幾 px，故向左走到真正字緣（停在圖示前的空白縫）再定蓋板左界（p6 CLAUDE.md 的 C 點陣起點在框左外 9px、貼 char box 會露出一條）。自限性：正常行第一字緊貼框左、清除區為空。全文僅這 4 個檔名觸發。
- **混合行（中英夾雜）字級取逐字 CJK 共識**（`style.py` 的 `_cjk_band_height`）：拉丁降部（g/p/y、括號、/）會伸到表意字帶底下約 0.15 em，整行字墨高除以 `CJK_INK_RATIO` 會灌高字級（第 4 頁「graph.sh 使用」：66px vs 純 CJK 同級行 58px → 18pt 應為 14pt）。每個 CJK 字各開欄窗測列範圍，**過半字落在中位數 ±4% 內才採叢集最大值，否則退回全字最大值（±2% 曾因正常字形差異讓叢集空集、退回 max 撿到被上行降部污染的字：p14「轉化為 AI 的 Guardrails」[80,75,93,75] 量成 24pt；±4% 仍排除 5% 的模糊突起）**——叢集規則同時剔除「鄰行墨水灌進單一字窗」（出 +14%）與「模糊突起」（序/譜 61px vs 四個同伴 58px，恰好跨 14/16pt 折點）；退回規則保住共識不足的雜訊行（12pt 的周郁凯字高散佈 39–47px，硬套中位數會錯殺正確的 47）。欄窗由逐字 advance 寬度按比例映射到框寬 — CTC word box 太抖不能用（「使用」的字框偏移半個字、量出 33px）；每字窗內縮 0.2 em；低墨全形標點（（）：。等）整個欄窗排除（全形（同樣降到帶底下）；窗內列範圍要套同樣的「空隙 >8 切群取最重」（graph.sh 框頂 3 列雜墨曾讓它反彈回 18pt）；窄欄窗**不可**套 `MAX_INK_ROW_FRAC`（CJK 橫筆畫會貫穿整窗被誤刪成 8pt）；結果以 `min(完整字墨高, 共識高)` 為準。**門檻：含拉丁降部字元的行（可單字錨定），或含拉丁字母且 ≥2 個表意字票的行**（p14 案例無降部字元但被上行 p/q 字莖橋接灌高；≥2 票防「2025 年」單字塌縮）。再放寬到所有 CJK 行已試過並回退 — ±3px 模糊雜訊讓其他臨界行掉一階、把原本一致的區塊拆散（p4 右註解 16→16/14/14），單 CJK 字的行（「2025 年」）更會塌到單字高度。「稀疏邊緣列裁切」（< 0.25×中位數）也試過並放棄（曾把 44pt 標題裁成 40pt）。蓋板與文字定位仍用完整字墨範圍 — 降部點陣必須被蓋住。
- **同段折行字級調和**（`blocks.py` 的 `harmonize_font_sizes`，在 `cli.py` 浮水印過濾後呼叫）：±3px 模糊雜訊跨在 12/14/16pt 折點上，同一段折行的兩行可能折到不同字級（p10「CB 詢圈案例/」12.6pt vs 折行同伴「法規或生辰八字」14.1pt，字形實際同高）。垂直緊鄰（間距 −0.6h~0.45h）、x 重疊 ≥40%、粗體/底色/文字色相同、**snap 前估值差 ≤14%** 的行歸入同組（12% 曾差 0.6% 漏掉 p11 的折行對 11.8 vs 13.5；真實 12-vs-14 對差約 19%；底色差門檻是 **25 不是 16** — 照片漸層讓折行同伴的 bg 估值漂移，p7 成果/零/研究 三行量出 180/160/153、曾被 16 拆組折成 18/20/20）；組內字級若散落在**相鄰** snap 階上就統一（多數決，平手取組內估值中位數重 snap），但**統一目標不可超過組內任一成員的 `max_fit_pt`（投影片邊緣硬天花板）** — p15 折行對曾被平手中位數重 snap 推回 32、再度衝出投影片。判斷用 `Style.est_pt`（snap 前、寬度夾制後的估值），不能用 snap 後值 — 兩行落在折點兩側時 snap 後差一整階。這個 pass 修掉的不一致橫跨 7 頁（觸發 Git Commit、N 個/Subagent、橘色[對比] 與另三色籌片、p15 終端機框四行等），全部經視覺驗證。**純字級觸發的自動粗體（≥24pt、無筆畫/模板量測）不可擋住折行歸組、且組統一字級掉到 24 以下時要撤粗**（`_autobold_only`，使用者回報 2026-06-13）：短的折行尾以真實未夾制字級渲染、長的折行首被投影片邊緣夾下（p15「物「準確且持續成長的表徵」。」「連結與維護。」量出真 24、其長折行首被夾到 20），24pt 的尾因 ≥24 規則自動判粗、與 20pt 常規首粗細不同而被擋在組外。放寬粗體閘門讓 `_autobold_only`（bold_r None、stroke_rel 0、font_pt≥24）的成員可入組（est ≤14% 差仍保護真標題不被併入內文）；組天花板把字級拉到 24 以下後，自動粗體的前提消失 → 跟隨被夾的常規折行首撤粗。
- **單行形狀關閉自動換行**（`builder.py`）：字級估計只要略大一點就會把整行折行、毀掉版面。
- **CJK 字型需要直接寫 XML**：python-pptx 的 `font.name` 只設定 `<a:latin>`；必須用 `_set_east_asian_font()` 注入 `<a:ea>`，否則中文會悄悄回落到佈景主題字型。
- **緞帶/色塊偵測**（`style.py` 的 fallback 分支）：當 OCR 框外圍環帶不是單一平面色時，取框內中段水平帶做雙色聚類並對各遮罩做侵蝕 — 細筆畫會消失、實心緞帶會存活；存活者為蓋板色，另一個為文字色。單純的主色佔比判斷在這裡會失敗（量化會把緞帶色拆散到多個 bin）。
- **粗體判定：≥24pt 一律粗體；≥16pt 用「模板比對」；更小用筆畫門檻**（`style.py`）：
  - **原始筆畫門檻（stroke_rel ≥ 0.13）在 16pt 以上完全重疊**：p3「Layer 3」標題（粗）量 0.108、p9「多模態輸入」（細）量 0.098 — 相對筆畫寬隨字級變大而下降、隨文字內容（拉丁 vs 密集 CJK）與對比度漂移，固定門檻救不了（使用者回報 2026-06-12：p2 核心洞見、p3 Layer 標題、p10 AI 逆向掃描全漏粗）。
  - **模板比對（`_template_bold_r`）**：該行文字用 YaHei Regular/Bold 各渲染一次（msyh.ttc/msyhbd.ttc）、縮放到同字墨高、套 σ=1.2 高斯模糊模擬 72dpi 升頻、以**該行自己的對比度相對門檻**（INK_DIST/文字-底色對比）切 ink mask、用同一個侵蝕筆畫估計器量測，r =（實測−Regular)/(Bold−Regular)。**對比相對切割正是「白字深底量得偏細」的根源**：白字深底對比（~160）比黑字白底（~235）低、固定 INK_DIST 相對切得更深、筆畫膨脹更少 — 模擬進模板後兩邊抵消（p12「1.先寫規則」深字與 p13「4. 批次整理」白字是同系列卡片標題，rel 差 35%，r 才同側）。全文校準（2026-06-12）：≥16pt 細體上限 r=0.09、粗體下限 0.17，門檻 `BOLD_R_THRESH=0.13`。σ 平移整個 r 刻度（1.0→+0.07、1.5→−0.15）但保持排序 — 門檻與 σ=1.2 綁定、不可單獨動其中一個。
  - **適用條件**：字級 ≥16pt、bg 已知、對比 ≥75、非弧形，文字先濾掉 YaHei 可能缺字形的字元（→、Ⅹ 等 .notdef 是實心塊）後 ≥2 字；w_reg > 2.5×實測 → 模板崩壞、放棄（全大寫拉丁＋模糊會黏成塊：PITCH DECK r=−2.4）。**14pt 以下模糊噪聲支配 r**（p12 同款內文散佈 −0.36…+0.10），維持筆畫 0.13 規則；14–15pt 仍算 r 但只當撤粗保護的第二意見。
  - **鮮豔深色文字（chromatic）退回筆畫規則**（`CHROMA_MAX=15`，使用者回報 2026-06-13）：模板的對比相對切割只對**無彩**文字校準（黑字白底 ~235、白字深底 ~160）。深色的有彩文字（綠/藍標籤）會破壞它——較低的色彩對比讓 w_obs 量得偏厚、相對切割又把模板削薄，r 大幅高估（p11 綠色「研究員、開發者、長期深度研究者」r=0.27、p10 綠色「用得越久」r=0.23、「綜合數十份…」r=0.16 本是常規卻判粗）。文字 chroma（max−min 通道）> 15 **且文字比底色暗**（`sum(text)<sum(bg)`）時改用筆畫 0.13 規則：綠內文 stroke_rel ~0.10 → 常規、真正的綠色粗體標題（「LLM Wiki」表頭 0.17）仍粗。**限「深色有彩」**——彩色晶片上的白字（p7 管線標籤）反鋸齒也染成有彩，但那是模板已校準的「白字深底」，文字比底色亮、不套此退回。無彩內文/表頭 chroma ≤6，有彩標籤 ≥20，門檻 15 乾淨分離。⚠️ **這條退回只覆蓋模板的「邊際帶」**（`CHROMA_TRUST_R`=0.35，使用者回報 2026-08-23）：它要擋的是模板對深色有彩文字的**高估**，而實測誤判上限是 r=0.27（上列三例）；`bold_r ≥ 0.35` 時模板照樣算數，否則 p6 深藍清單標題「2. 註解寫…」r=0.44、「4. 規則檔當上下文預算管理」r=0.57 會退回筆畫規則（rel 0.117/0.128 < 0.13）掉成常規，而同款的「3.Commit 訊息當決策日誌」因 ≥24pt 自動判粗，三行變一粗兩細。`blocks.py` 的 `reeval_clamped_bold` 有同一份判別式，兩邊要一起改。
  - **被夾到 <24pt 後重判粗細**（`blocks.py` 的 `reeval_clamped_bold`，clamp 之後、隊列投票之前跑，使用者回報 2026-06-13）：≥24pt 的「一律粗體」只是假設；`estimate_style` 對**每一行**（含 ≥24pt）都記錄模板/筆畫量測，一旦 clamp（折行調和、`sync_clamped_twins`、欄夾制）把字級壓到 24 以下、size-only 的粗體前提就消失，改信量測。真標題量得粗、續粗（p5 Git Hook 家族）；被 round-up 誤粗的表格儲存格退回常規（p10「手動/靜態」r=−0.13、「搜尋破碎的原始文字塊」r=−0.10 被同款表頭在 sync 拖到 20pt 卻留著粗體；p15「物「準確…」」r=0.11、「連結與維護。」r=0.08 被投影片邊緣夾下的折行尾）。對本就 <24 的行是冪等（粗細已等於量測）；筆畫判的粗體（無模板分數）不動。同樣套上述 chromatic 退回。
- **粗體隊列投票**（`blocks.py` 的 `harmonize_bold`，在字級調和**之後**跑）：判別器在門檻附近擲硬幣，同頁同字級、由筆畫/模板判別的行組成隊列投票：**補粗體需 ≥2/3 多數**，**撤粗體需 ≤1/5**（孤立誤判如 p7 Before；曾設 ≤1/3 誤撤 Input:/OutputA 真框標題）。可翻動帶按主判指標分流（`_bold_promote_ok`/`_bold_demote_ok`）：模板判的行補粗需 r ≥ −0.05（「Karpathy Wiki 模式」r=−0.008 靠七個粗體鄰居救回 — 拉丁字模板基準偏高害它落線下）、撤粗限 r < `TPL_MARGINAL_R`(0.22)（「人類輸入：」r=0.25 是孤身在 8 行內文隊列中的真強調、不可撤）；筆畫判的行維持 [0.115, 0.15] 帶，但 r ≥ 0.28 否決撤粗（p12 14pt 卡片標題「1.先寫規則」rel 0.144/r 0.43、「3.人類給骨架」rel 0.133/r 0.31 淹在 17 行內文隊列裡，舊規則誤撤）。隊列鍵**不含文字色**（p5 四個同款標題曾各一色）。順序必須字級先調和（SKILL.md 案例）。
- **邊緣模板粗體不可鎖死折行組**（`blocks.py` 的 `harmonize_font_sizes`）：14pt 折行尾「為體系化的高價值資產。」出生 snap 成 16pt、r=0.146 恰過門檻判粗，「粗體不同不入組」曾把它擋在 14pt 細體折行同伴組外、字級卡死 16pt。r < `TPL_MARGINAL_R` 的模板粗體不阻擋歸組；組內粗體屬**嚴格少數**時邊緣粗體跟隨多數撤掉（1v1 平手不動：p4「人類專屬：」r=0.219 保住粗體開頭、其折行尾維持細體）。
- **文字垂直定位**（`builder.py`）：形狀同時身兼蓋板（必須涵蓋整個 OCR 框）與文字容器（字形必須落在點陣字墨上）。小字時偵測框上緣留白會大於行高補償、兩者衝突 — 解法是用 `margin_top` 內距解耦，絕不要移動形狀頂端。
- **蓋板裁掉框緣外溢的彩色圖示**（`style.py` 估 `cover_x0_px`/`cover_x1_px`、`builder.py` 套用；使用者指示 2026-06-13）：偵測框左/右緣常外溢吃進鄰近的彩色狀態圖示（p13「Not That」左邊的紅 ✗、p11 Tip/Warning 的 ℹ/⚠），單一蓋板就把晶片色塗在圖示上、且粗體文字擠到圖示旁。判別用**色彩飽和度**（max−min 通道 >60）：紅 ✗／綠 ✓ 圖示飽和度極高，而帶色文字（藍/紫連結）只是輕微染色（<60），可分。三道閘門防誤裁正常文字：(a) 行首/尾字必須是真字母/CJK（行內 `×` 是內容、不裁，否則會重出一個 × 字）；(b) 圖示與文字間須有**空白欄縫**隔開（`結論：` 開頭染色但無縫 → 不裁）；(c) 裁掉後文字區仍須夠寬（>20px，整行就是圖示的「X」不裁）。**第四道閘門：文字本身飽和時整條停用**（使用者回報 2026-06-13）：飽和度判別只在「文字是深色、圖示才是飽和色」時成立。鮮豔色的標題/標籤（p2 橘色「現狀痛點」飽和 110、棕色圖表註解「傳統 RAG」飽和 78）整行每欄都飽和，邊緣掃描會停在文字內某根稀疏筆畫欄、把真的邊緣字裁掉 — 現狀痛點尾端的「點」被露出、傳統 RAG 行首「傳」被重出成「傳傳」。全文每一次觸發 trim 的行都是這種鮮豔文字行（p13「内容矛盾」等真圖示案例文字與圖示同棕色、掃描根本停不下來、本就不會 trim），故 `text_rgb` 飽和度（max−min）>60 時直接不裁 — 同色圖示無法用飽和度與同色文字分離。
- **同列重疊框要裁切**（`builder.py` 的 `_trim_row_overlaps`）：偵測器會把框外擴壓進同列鄰行的字形（例：「…書）」框尾蓋進「再批次…」框頭 43px），後畫的蓋板就會塗掉先畫的鄰行文字。在渲染圖上找兩段字墨間最寬的空白欄縫、把兩框各裁到縫中線兩側（各退 `COVER_PAD_PX`）；縫被實心墨水填滿時退而取重疊區中點。**嵌套框必須整對跳過、且邊界要鉗回兩框之內**（2026-08-16 審查）：閘門只擋住「b 從 a 的右緣之外開始」，擋不住「b 整個窄框落在 a 的寬框裡」（`bx1 ≤ ax1`）——那不是接縫重疊，取中點會把寬框腰斬、又把窄框裁成 `x0 > x1`，builder 算出**負寬度** `<a:ext cx="-…"/>`，PowerPoint 直接判檔案損毀要求修復、該塊文字消失。空白欄縫的掃描視窗還會伸出兩框之外（`x_lo = bx0−8`、`x_hi = ax1+16`），勝出的 run 可能整段落在框外，所以 boundary 另需鉗進 `[ax0+2·pad, bx1−2·pad]`，裁完再檢查兩框都沒退化。
- **垂直堆疊框要裁切**（`builder.py` 的 `_trim_stacked_overlaps`，緊接 `_trim_row_overlaps` 後跑，使用者回報 2026-06-14）：上下堆疊的兩個單行框其字墨帶若重疊，上框蓋板底會塗掉下行字頂（p8「單一」蓋住「主專案」上半成亂碼、p10「Legacy」蓋住「Codebase」——偵測框在 y 軸重疊但字形不重疊）。在 x 重疊區用各塊自己的 bg 算 ink mask 找兩字墨帶間的空白列縫。閘門：x 重疊 ≥0.5×min 寬、兩蓋板 y 確實重疊。**要裁的是「實際塗出來的色帶」而不是字墨界**（`Style.cover_band_px`，2026-08-16 審查）：裁 `ink_top_px`/`ink_bottom_px` 根本分不開兩塊蓋板——builder 之後會把色帶重新推導成「字墨 ± max(4px, 0.08×字墨高)」，等於把這一遍剛買到的 6px 間隙又還回去 `2·pad_v − 6`。實測重疊量：20pt −0.2px（剛好不碰）、24pt +0.96px、28pt +2.11px（就是 p8 單一/主專案）、40pt +5.6px、60pt +11.4px ⇒ **p8/p10 的修正在 ≥24pt 只是部分生效、字級越大越糟**。改成把最終色帶寫進 `cover_band_px`（單一 tuple，半設狀態無法表示）、builder 有覆寫就直接用；**覆寫之後仍要再套一次「自帶底色的 leading 延伸」**，覆寫才可以直接用原始幾何寫入而不會靜靜丟掉那 0.20em（也因此 `--no-cover` 下方色塊被推下去的值會自動被夾回字形位置，裁切端不必自己分模式），連帶讓 `highlight_removed` 那條（色帶取自原始框 ±max(8,0.12h)、完全不看字墨界）第一次真的能被裁到。對照組：水平方向的 `_trim_row_overlaps` 本來就是精確的（退 `COVER_PAD_PX`、builder 再加回同一個 `COVER_PAD_PX`），兩邊做法要一致。空白列掃描視窗也要涵蓋「只有 pad 重疊、字墨其實已分開」的配對（視窗取 `min(itop_lo, ibot_up)−6 … max(…)+6`）。
- **`--cover` / `--no-cover` 只決定「色塊畫在哪」，不是精緻度開關**（`builder.py` 的 `self.cover`，2026-08-16 審查）：`--no-cover` 的用途是讓底色長在文字方塊自己身上，**在 PowerPoint 裡移動文字時底色跟著走**；它**不是**「關掉蓋板機制」。三件事因此**不可**掛在 `self.cover` 上——(a) **同列／堆疊裁切**本來就沒掛（它們是幾何修正），但 help 與 README 曾宣稱有，說明必須改掉；(b) **雙色橫幅的分段蓋板**：`style.py` 只把 `bg_rgb` 記成**最右段**的顏色而 `runs` 仍逐段上色，跳過分段蓋板會把整行刷成右段色、左半白字變成白底白字整段消失、連底下的深色點陣也一起蓋掉（p2「採用 BSD 授權…」）—— 一個形狀承載不了兩種填色，所以**兩種模式都要畫**；(c) **弧形切線段**：`_add_arc_cover` 在 `--no-cover` 直接 return，若 `_add_arc_segments` 還硬寫 `fill.background()`，弧形就成了全份唯一「可編輯文字透明疊在點陣原字上」的重影（p3 緞帶）—— `--no-cover` 時改讓切線段自帶 `bg_rgb`。另外 `_add_arc_cover` 的 `_arc_geometry`（在全解析度圖上做三點字墨掃描）**必須在 `self.cover` 閘門之後才呼叫**，否則整段算完就丟。
- **`--no-cover` 的色帶比字墨高 0.20em，裁切必須知道**（`builder.py` 的 `_cover_band`，2026-08-16 審查）：文字方塊頂端是 `ink_top − LEADING_COMP(0.20)×字級`（PowerPoint 的行首 leading 補償），所以底色長在文字方塊上時，**實際塗到的最上緣就是那裡**，不是 `ink_top − pad_v`。`nudge = 0.20em` 恆大於 `pad_v`（≈0.09em），因此 `text_top < ey(cov_y0)` 這個判斷式對 **6pt–60pt 每一行都成立** —— 它不是「緊密行特例」，cover 模式下每一行都會拆成獨立色塊＋透明文字框（形狀數約兩倍，這是設計、不是 bug，但註解不可寫成特例）。`_cover_band` 在 `--no-cover` 時把這 0.20em（200dpi 下約 0.8×字級 px，24pt ≈ 19px）折進回傳的色帶，堆疊裁切才不會裁到一個色塊照樣跨過去的邊界。反過來，**`--no-cover` 時下方色塊的頂端不可被推下去**：它被字形位置釘死，推它只會讓記錄的色帶與實際塗的不符、害到第三塊。**邊界必須鉗制到 ≤ 下行字頂 − `COVER_PAD`**（使用者回報 2026-06-14）：暗色/紋理背景（p8 單一/主專案 在裂痕機器上）讓空白列掃描失敗、退回「框重疊中點」，但這個中點**低於下行字頂** → 上框蓋板仍蓋住下行字頂、且下框蓋板頂被 `max(itop, 中點+pad)` 推到字頂之下、露出自己上緣 raster。鉗制後只縮上框、下框維持蓋住自己字頂（下行字頂是可靠分界、上行字墨帶才是被鄰字灌爆的那個）。
- **空格還原**（`ocr.py`）：辨識模型會丟空格。拉丁字詞間的真實空格從逐字 word box 復原（`return_word_box=True`，間隙 > 行內字寬中位數的 1/3）— 但 CTC word box 邊緣不準（KEEP 中間出現過 17px 的假縫隙、句點框寬達 30px），所以候選間隙還必須通過物理檢查：間隙條中段的墨水比例 < 0.15（實測：真空格 0.00–0.10、假縫隙 0.24）**且完全空白欄帶寬 ≥ max(4px, 0.27×字寬中位數)**（實測：Git Commit 真空格 23px、p6 Transformer 的 r→m 假縫隙 0px — r 字形右下天生稀疏、墨水比例擋不住，但模糊讓字墨連續、空白帶寬抓得住）。**空白帶寬門檻要按字寬比例縮放、不可用固定下限**（使用者回報 2026-06-13）：CTC word box 對寬的斜筆字會少框（W 的右臂落在框外），在字內製造假的「字母間」縫隙 — p4「Wiki」被拆成「W iki」（W→i 假縫只跨 6px 空白），而真正的詞間空格（LLM␣Wiki 跨 11px、b␣C 跨 18px，字寬中位 28–34）。0.27×字寬中位讓窄的字母抖動縫不再被當空格，真詞間空格仍以 ~1.7px 餘裕通過（舊的 0.2 固定係數在字寬 28 時放行了 6px 縫）。中文字 ↔ 英數字交界一律補空格（盤古之白），因為該處的量測間隙不可靠。另外重組時必須**走訪原始文字**以保留模型自己輸出的空格 — 間隙分析只能「增加」空格、不能弄丟。
- **表格分隔線 `|` 復原**（`ocr.py` 的 `_restore_pipes` + `_find_pipe_gaps`，使用者指示 2026-06-13）：rec 模型常把細的 `|` 直線吞掉（p9 markdown 表格示範「內容 | 內容」、提示行「段落，| 需」）。難點是字間隙裡的 `|` 與 CJK 字本身的直筆畫一模一樣，純偵測「間隙裡的細高直條」會在全頁誤判數十處（編/業/格/繁 等字的直筆）。**關鍵判別（使用者點出「全型｜」的啟發）：pipe 跨越整行高、比 CJK 字還高**（barh/CJK 高 ∈ [0.9,1.3]，CJK 邊緣筆畫只到 ~0.65）、**且是有空格的獨立 token**（間隙 ≥0.85 字寬）。再加上細（≤0.22 字寬）、寬度均勻（std/mean ≤0.3，排除 `[` 括號的上下勾）、兩側各留 ≥4px 空白（排除貼著字的邊緣筆畫）。六條閘門合一後全文僅命中 4 個真 pipe（提示行 1 + 表格 3）、零誤判（checkbox 的 `[ ]`、CJK 直筆全部排除）。插入 ` | ` 並把 `char_boxes` 設 None（1:1 映射已破壞）。輸出用半形 `|`（markdown 語意正確、與已辨識的表格 pipe 一致）。
- **行尾標點復原**（`ocr.py`）：偵測器常把行尾的 。/） 裁掉。當框右側有墨水時，將行條右延 1.2×行高後以 **rec-only** 重新辨識（`use_det=False` — 小裁切跑偵測會把行打碎），只接受「補回 1–2 個 `TRAIL_PUNCT` 標點」的結果。**兩道防幻覺守衛**（p12 卡片的紅棕虛線邊框被讀成 ：、整頁長出「單次寫：」「免疫防護。：」）：(a) 行尾已是 。！？ 的行不得再補 ：；，、．；(b) 補回標點的墨水欄位若在行帶上下的行距區（±0.08h~0.5h）都有墨、而**右移一個標點寬的對照窗乾淨** → 是垂直邊框、拒收 — 對照窗是關鍵：p2 橫幅色帶的上下緣會讓行距區全寬有墨，沒有對照窗時合法的 …的書。 也被誤殺。**行尾出現開引號（「『（等）視為幻覺**（框外被切半的 。 左弧會被認成 「，p6 案例）：改重讀「行尾 5 字高寬度」的小段、以 4 字後綴比對 — 整行重讀在 3400px 長行上會丟字（翻 N 本 → 翻本）害 startswith 比對失敗；真正的折行開引號重讀仍是 「、不在 TRAIL_PUNCT 內、自動保留原文。注意：RapidOCR 的 `use_det/use_cls/use_rec` 會**跨呼叫殘留** — 每次引擎呼叫都必須三個旗標全部顯式傳入，否則 rec-only 呼叫會污染下一次完整呼叫。**行尾 CJK 詞被併讀的復原**（同函式，使用者回報 2026-06-13）：太短的框會把行尾的兩字 CJK 詞併成一個字（p13 膠囊標籤「執行 --lint 掃描」讀成「…描」，掃描→描）。重讀延長框後，若結果剛好多一字、前綴與原文（去掉原最後一字）完全相同、且復原的 2 字尾是**頁面詞彙**裡的詞（同頁「全域掃描」佐證），就採用。這些字其實在框內（框右側沒有新墨——只是更寬的裁切讓 rec 有足夠脈絡拆字），故保留原框、只改文字、跳過行尾標點的延框與邊框檢查。頁面詞彙佐證是防幻覺關鍵。
- **同晶片堆疊文字共用底色**（`blocks.py` 的 `harmonize_chip_bg`，使用者回報 2026-06-13）：一塊純色晶片上垂直堆疊的多行，逐行 bg 估值偶爾漂移、害蓋板畫出色差接縫（p7 藍色 Synthesize 晶片：步驟名「Synthesize」量成 [59,95,162]、晶片其餘行 [55,99,184]，蓋板出現一塊深色框）。把**垂直緊鄰、x 重疊、同文字色、bg 相近（Chebyshev ≤40）**的行歸組、各行 bg 取組內逐通道中位數。閘門排除 `bg_segments`（雙色橫幅）與不同填色（>40）。全文除 Synthesize 的接縫修正外，其餘變動皆 ≤2/通道（同組噪聲正規化、肉眼不可見）。
- **蓋板/文字色的分層情境**（`style.py`）：只靠環帶取樣會以兩種方式失敗。(a) 文字位於灰帶之間的近白色橫條 → 環帶取到錯的帶；以「光暈修正」解決（蓋板色改取距筆畫 4–6px 的帶 — 緊貼筆畫的 3px 是被字色染色的反鋸齒區，曾經悄悄偏移過整份文件的蓋板色；且光暈色只在與環帶色相差 ≥20 時才覆蓋，同一表面上環帶色更純）。(b) OCR 框溢出實心膠囊、壓到頁面背景 → 環帶看到外圍色、整顆膠囊被當成墨水；以 `ink.mean() ≥ 0.45 且 survival ≥ 0.45` 偵測，再用 k=3 色彩聚類重推底/字色（最實心 = 膠囊、最細碎 = 筆畫），且**重推底色必須與環帶色差 ≥40 才採用** — 密集粗體大字（p15「把精力保留…」ink.mean 0.46）會誤闖此分支，把反鋸齒灰殼當文字色、整行刷成灰。文字色一律只平均「筆畫核心」（離底色最遠的前 30% 墨水像素），因為反鋸齒會把筆畫邊緣拖向背景色。
- **光暈取樣要「往外走到顏色穩定」，不能只取一圈**（`style.py` 的 `_surface_around_ink`，使用者回報 2026-08-23）：上一條的 (a) 直接把「距筆畫 3–6px 的環」當成表面色，**卻從來沒有驗證過那一圈是不是表面**。`AI_Quality_Guardrails` 這份 deck 的白字全都帶一層**柔和暗色投影**（200dpi 下實測延伸 10–20px），3–6px 的環整圈落在投影裡 → 藍籌片 [81,144,201] 的蓋板畫成 [83,129,169]、橘色橫幅 [196,106,73] 畫成 [162,97,70]，p11–p15 共四十幾行出現「比籌片深一階」的色塊（p6/p7/p10 也中）。**修法**：從字墨往外一次走一段 3px 的帶（3–6、6–9 … 15–18），**只有當下一段與這一段一致（Chebyshev ≤ `HALO_SETTLED`=5）才承認那是表面**；平坦表面在第一段就成立（與舊行為等價），投影則會一路漂移、直到融進填色為止；五段走完仍不穩定就回報「找不到表面」、環帶色照舊生效（≥20 才覆蓋的閘門不動）。判別依據是實測分離出來的：**真的「環帶取錯帶」case 逐段完全平坦**（`Transformer_演進地圖` p1 [192,235,236]→[191,235,236]、p11 Query 一路 203–206、`The_GPT_Blueprint` p7 242–244），**投影 case 逐段單調逼近環帶色**（p13 Hook 164→178→185→190→193，環帶 197）。四份 deck 1158 行掃描：舊規則觸發 49 次，新規則保留 4 次真表面覆蓋、其餘 45 次改回環帶色。兩個附帶要點：(i) `_dilate` 用 `np.roll` 會**繞回對邊**，走到 18px 會把字墨環抹到裁切另一側，所以這條路徑改用不繞回的 `_grow`；(ii) **底色正確之後，`_chip_room_right` 的餘裕掃描才會準** —— 它拿 `bg_rgb` 當「自由欄」判準，投影會讓掃描提早撞牆、把字級夾小；修好之後 p14 規則檔骨架／決策日誌、／內建 Review／故障注入／Log 讀取分析 由 20 回到 24，目視與原稿一致。**已知未涵蓋**：p13 五張並排卡片中，第四張（讀紀錄檔／（抓未知／失敗模式，全是 4 字短行）因此完全不受夾制、snap 到 28，另外四張卡的最長行仍被卡框夾在 20，原稿五張同字級卻輸出 20/20/20/28/20。成因是 YaHei 比來源字型寬、長行必被夾（舊版是靠錯的底色「意外」把第四張也夾下去），屬既有限制。**已於 2026-08-23 由使用者裁示並修掉**——見下面 `propagate_row_clamp` 那條，現在輸出 20/20/20/20/20。
- **蓋板要吃掉文字自己的投影，堆疊的同色蓋板之間不可留縫**（`Style.glow_px`、`builder.py` 的 `_cover_band`／`_glow_reach_x`／`_trim_stacked_overlaps`，使用者回報 2026-08-23）：底色修對之後**殘留的投影反而現形**——舊版蓋板本來就是投影色，與框外的投影融成一片看不出來；蓋板改成正確的籌片色，框外那一圈投影就變成沿著蓋板邊緣的暗框。三個方向都要處理：
  - **垂直**：蓋板高原本是「字墨 ± max(4px, 0.08×字墨高)」，p13 卡片內文只有 9px，而投影量到 10–20px。`_surface_around_ink` 走了幾段才穩定就是投影半徑（settle index i → `3 + 3(i+2)` px，取**確認帶的外緣**；**第一段就穩定＝沒有投影＝0**。內緣曾用過，但 `HALO_SETTLED` 容許 5 個單位殘差，邊界上還留著約 9 個單位的投影——p14 橫幅最後一行下緣露出一條，2026-08-23 改成外緣），存進 `Style.glow_px`，`pad_v = max(4, 0.08h, glow_px)`。
  - **水平**：OCR 框左右留白不一致（p13 Hook 左邊有 22px、`(無腦零等待)` 的左括號只剩 4px），後者整條左緣露出暗痕。`_glow_reach_x` 從蓋板邊往外走 `glow_px` 欄、**碰到任何真墨水就停**（卡框、牽引線、鄰字都是墨水），所以吃不到卡片邊框。
  - **行與行之間的縫**（最明顯的那條）：p13 同卡兩行字墨相距 35px、上下各 pad 9px → 中間 17px 沒被蓋到，而那 17px 正是上一行投影的尾巴。`_trim_stacked_overlaps` 原本只處理「兩塊蓋板重疊」，`up_y1 <= lo_y0` 直接 `continue`；改成**縫也要處理**、兩塊在接縫中點會合。四道閘門避免灌滿真的間隔：(a) 至少一方 `glow_px > 0`（黑字白底 glow=0，**這條規則對它完全不作用**，格線紙背景不會被塗掉）、(b) 兩塊底色差 ≤20、(c) 縫寬 ≤ max(8px, 0.5×較小字墨高)、(d) 縫內整段無墨水（`_strip_is_blank`，同一個 >60 門檻）。三行以上的卡片天生安全：跨行配對的縫裡有中間那行的墨水，(d) 就擋掉了。
  - **仍未涵蓋**：p14 橘色警告橫幅蓋板量到 [194,104,72]、橫幅本體 [200,106,73]，差 6，放大仍看得出一塊淡方形。成因是**環帶自己也在投影裡**（`RING_PX`=4 貼著框、框又貼著字），而往外走到 18px 也只回到 [197,105,72]；橫幅本身還有紙紋與暗角，平整填色無論如何都會現形。  - **後續（2026-08-23 使用者再報，已修）**：外推整個環帶取「主色」確實沒用，但**取「離投影色最遠的那一段邊」有用** —— 見下一條。
- **底色要在投影之外重新取樣，取離投影最遠的那一段邊**（`style.py` 的 `_glow_free_bg`，使用者回報 2026-08-23）：`RING_PX`=4 貼在「本來就緊貼字形」的框外，所以**環帶自己也在投影裡**，蓋板比籌片暗個幾階；小色塊看不出來，大片平整填色就變成一塊淡淡的方形（p14 橘色警告橫幅：蓋板 [194,104,72]、旁邊橫幅 [200,106,73]）。修法：量到投影（`glow_px > 0`）時把環帶整個外推 `glow_px` 再讀一次**四個邊**，取**離投影色（`_surface_around_ink` 回傳的第一段帶色）最遠**的那一段 —— 投影只會把樣本往自己的方向拉，所以同表面上離投影最遠的那段汙染最少。兩道閘門擋掉「已經不是同一個表面」：該段主色與原環帶差 ≤`GLOW_BG_DIST`(20)、share ≥`BG_MIN_SHARE`（p14 第一行的 `bot` 段已離開橫幅落到頁面上，[254,242,227] share 0.38，兩關都不過）。實測 p14 橫幅 → [201,106,74]（真值 200–201）、p13 藍卡 → [82,143,199]（真值 [84,143,199]）、p11/p12/p15 皆 ±2 內；四份 deck 掃描只動底色、不動任何字級。
- **偵測框切到字形時要沿著字墨走出框外**（`style.py` 的 `_ink_run_out`，使用者回報 2026-08-23）：p15 步驟徽章的「2」框只有 209×240，兩個同款兄弟是 248×359／283×389 —— 偵測器把 2 的上弧與底橫都切掉，蓋板因此蓋不住那兩塊點陣（上方露一道弧、下方露一條橫槓），可編輯的 2 還比 1／3 低 64px。判別：**框自己的邊緣列有墨（≥0.15 框寬），且與字墨帶之間沒有任何空白列**（鄰行殘墨一定隔著空白列，本檔記載的案例都 ≥9 列）。**邊緣測試要用 `present` 而不是 `rows`**：2 的底橫佔框寬 99%，`MAX_INK_ROW_FRAC` 剛好把它從 `rows` 剔掉、而切口正在那裡。四道防誤判缺一不可：
  - **環帶必須是平整背景**（`ring_flat`，share ≥`BG_MIN_SHARE`）：漸層晶片上（`Transformer_演進地圖` p8 的青→琥珀「Mixtral 8x7B」膠囊，環帶 share 0.24）整片晶片對 `bg_ref` 都算墨水，「字形還在延續」在框外根本沒有意義。
  - **只對 `ink_h_px ≥ MIN_CUT_BAND_PX`（60px ≈17pt）的行做**。插圖裡的 8–11pt 標籤四面都貼著圖形，讓它們走出去會改字級 →`drop_illegible_lines` 的弱鄰域擴散跟著翻盤（`Transformer_演進地圖` p1 的 9pt `Sparse` 變 11pt，一口氣救回七個本該留原圖的圖表標籤）。
  - **走到 `cap`（0.6×字墨帶高）還沒遇到空白列就整個放棄**：那不是被切的字，是緞帶邊或整片填色，跟下去會吞掉半頁。
  - **兩側邊距都有墨的列＝畫出來的線，不是字**（`CUT_MARGIN_PX`=12，兩側各 ≥50%）：表格橫格線與公式根號上橫線天天壓在框邊（`The_GPT_Blueprint` p3 儲存格列、p6 的 √dk）。2 的底橫只佔自己的字寬、兩側邊距乾淨，照樣走得過去。
- **同列標題碎片不可跨越欄線合併**（`blocks.py` 的 `_rule_between`，使用者回報 2026-08-23）：表格同列的兩個儲存格，長得跟「被打散的標題碎片」一模一樣。p11 的 `/code-review` 與 `抓正確性 Bug` 兩道幾何閘門都只差一點點就過（垂直重疊 0.64 vs 門檻 0.60、水平間距 119px vs 上限 126px），合併後成為一條 32pt 橫跨整個表格的行，第二欄的點陣還從蓋板下露出來。幾何給不出的訊號是**那道欄線**：間隙欄裡只要有一欄的墨水覆蓋接近整個列高（≥0.8）就是分隔線、不合併。p6 那組真碎片之間只有空白，照常合併。附帶修好被合併行拖著的「分級」（32→28，回到與同欄儲存格一致）。
- **`propagate_row_clamp` 不可要求「並排」**（使用者回報 2026-08-23）：原本規定被拉的堆疊必須與被夾的那些左右並排（x 重疊 ≤0.2×較窄者），理由是「同欄的上下兩塊是標題＋內文」。但 p10 三個註解框的**最後一行**被量高一階，它與同框前兩行**同欄**、不並排，於是拉不動（原稿三框九行同級）。多數決本身已經足以擋住 p15 的「少數拖多數」，並排這條只擋到正確的情況，移除。
- **並排卡片同列共用字級，且必須是多數決**（`blocks.py` 的 `propagate_row_clamp`，在 `propagate_column_clamp` 之後、粗體重判之前跑，使用者裁定 2026-08-23）：p13 一列五張卡，其中四張各有一行長到被卡框夾住（YaHei 比來源字型寬，長行留不住原字級），第四張三行全是四字短行、完全不受夾制、snap 高兩階 → 輸出 20/20/20/28/20，原稿五張同級。`sync_clamped_twins` 已帶著「同家族混雜比全家一致醜」的裁示（2026-06-13 p5 Git Hook），但它只跨一階、又要求孿生**剛好**等於被夾行想要的字級，兩階差穿不過去。兩條規則讓它不會把一次誤夾放大成一整列：
  - **以「堆疊」為單位、用組內 est 中位數比對**，不是逐行：p13 的「（抓未知」量到 28.4（全形括號撐滿 em 框），同卡另兩行 25.5/26.4；逐行比對只會拉下三行中的兩行、把卡片自己拆成兩級。
  - **被夾的字級必須是這一列家族的多數**：p15 一列三欄引言，第一欄開頭行**本來就被誤夾**到 20（est 26.6、自然階 28），另外五行量到 24——少數拖多數會讓整頁失去「引言 vs 內文」的層次。多數決之後 p13（五分之四在 20）照樣生效、p15（四分之一）完全不動。
  （曾另有一條「被拉的堆疊必須與被夾的那些左右並排」，**2026-08-23 已移除**——見上一條 p10 的註解框案例。）四份 deck 全跑：guard 只動 p13 那三行，`The_GPT_Blueprint`（p3/p6/p8/p10/p12）與 `Reinforcement_Learning_Blueprint`（p6/p8/p13）共 26 行往下修，逐頁目視都是「同列的兩欄／四個註解框回到同一級」，與原稿一致。
- **筆畫核心跨 bg 兩側要選單側**（`style.py` 的 `_core_color`，使用者回報 2026-06-14）：當「離 bg 最遠的前 30% 像素」**同時含比 bg 暗與比 bg 亮的兩群**（彩色/3D 框上有暗邊框/陰影＋白字：p12「開發」灰色 3D 按鈕暗邊框＋白字、p14「根本沒提早完工」暗卡片＋白字），把兩端平均會得到無意義的中灰 [172,176,177]、白字被刷成灰幾乎隱形。**解法**：核心像素跨 bg 兩側時收斂到單側——有**夠多亮像素（≥0.2）即判淺色文字取亮側**（這類簡報每個填色框都是白/淺字），否則亮像素只是反鋸齒雜點、取暗側。對「全部同側」（一般深字白底、白字純色底）是 no-op、不動既有校準。全文修好 p12 開發、p14 根本沒提早/重啟計量、p13 灰塊 確診/繪製 等白字，深字行（p2/p3/p5/p9/p11 內文）零回歸。**未涵蓋**：純暗照片上的黑字＋白光暈（p5「高耦合金流邏輯」橘色電路球上）——bg 取到照片色、非框，仍 faint。
- **弧形文字**（第 3 頁緞帶橫幅）：弧度只能從**救援片段中心**擬合（`_arc_from_fragments`），純影像逐欄追蹤法已嘗試並放棄 — 平行的兩條弧線框深度交疊、互相污染、連符號都會判反。高分的鄰行用「夥伴傳播」繼承曲率（同半徑 → 弧深隨寬度平方縮放）。輸出端 PowerPoint 的 `prstTxWarp`（textArchUp/Down）也已嘗試並放棄：其下沉量 ≈ 0.13×框高（實測校準），但字級/路徑縮放行為不透明，長文字會繞圈重疊 — 改用 **3–6 段切線文字方塊**沿拋物線排列（局部切線角 = atan(2·弧深·t/半寬)），色塊用 12 條沿弧條帶（縱向邊距 0.3 字高吸收拋物線 vs 圓弧的側翼偏差）。段落切割用 em 寬度加權並貼齊空格。拋物線錨點**不能用 bbox**（救援片段會把框灌高：中段錨點實測低了 39px、弧深 172px vs 實際 ~97px）— `_arc_geometry` 用渲染圖在中心與 ±0.65 翼側量「文字色字墨 run」重新錨定，每個探測點取離預測最近的 run（同一緞帶有兩行弧線、視窗會同時看到兩條字墨帶）。條帶高 1.5×字高（2.2× 會把上緣的柔和漸層蓋成一條硬邊 — 使用者回報過）；垂直範圍再以 `_ribbon_limits` 夾進緞帶本體（只縮不放）：字墨中斷的橋接只允許在距切線中心 ±0.9 字高內，區外只容 10px — 否則會跨過「邊緣帶+白縫」橋接到同色的虛線、夾製整個失效；並沿條帶取 3 個樣本欄、在切線座標系取極值，旋轉後的角落才不會凸出緞帶（曾在翼側產生凸塊）。相鄰條帶的角差會在底部張開楔形縫，條帶寬度要加 `strip_h × Δslope` 的重疊量補縫。
- **直排 CJK 文字**（`style.py` 的 `_is_vertical_cjk` + `_estimate_vertical`、`builder.py` 的 `vert="eaVert"`；使用者指示 2026-06-13）：偵測框內字垂直堆疊的純 CJK（p7 象限圖 y 軸標籤「純寫作」「技術開發」）——框高窄、≥2 個純 CJK 字、且每字格近正方（框高/字數 ÷ 框寬 ∈ [0.55,1.7]）。字級取**字欄寬**（每字與欄同寬）而非堆疊高、蓋板涵蓋整框、builder 用東亞直排文字框（bodyPr `vert="eaVert"`、置中、無 leading 內距）。原本當水平行處理 → 字級量太小、文字水平排不蓋住直排點陣（使用者回報）。全文僅這兩個標籤觸發、零誤判。
- **傾斜需要基線：單一字形的角度一律丟棄**（`ocr.py` 的 `_tilt_has_baseline`，使用者回報 2026-08-17）：文字的方向是由「多個字沿基線排列」定義的，**只有一個字就沒有基線**，偵測器的 min-area quad 會改鎖到該字自己的筆畫方向或任意對角線。p15 圓形步驟徽章裡的白色「1」因此回了 **26.31° 的旋轉四邊形、分數卻高達 0.956**（不是低分救援路徑），渲染出來是一撇斜的筆畫、看起來就是斜體「7」。兩份 deck 共 29 頁全掃：**合法的傾斜行全部 ≥2 字且去斜後明顯狹長**（p6 三個「Step」，角度 5.1–5.9°、w/h 1.83–1.90），**而每一個單字元傾斜都是假的**（'1' 26.31° w/h 0.96、'←' −6° 、'と' −26.9°）。閘門放在兩處共用：偵測器 quad 的角度計算，以及旋轉救援的 `elif ang` —— 救援的**文字照樣採用**，只丟掉沒有基線支撐的角度。**不可改用「去斜後 w/h 狹長」當閘門**：兩字的斜置籌片（p8「98」）w/h 只有 ~1.1，會被一起殺掉。丟掉角度後該行走一般路徑（色塊＋文字框，形狀數 +1），點陣仍被完整蓋住。
- **傾斜文字，兩種情況**：
  1. 偵測器回傳「旋轉 quad」（例如第 5 頁 26° 的金字塔色帶）：`ocr.py` 把角度/中心/轉正尺寸存進 `Line`；`style.py` 用 PIL `Image.QUAD` 轉正後的貼片取樣顏色與墨水（若改用軸對齊外接框取樣會混入周圍顏色 — 那個 bug 曾產生蒼白的蓋板）；`builder.py` 輸出旋轉形狀（`shape.rotation`，兩者皆順時針為正，PowerPoint 以形狀中心旋轉）。
  2. 偵測器把傾斜文字合併成「軸對齊」框（第 3 頁的弧形緞帶）：該行分數會 < 0.75 並觸發旋轉救援 — 將外擴裁切以 ±3/6/9° 重新 OCR，只保留「自身高度帶跨過裁切中心」的片段（否則鄰行會混入），在「長度不超過原文 2 倍」的前提下取最長、分數最高的組合。PCA 與投影剖面去斜都在這種雜訊裁切上失敗過；暴力法勝出。當 quad 已帶角度（情況 1）時跳過救援。救援成功時同時重建旋轉矩形（用 `p_in = c_in + R(+θ)(p_out − c_out)` 映射回原圖，已數值驗證），輸出形狀同樣旋轉 — 高度要用片段高度的**中位數**，不能用 quad 聯集（弧形片段會把聯集灌爆：243px vs 實際 ~80px）。
- **s2tw 只准改「不可能是繁體」的字**（`ocr.py` 的 `_is_big5`／`_emits_simplified`，使用者回報 2026-08-23）：OpenCC 會把**兩種寫法都是合法繁體**的字對合併掉（划/劃、升/昇、台/臺、注/註），在一份模型讀對了的頁面上那是純粹的破壞——實測四份 deck 23 次轉換裡有 7 次是這種傷害（`AI_Quality_Guardrails` p2/p15「划得來」變「劃得來」、p1「拉升」變「拉昇」、p5「注定」變「註定」、p7「他那台壞」變「他那臺壞」，每一個都與渲染圖逐字核對過），真正的簡體殘留只有 14 次。**判準是 Big5**：`cp950` 編得出來的字本來就可以是繁體（划／升／台／注／征），編不出來的（恶／实／内／两／决／纪／装／试）才是模型滑進簡體的鐵證。沒有鐵證時只改編不出來的那些，其餘保留原樣。**鐵證以「頁」為單位**（`_emits_simplified`）：同一頁只要出現一個 Big5 編不出的字，就代表模型在這頁確實在滑，該頁的模糊字對照舊信任 OpenCC（`Transformer_演進地圖` p12 靠同頁的「训」把「新表征空間」修回「新表徵空間」）。**已知未涵蓋**：`The_GPT_Blueprint` p6 整頁沒有任何 Big5 編不出的字，「特征」因此留著沒修成「特徵」（原圖確實是「特徵」，屬模型滑動）——但那是一份簡體來源的 deck，代價可接受。純簡體行仍由既有的 `_confirm_simplified_strays`（頁面詞彙佐證）處理，不受影響。
- **有彩深色文字的退回只覆蓋模板的「邊際帶」**（`style.py` 的 `CHROMA_TRUST_R`=0.35，`blocks.py` 的 `reeval_clamped_bold` 同步，使用者回報 2026-08-23）：`CHROMA_MAX` 那條退回是為了擋住模板對深色有彩文字的**高估**，而實測到的誤判上限是 r=0.27（p11 綠色「研究員…」）。p6 的深藍色清單標題「2. 註解寫「為什麼+實測數字」」「4. 規則檔當上下文預算管理」量到 r=0.44／0.57（chroma 50，被判有彩），退回筆畫規則後 rel 0.117／0.128 < 0.13 就掉成常規，而同一組的「3.Commit 訊息當決策日誌」因為 ≥24pt 自動判粗——同款三行變成一粗兩細。改成 **r ≥ 0.35 時模板照樣算數**：0.35 遠高於所有實測誤判（≤0.27）、又低於這三行（≥0.44），乾淨分離。
- **`harmonize_stacked_overlap_size` 的「自然字級相近」要 10% 不是 15%**（使用者回報 2026-08-23）：p7 橘色註解框的標題「盲改 CSS3 次不中」（字墨帶 114、28pt）與它下面的內文「AI 講得很有道理，但它沒去看」（帶 97、20pt）只差 14.9%，**擦邊過關**後標題被一路拖下兩階到 20pt，與對面同款標題「看真實 DOM，2 分鐘結案」（28pt）不一致。這個形狀是為「同一個標籤的折行對」設計的，那些實測差距遠小得多（p13「第三階段：」122 vs「擴展與制度重構」114＝6.6%），10% 保住它們、擋掉標題＋內文。
- **投影半徑要取「確認帶」的外緣**（`style.py` 的 `_surface_around_ink`，使用者回報 2026-08-23）：`HALO_SETTLED` 容許 5 個單位的殘差，所以用**內緣**當半徑時邊界上還留著約 9 個單位的投影——p14 橘色橫幅最後一行的下緣就露出一條。半徑改成 `3 + (i+2)*3`（確認那一帶的外緣），實測該行 dy=12 的殘差 8.7 移到 dy=15 的 4.x，目視消失。
- **`sync_clamped_twins` 的折行組要整組一起拖，孿生比對不看行長**（使用者回報 2026-08-23）：兩個相反方向的災情。(a) p15 引言首行「AI 讓「一直以來划不來」（24）與內文「過去昂貴的品質工作，現」（被夾到 20、est 24.4）est 只差 8.4%，被拖到 20，而它自己的折行尾「的事」變得划得來。」（est 28.4）差 16.5% 沒被拖，一句話裂成兩級。(b) 反過來，p11「分級」／「(low~xhigh)」這格，孿生條件完全符合、只卡在 **em 長度比**（2 em vs 6 em）這條「像不像同一族」的啟發式上，結果整格留在 32、與整張表的 28 不一致。**修法**：先找出 j 的折行同伴（同字級、相鄰、x 重疊、同粗細/文字色），**同伴也要通過孿生檢查（但不看 em 長度——同伴本來就屬於這裡）**，通過就整組一起拖、不通過就整組不動。p5 Git Hook 的孿生是獨立標籤，沒有同伴，這條閘門對它是空的。
- **「這頁的模型在滑」這個判斷要收得很緊**（`ocr.py` 的 `_emits_simplified`，2026-08-23 code review）：上一條用「同頁出現 Big5 寫不出的字」當作放行 OpenCC 的鐵證，但**三種無辜的字也會踩到**，任何一個都會把整頁的保護關掉：(a) **刻意呈現的純簡體內容**（CLAUDE.md 自己的 `掌控习惯`）——那是受保護的內容而不是誤讀，所以**純簡體行沒有投票權**；(b) **不是簡體形的異體字／外文漢字**（喆 堃 犇、辻 働 図）——所以還要求該字**確實會被 s2tw 改寫**；(c) **插圖裡的亂碼行**——所以套上與 `_page_vocab` 相同的信心下限（score ≥0.8）。三道一起加之後，`['划得來的成本','他那台壞了','干擾訊號','後台管理','掌控习惯']` 這種頁面不再被一行簡體示範字整頁解除保護。
- **`_confirm_simplified_strays` 也要吃同一道閘門**（同上）：它在逐行修正**之後**才跑、直接呼叫 `self._s2t.convert`，只靠「改動的 2–4 字詞出現在頁面詞彙裡」把關——而**一份 deck 只要同時寫過 台北 與 臺北，它就自己提供了那份佐證**，於是把剛保住的 台 又改成 臺，在 台 的點陣上蓋一塊 臺。沒有頁面級鐵證時，這裡也只改 Big5 寫不出的字。
- **`sync_clamped_twins` 用 `harmonize_font_sizes` 的真實分組，不要自己近似**（`blocks.py` 的 `_wrap_pair`／`_wrap_partition`，2026-08-23 code review）：折行組的判準（垂直間距 −0.6h/0.45h、x 重疊 0.4、文字色 ≤45、底色 ≤25、est ≤14%、邊際粗體例外）本來在檔案裡被手抄了好幾份，抄歪的地方就是災情：**只看直接鄰居**會讓三行以上的段落只被拖走前兩行（A–B–C 鏈上 A 與 C 不相鄰）；**漏抄 bg／est／angle 閘門**會把旋轉籌片也一起改字級、或讓一個根本不同晶片的鄰居否決掉正確的拖動；**嚴格比對粗體**又會讓 1v1 的「邊際粗體＋常規」折行對漏掉。改成單一 `_wrap_pair` 述詞 + `_wrap_partition` 傳遞閉包，兩邊共用。**分組必須在任何改動之前算一次**：從即時的 `font_pt` 推導會讓結果跟著行的排列順序跑（同一份輸入，由上而下與由下而上得到不同的孤兒行）。**拖動的判準是「組內 est 中位數」**：p15 引言首行單看自己與被夾內文只差 8.4%（會被拖走、把一句話拆成兩級），組中位數 27.5 差 11.3% 就正確拒絕；p11「分級／(low~xhigh)」組中位數 31.8 對上 31.0 的夾制行則整格一起下來、不再從整張表突出。
- **`_glow_free_bg` 的取樣半徑要比蓋板的伸展量少一段**（`style.py`，2026-08-23 code review）：`glow_px` 是**蓋板**要吃掉的距離（確認帶的外緣），把重新取樣的環也推到那麼遠會走出填色——四份 deck 實測 16 行底色漂移 7–14 個單位，其中數行整個落到鄰近表面（`Transformer_演進地圖` p2 `Pre-RMSNorm` 248→234、p4 的 K 籌片）。而且「取離投影最遠的一段」這條選擇規則天生偏好漂到亮鄰居的那一段。取樣改用 `glow_px − HALO_BAND_PX`，蓋板維持外緣。
- **`harmonize_across_dropped` 不可越過字級天花板**（`blocks.py`，2026-08-23 code review）：它是檔案裡唯一會把字級**往上**設、又不檢查 `max_fit_pt`／`clamp_pt` 的地方，實測可以把一個 `max_fit_pt=20` 的段尾推到 24——正是 CLAUDE.md 記載「統一目標不可超過組內任一成員的 max_fit_pt」要防的 p15 衝出投影片。往上調時夾進兩個天花板，往下或持平時照舊。
- **2026-08-23 code review 留下、尚未處理的三個結構性風險**（有紀錄才不會被下一輪「順手修好」或悄悄踩到）：
  - **`_surface_around_ink` 對投影深度的反應不是單調的**：投影若深到把最前面兩段都灌滿（合成測試：投影核心 ≥18px 平坦），兩段會「一致」，函式就回報**投影色本身**當表面、且 `glow_px=0`（因為半徑寫死 `0.0 if i == 0`），等於把 73dd4b3／fcf74ee／43bbdc7 三次修正整組關掉。CLAUDE.md 量到的真實投影是 10–20px，失效帶就在這個範圍的上緣。真實 deck 沒有重現（四份掃描全數正常），但**「走完五段仍不穩定」的哨兵值與「最後一段才穩定」的半徑同為 18，兩者無法區分**——要改的話得改成拿收斂後的顏色（而不是帶的索引）當判準，並給 `HALO_BANDS` 留出 18px 以外的餘裕。
  - **`harmonize_bold` 會用 `bold_r` 推翻 `CHROMA_TRUST_R` 剛做的判斷**：隊列鍵只看字級（刻意不含文字色，也沒有 chroma 閘門），`_bold_promote_ok` 只要 `r ≥ −0.05` 就補粗、`_bold_demote_ok` 只在 `r < 0.22` 才撤粗——於是同一個 `bold_r` 在檔案裡有三個互相矛盾的信任門檻（有彩時 <0.35 不可信、補粗時 ≥−0.05 可信、撤粗時 <0.22 可信）。`Reinforcement_Learning_Blueprint` p3「延遲回饋」(r=0.29) 實際走過這條路，但最終輸出**是對的**（原稿三行同粗），所以目前是一致性缺陷而非可見回歸。
  - **`_cover_band` 的垂直 `pad_v` 沒有墨水測試**，而同一個半徑的水平版本 `_glow_reach_x` 有（它的 docstring 就寫著「不可吞掉卡框、牽引線、鄰字」）。`_trim_stacked_overlaps` 只保護「兩個文字塊」的配對，卡框／格線／牽引點不在其中。另外接縫灌填的閘門量的是**加了 pad 之後**的間隙，所以 `glow_px` 每加 3px，可被灌填的真實設計間隙就多 6px。
- **簡體混入修正**（`ocr.py`）：辨識模型偶爾吐出簡體字形（惡→恶）。OpenCC **只套用在繁簡混雜的行** — 純簡體的行是刻意呈現的內容（例如第 4 頁畫面中的搜尋詞「掌控习惯」），必須保留。行級平手（規则）純文字無法判斷，預設轉繁體。字典必須用 **s2tw 而非 s2t**（使用者回報 2026-06-12）：s2t 是正體舊形字典，會把台灣慣用的合法繁體字也改寫 — 「越吃重」被轉成「越喫重」；s2tw 保住 吃、照樣修 恶→惡。**整行 CJK 全滑成簡體時混雜測試不會觸發**（p3「AI 执行：」、p9「LLM 合规」— 拉丁字母不影響 t2s 往返、行被當成純簡體保護）：`_confirm_simplified_strays` 用頁面詞彙佐證解套 — 轉繁後「有變動的 CJK 字串」（2–4 字）逐一在同頁高分行詞彙中找到才採用（執行 由同頁「…AI 執行的自動化編譯…」佐證）；找不到佐證就維持原樣，刻意呈現的純簡體內容（掌控习惯）因此仍受保護。
- **行內多重背景填色（雙色橫幅＋行內 highlight）**（`style.py` 的 `_detect_bg_segments` + `_split_color_runs_segmented`、`builder.py` 的 segment 蓋板；使用者指示 2026-06-13）：一行可能橫跨多塊不同背景填色——整行的雙色橫幅（p2「採用 BSD 授權…」前半深底白字、後半白底黑字），或行內被 highlight 的程式碼/關鍵字色塊（p2 `Perl`/`Email`/`Markdown`/`Markdown.pl` 淡紫/淡藍碼片、p6 `==螢光筆==` 紫色塊）。用**框上下緣列**（字帶外的純背景，不受字墨密度影響——全欄中位會被密筆畫拉黑、單一 bg_ref 的 ink mask 在橫幅遠側會反相）逐欄取背景色，再以游程編碼（running mean，相鄰欄差 ≤`BG_SEG_MERGE` 併為同一塊）切成數塊平整填色。**通用閘門排除漸層/照片**：漸變欄會逐步併成一塊；任何塊自身欄色 std > `BG_SEG_STD`(16) 即整行作廢（漸層/照片永遠收斂不成平整塊）；相鄰塊色差需 ≥`BG_SEG_DIST`(32) 才算刻意。內部邊界精修到**逐通道中位交界中心**（視覺幾何交界、天生落在字間空隙）——**不可貼齊 char box 空隙**：char box 有 padding（p2「個」墨水到 ~1911、框卻灌到 1931），貼齊會把色塊往右推 27px、突出到鄰塊（使用者回報）。**分界兩側文字色是否不同決定保留或移除色塊**（`_split_color_runs_segmented` 回非 None=不同色）：(a) **文字跨塊變色（橫幅白↔黑、p6 螢光筆紫↔白）→ 保留色塊**並逐字按所屬區段背景量色（否則白底會被當墨水量成白字）。`bg_segments` 存各區段 `(x0,x1,bg)`；builder **先鋪一整條最右側填色的全寬底板**（接縫永不露出底圖、即使色塊被手動微移）、再把較左區段疊到各自邊界、最後疊透明文字框承載分色 run。(b) **文字同色的純行內 highlight（Perl/Email/Markdown/Markdown.pl）→ 移除色塊**（使用者指示 2026-06-13）：色塊畫在原稿欄位，但輸出字型步進寬與原稿不同、中段字前面累積位移、色塊對不上文字（使用者否決）；純深字落在底色上仍清楚，故 `highlight_removed=True`、改用**符合環帶背景的底色**（**不是最寬區段**——Markdown.pl 的紫塊比周圍文字寬、取最寬會把整行刷紫）整片蓋掉原稿色塊，蓋板涵蓋整框高 ±max(8,0.12h)（色塊常溢出 OCR 框幾 px、不夠寬會露一條）。全文 5 處 highlight + 1 橫幅全數偵測命中、零誤判（已逐一比對原稿）；其餘行維持單一蓋板。
- **上標註腳標記**（`style.py` 估 `Style.superscript_tail`、`builder.py` 的 `_mark_superscript` 設 run `baseline="30000"` + 0.65 倍字級；使用者指示 2026-06-13）：行尾的註腳標記 `[1]`（p10「需要出處佐證[1]」）是縮小上移的上標、被 rec 當成正常字。判別：text 結尾符合 `\[\^?\d+\]$`、且**該標記字墨明顯上移**（底部離本文底 ≥0.2 本文高）**又較矮**（≤0.92 本文高）。**程式碼框內字面 markdown `[^1]` 不上移（raised 0）→ 排除**；行首的註腳定義標記 `[1]`（不在行尾）也不符。builder 把行尾 N 字切成獨立 run、設上標基線與小字級。全文僅 Render 面板那個 `[1]` 觸發。
- **刪除線**（`style.py` 估 `Style.strikethrough`、`builder.py` 設 run 的 `strike="sngStrike"`；使用者指示 2026-06-13，2026-06-14 重寫判別）：字墨中線有一條橫貫的細線（p9 `~~作廢內容~~`）。**純覆蓋率無法分辨「畫出來的線」與「碰巧對齊的 CJK 橫筆」**——使用者校驗的這份文件完全沒有刪除線，舊的「中段某列覆蓋 ≥0.75 且 ≥1.5×中位」卻在三行誤判：牽一髮動全身（「一」本身就是滿寬橫條＋鄰字中段橫筆對齊，峰 0.84）、單一（「單」堆疊多條 ~0.75 橫筆帶、峰僅 0.77）、(Context Engineering)（拉丁 x-height 密集、峰僅 0.65）。真刪除線是**單一、細、近滿寬、且疊在稀疏字身上**的線，三個誤判各破壞其中一條。新判別（字墨帶內 cov≥0.70 的連續列為「帶」）：**恰好一個帶**（排除單一的 4 條堆疊帶、與 Context 的零帶）、帶**厚 ≤0.10×字高**（排除 ~0.13×字高 的「一」字橫條）、峰 cov **≥0.80**（排除單一/Context 的 <0.80）、帶中心落 0.30–0.70h、峰 ≥1.5×全列中位、且**帶外字身中位 ≤0.6×峰**（排除牽一髮動全身的密集字身 0.48）。閘門仍限水平、非弧/斜、≥2 字。**取捨**（使用者裁定 2026-06-14）：緊排 2 字若真有刪除線可能漏判，但遠勝誤判——這類簡報極少出現刪除線。**厚度閾值 0.07→0.10**（使用者回報 2026-06-15，P9 更新後加上真刪除線）：真刪除線 band_h=5px / rh=67＝ratio 0.0746，舊的 0.07 差 1px 漏判。放寬到 0.10：全文掃描確認「作廢內容」是**唯一**通過其餘所有閘門的近候選。**但「三個舊誤判在前置閘門就被排除」這句話只對其中兩個成立**（2026-08-16 審查更正）：單一 靠 band 數（4 條堆疊帶）、(Context Engineering) 靠零帶，都與厚度無關；**牽一髮動全身 四個前置閘門全過**——peak 0.84 ≥ 0.80、ctr ~0.5 在 0.30–0.70 內、body 0.48 ≤ 0.6×0.84 = 0.504 —— **厚度是它唯一的判別器**，而餘裕已從 0.07-vs-0.13（1.9×）縮到 0.10-vs-0.13（1.3×），body 距離翻轉只剩 5%。⚠️ **不可再放寬**。`band_h/rh` 本身也帶字重（粗體上的刪除線畫得比較粗），正解是改用同函式 130 行前已算好的 `stroke_w` 正規化（`band_h ≤ K×stroke_w`，對字級／字重／DPI 都免疫），但那要有 SOURCE.pdf 做視覺回歸才能定 K —— **沒有視覺驗證就不要動這個數字**。另：**`strikethrough` 在 builder 必須加 `len(block.lines) == 1` 閘門**（與同處的 `runs`/`superscript_tail` 一致）：合併後的 block 只保留第一行的 style，`--merge-lines` 會把整個合併段落全部畫上刪除線。
- **多色行**（`style.py` 的 `_split_color_runs` + `builder.py` 的 `_split_text_runs`）：用逐字 word box 取每個字的顏色並分組成 run（合併門檻 ≤45、只有色差 ≥60 才真正分段 — 不對稱門檻防止模糊雜訊拆散單色行）。run 的字數涵蓋「去空格」後的文字；`Line.char_boxes` 攜帶字框，凡是破壞字元 1:1 對應的轉換（救援）都要把它設為 None。**夾在兩段同色 run 之間的單字 run 是 CTC 字框抖動**（p4 Idempotent 的尾 t 字框壓到「(」字縫、取到反鋸齒灰 150）— 併回鄰段，不要讓單字雜訊拆 run。
- **無蓋板救援**（`style.py`）：斜置籌片（p8 等距視角的綠色平行四邊形）的軸對齊框同時罩到籌片+頁底+鄰籌片，環帶混色、雙色聚類也失效（兩個叢集都是實心面、無侵蝕存活差）→ bg 判 None → 透明文字疊在點陣數字上出現重影。救援：取字墨帶光暈（dilate 3→6 的環）主色，佔比 ≥0.65 即當蓋板色；漸層/插畫區主色不純、維持無蓋板。
- **頁面詞彙校正**（`ocr.py` 的 `_vocab_correct`）：最小的籌片字退化到模型極限（p9 資訊 讀成 黃訊 0.66、驅動 讀成 驅勤），但同頁通常有大字高分行寫著同樣的詞（商業驅動>資訊架構>系統建設 0.85）。低分（<0.8）2–4 字純 CJK 行若與頁面詞彙（高分行的 2–4 字子串）恰差一字就校正。**候選以頻次裁決、嚴格最高頻才採用** — 跨詞邊界子串會污染候選（架的 來自 框架的…），但真詞會在頁面重複出現（架構 在 資訊架構+RAG架構）。
- **同籌片漏行救援**（`ocr.py` 的 `_rescue_sibling_bands`）：偵測器整行漏掉小籌片的第二行（p9 資訊 下面的 架構，det 任何解析度/模型都看不見 — 試過 max_side_len 4096 與 det server 模型，更糟）。對 2–4 字小 CJK 行上下探帶、rec-only 重讀，**只在頁面詞彙能驗證（精確或差一字）時採用** — 未驗證就插入會蓋掉正確點陣字卻顯示錯字，比漏掉更糟（架構 band 讀出來是 架樽/架模）。
- **警告圖示修正**（`ocr.py`）：行首的 ⚠ 三角形會被辨識成 'A'。判別器：圖示底部是一條連續墨水帶（佔框寬 0.93）、真正的 A 是兩隻分開的腳（0.24）；門檻 0.6，只套用在「行首 A、後接空格或中文」。
- **浮水印品名是清單、不是單一字串**（`cli.py` 的 `WATERMARK_MARKS`，使用者回報 2026-08-17）：產品會改名而**已匯出的舊 PDF 仍帶舊標記**，所以字尾比對必須同時收 `notebooklm`（2026 年中以前）與 `gemininotebook`（之後；去空格後比對，原文是「Gemini Notebook」），並以 casefold 比對。新標記整份 12 頁 OCR 全讀成乾淨的 'Gemini Notebook'（14 字、去空格），舊上限 16 字看似夠、但**是 `endswith("NotebookLM")` 這一關擋下的** —— 症狀就是整份浮水印一個都沒清掉、變成可編輯文字。⚠️ **位置閘門補不了這一刀**：底右角（y>0.85H、x1>0.65W）同樣會命中橫跨整頁底部的引言帶（新 deck p3 的紅底標語 y0=1976、x1=3233 兩條都過），只有品名能區分。前綴雜字上限改成「扣掉標記後 ≤6 字」（原本是總長 16 減 10，等價），成因見下條沿革：OCR 有時把 logo 與淡淡的頁碼戳記一起併讀（舊 deck p1 讀成「P92F2NotebookLM」）。
- **浮水印遮擋範圍要量、不能用框高的倍數猜**（`cli.py` 的 `watermark_wipe`，2026-08-16 審查）：遮擋盒的上下界曾是 `y0−0.05h` / `y1+0.12h`，理由是「在這張 render 上量到膠囊頂在 y0+0.16h、淡色膠囊底在 y1+0.10h」——**下緣只剩 0.02h 餘裕**，框高 25–50px 時是 0.5–1px，小於膠囊邊緣本身 1–2px 的反鋸齒暈。這正好違反本檔第二重要的不變量「RapidOCR 偵測框的留白不一致」：單一 render 校準出來的倍數對下一份 PDF 沒有保證，而且 **logo 圖示比文字帶高、又整個落在 OCR 框外**（⚠️ 這是當時 SOURCE.pdf 的觀察；2026-08-17 複驗的新舊兩份 deck 都是 logo **在框內**、距框左 3–5px，見下條 —— 這正是「框預測不了浮水印」的另一面，兩種都要能處理），框的任何尺寸都預測不了它。改成在遮擋盒自己的欄帶內掃描列墨、再加 `WIPE_PAD_PX`=2，最後**鉗進 [tight, wide]**：不得比使用者調過的緊盒更緊（`y0−0.05h`/`y1+0.12h`，使用者 2026-06-15「剛好遮住即可」），也不得超過改動前的 `0.35h`/`0.40h` —— 掃描失敗就退回已知可用的行為，永遠不會吃到投影片內容。`img` 為 None 時退回緊盒。
- **掃描要「從浮水印往外走、遇空白就停」，不是取視窗內最外側的墨**（`cli.py` 的 `_contiguous_ink`，2026-08-17）：`min/max` 版本會把**碰巧靠近角落的頁面元素**一起吞進來，再被 clamp 放大成 wide 盒塗掉它（新 deck p8 的灰色卡片離浮水印 14 列、p1 的頁框底線離 8 列，舊 deck p5 的格線離 20 列 —— 全被塗）。改成從文字帶取 seed（框中央 60% 內的有墨列，框緣正是外來墨滲入處）向外走，連續 `gap` 列全空白就停；浮水印自己的零件（logo／字／膠囊）之間**沒有任何空白列**，這個間隙就是判別。三個 gap 不同（皆下限 4px）：垂直 0.15h、**左 0.35h**（要跨過 logo 與品名之間的詞距，萬一 logo 落在框外）、**右 0.10h**（字尾之後不再有浮水印零件，而頁框右線只離 8px）。
  - **列門檻 `WIPE_INK_FRAC` 是 0.05 不是 0.01**：貫穿整條視窗的**頁框直線只佔 1–2% 欄**，卻讓每一列都算「有墨」→ 掃描永遠走不到頭、退回 wide，而 wide 恰好蓋掉頁框底線（新 deck p1 實測底線 y=2120–2125、`wide_b`=2126）。浮水印自身最稀疏的列（logo 尖端）仍佔 10–11%，兩者中間切 5%。
  - **水平也要量，且允許往框內縮 `WIPE_SHRINK_FRAC`=0.3h**：`x0−1.8h`/`x1+0.3h` 是為「logo 在框外」猜的倍數，但兩份 deck 實測 **logo 都在框內**（新 deck 距框左 3px、舊 deck 5px），左延 68–90px 只會塗掉旁邊的頁面內容（新 deck p12 的水平分隔線被截斷一截）。**右側更要縮**：OCR 框右緣本身就壓在頁框直線上（框 x1=3798、線 3798–3803），不准內縮就一定塗掉線。量出來的邊界鉗進 `[x0−1.8h, x0+0.3h]`／`[x1−0.3h, x1+0.3h]`，掃描失敗（紙紋背景讓整片連通：舊 deck p1/p12）照樣退回原本的 wide。
  - 驗證：新 deck 12/12、舊 deck（`Transformer_演進地圖_NotebookLM簡報.pdf`）12/12 全數偵測且盒外 4px 環帶無殘墨（唯二報墨的是舊 deck 兩張紙紋頁與新 deck p8 的灰卡，皆為**正確避開**的頁面內容）；PowerPoint COM 匯出目視 p1/p8/p12：浮水印消失、頁框線與灰卡完整。
- **實心圖示誤認單字母**（`ocr.py` 的 `_looks_like_filled_icon`）：彩色圖示會被認成字母（p15 綠色循環箭頭 → 分數 0.86 的「S」）。判別：字形墨水只有一種顏色，圖示是「填充+描邊」兩個強色叢集（各 ≥25%、距離 ≥60）。兩個陷阱：背景色**不能用環帶**（緊貼圖示的環帶會落在圖示上、把背景估成圖示色），要用框內主色；反鋸齒也會形成第二叢集，但 AA 色落在「背景↔字色」連線上（殘差 < 45），圖示填充色偏離該線 — 必須做共線檢查（TSV 曾被誤判）。只套用在「單一拉丁字母」的偵測行。
- **寬度量測按字種分字型**（`style.py` 的 `_measure_em`）：混排行的拉丁段用 Arial、CJK 段用 YaHei 分段加總 — 全用 YaHei 量會高估輸出寬度（YaHei 拉丁比 Arial 寬 ~9%），夾制把 p5「Git Hook 自動化」從真實 24pt 掐到 20。單字呼叫與 2 字短串維持原規則（見上）。注意**來源字型的拉丁還要再窄 ~14%**，Arial 也吃不下時靠懸崖防護（見下）。
- **寬度天花板分三級、snap 有懸崖防護**（`style.py`）：
  - **投影片右緣（硬）**：`(960 − x0 − 3)/em` — p15 主標 60pt/內文 32pt 曾衝出板面（60→54、32→28，折行尾隨組同縮）；存進 `Style.max_fit_pt`、字級調和的組統一尊重。其他天花板都**不可**放進 `max_fit_pt`：p12「嚴格的格式約束…」（幻覺 ： 移除後框變窄）的緊夾制曾把三個真 14pt 折行同伴拖到 12。
  - **障礙（近硬）**：餘裕掃描撞到任何障礙 → 容差只給絕對 3.5pt 渲染寬、**停用懸崖防護**：p13 卡片內文 16pt 曾靠相對 1.02 容差＋懸崖防護連手戳穿卡框 80px（21em 長行的 2% = 27px，相對容差在長行上的絕對量失控）；p5 Git Hook 的 24pt 曾把「化」壓上牽引線圓點（使用者否決）— **「物件 vs 牆」分類已試過並移除**：使用者裁定圓點也不可壓，且被夾下來的標題改由 `sync_clamped_twins` 拖動全家族。掃描**全程無障礙（1.5×行高內）回傳 inf、完全不設寬度天花板** — 拿掃描範圍當天花板曾把右側開闊的 p10「AI 逆向掃描」從 24 掐到 20。bg 不明時退回字墨寬天花板（`width_tolerance`）。
  - **懸崖防護**（僅字墨寬天花板路徑）：最近 snap 階超過天花板 ≤3% **且絕對超寬 ≤3pt**、回退會掉 >10%（20↔24 階距 17%）時，貼上去（p8 舊框架、p12 卡片內文等 est 恰在階上的行）。沒有絕對上限時 21em 長行的 3% 一樣失控。
  - **標題足跡檢查**（snap 後、僅 ≥`NARROW_MIN_PT` 且 **em ≥10** 的標題）：標題多在開放空間（無天花板）、高度估值又騎在 snap 中點 — p8 標題 est 39 上捨成 40、渲染比原始字墨寬 21%、尾端逼近頁緣（原稿是左右等距置中；使用者要求降一級）。snap 後渲染寬 > 字墨 ×**1.07** 且降一階仍 ≥ 字墨 ×0.90 → 取小（p5/p11/p13 同輪抓出；門檻曾設 1.09 漏掉 p1 封面標題 1.072 — 使用者再報；全文未觸發標題都 ≤1.07，p9 1.069 恰好倖存）。em ≥10 是為了 p15 引言塊（em 4–6）：同一真實字級內足跡比散佈 ±8%，短行不可比。**全文性的字墨足跡天花板已試過並撤回**：來源 CJK 字距比 YaHei 均勻緊 ~10%，能抓住超寬標題的容差同時會勒死正常內文（1.098）— 曾一口氣降了 56 行。**內文（<`NARROW_MIN_PT`）也套足跡檢查、但門檻 1.13**（使用者回報 2026-06-14）：**粗體筆畫加厚字墨帶 ~6%**，把真實 S 級的粗體行高度估值推過 snap 中點到 S+1 階，渲染就比卡片寬一階而溢出（p3 阿姆達爾…的鐵律 1.26、p14 重啟計量 1.14/為釐清方向 1.41、p5 歷史金流牽一髮 1.19、單執行緒 1.25——**全是粗體、全溢出**）。1.13 留給合法內文（非粗體內文足跡全 ≤1.06、粗體但量對的也 <1.13；門檻內只命中過度 snap 的粗體行）；降一階仍 ≥字墨×0.90 才降。`bold` 在 font_pt 之後才算、足跡檢查取不到，但全文 ratio≥1.10 的行皆粗體、非粗體 ≤1.06，故無需 bold 閘。副作用：粗體掉到 ≤24 後可能被模板重判為常規、再被字級調和併入下行（p3 鐵律 →18 失去粗體前綴），視覺仍正確、可接受。
- **被夾制的標題拖動同款孿生**（`blocks.py` 的 `sync_clamped_twins`，字級調和後、粗體投票前跑）：p5「Git Hook 自動化」est 23.6 想要 24、但牽引線圓點離框 6px、夾到 20 — 同款的「規則演化 (免疫力)」「跨頁連動修復」在開闊處仍是 24，同家族 20/24 混雜比全家 20 更醜（使用者裁定）。閘門收緊：限粗體、est 差 ≤10%、文字色 ≤45、底色同類 ≤25、em 寬比 0.6–1.6、恰差一個 snap 階 — 被夾行把孿生拉到自己的字級。**方向只往下**；p6「6 冊商管書籍」這類非粗體行不入閘（曾差點把同頁 14pt 內文拖到 12）。
- **右側餘裕掃描可穿透「細且淡」的障礙**（`_chip_room_right`）：寬 ≤8px 且 Chebyshev 距 <75 的淡裝飾格線放行繼續掃；深色障礙（牽引線圓點、卡片邊框）不論多細都停下 — 放寬到「細就放行」會讓文字長過 p12 的卡片虛線邊框。
- **書封/告示牌式斜排裝飾字成對丟棄**（`blocks.py` 的 `_rotated_decor_pair`）：兩行 |角度|≥15°、皆無 CJK、旋轉矩形互相交疊（雙方坐標系都檢查）→ 整對留原圖 — p10 書脊 VENDOR PITCH DECK 被拆成 −28° 的 VENDO + PITCH DECK 交疊框，後畫的蓋板塗掉先畫的點陣字；VENDO 本身就是截斷誤讀，可編輯價值為零。CJK 豁免：斜置金字塔籌片是真內容。
- **混淆雙字組修正表**（`ocr.py` 的 `_CONFUSION_BIGRAMS`）：rec 模型穩定誤讀、且以 5× 放大目檢確認過原圖真值的字對（反椎→反推、雨份→兩份、幹擾→干擾）。**只收非真實詞彙的雙字組**（無誤殺空間）。三條原則性路線都實測失敗才收表：局部窗口 rec-only 重讀照樣讀錯；YaHei 模板 XOR 區比對與滑動對位 IoU 都分不開「只差部首」的字（椎/推 邊際 −0.02，來源字型又非 YaHei）。
- **行首圓點統一**（`ocr.py` 的 `_normalize_bullet`）：行首的 ·‧・∙ 一律正規化為「• 」（含補空格）— p14 同一張清單 rec 吐出 · 與 • 混用、渲染大小不一。改字後要把 `char_boxes` 設 None。
- **行首圓點復原**（`ocr.py` 的 `_restore_leading_bullet`，使用者指示 2026-06-13）：NotebookLM 每個標籤的牽引線末端是一個實心圓點，rec 對它的轉寫時有時無（p4 讀出「• 原始資料層」「• Wiki 層」卻漏掉同款的「約束架構層」前圓點 → 該行蓋板蓋掉點陣圓點、可編輯文字又沒有「• 」，視覺上沒了圓點）。**不需找同款孿生**：偵測框左緣伸到第一個真字之外（圓點在框內但沒被轉寫），且那段間隙裡有一顆實心、近圓、孤立、圓點大小的色塊。判別（皆相對行高 lh=框高）：第一字框左緣 − 框左 ≥0.4h（有空間）；間隙內逐欄墨高 ≥0.12h 的最右**且尾端有 ≥0.2h 空白**的連續欄段（細的牽引線 <0.12h 被排除；純取最右會抓到下一個字外溢進框的墨、它尾端無空白）；該段寬/高 ∈[0.55,1.7]（近圓）；段內填充率 ≥0.55（實心非空心）。命中就在文字前補「• 」並把 `char_boxes` 設 None。**框外搜尋與蓋板左延**（使用者回報 2026-06-14）：偵測框常切掉或排除行首圓點 → 蓋板從框左起算遮不到點陣圓點、露出一截（p2 改變計量/投资工程，rec 已把圓點轉成 raw「·」但框左切過圓點中心），或圓點整顆在框外（p2 制度面，框左 1497、圓點 ~1450）。改用**固定視窗** `[x0−0.9lh, x0+1.0lh]`（不再以 fc_left 為界——rec 已轉寫時第一字框就是圓點本身、反而排除它）搜最左合格圓盤，回傳 `new_x0` 把 `Line.bbox` 左緣延伸到圓點左邊（等同修正偵測誤裁、下游蓋板與「• 」文字自然對齊）；raw「·」開頭也照搜（只延蓋板、不重複加）。**三道防誤判閘門**（避免左探撞到鄰字）：圓盤 dh ≤**0.40×lh**（真圓點 0.28–0.35、大數字「5」填 0.46 必須擋掉，p4 5 週）、**左側也要 ≥0.15h 空白**（牽引線細<0.12lh 算空白；鄰段字母的圓弧右緣高墨擋掉，p6 拆段標題 Legacy 的 g 在「（歷史金流）」左）、右緣 ≤x0+0.7lh（近行首）。全文 4 個圓點觸發、零誤判。**「實心」不夠，必須是「圓盤」**（2026-08-17 以 CB 詢圈策略／Markdown Blueprint 兩份新 deck 驗證）：原本 `fill ≥0.55` + `w/h ∈[0.55,1.7]` 太鬆，會把**楔形標記**當成漏掉的圓點，於是把點陣三角形蓋掉、改畫一顆圓點（換掉了原稿真正顯示的字形），或在已轉寫的箭頭後面再補一顆。三道量測到的判別（真圓點 vs 誤判，樣本 8 vs 7）：
  - `fill ≥ 0.72` —— 圓盤佔外框 π/4≈0.785，三角形只有 ~0.5。實測真圓點 0.806–0.881，箭頭 0.587–0.625、逗號 0.701。
  - `w/h ≥ 0.72`（原 0.55）—— 實測真圓點 0.76–1.00，箭頭／逗號／數字豎筆 0.57–0.69。
  - **上下對稱 `min(上半,下半) ≥ 0.70×max`** —— 圓盤對自己的中線對稱，楔形不對稱。這是唯一擋得住 `<details>` 的 ▼（上 0.99／下 0.45）與 ⚠ 警告三角（上 0.51／下 0.97）的判別，兩者 fill 都過得了 0.72。實測真圓點 0.87–1.00。
  **fill 是雙側區間 `0.72 ≤ fill ≤ 0.95`，不是單側下界**（2026-08-17）：只設下界時**實心方塊 ■／▪ 三道閘門全過**（fill 1.00、w/h 1.00、對稱 1.00），方塊清單標記會被當成漏掉的圓點蓋掉、改畫成圓點。三種形狀的外框佔比天生分離：圓盤 π/4≈0.785、三角形 ~0.5、方塊 1.0。以 msyh.ttc 在 14/20/28/40px 實際渲染（走同一條 >60 墨水門檻）驗證：● 0.769–0.852、• 0.722–0.844 全數通過；■／▪ **在每個尺寸都是 1.000**（反鋸齒不會把它拉到 0.95 以下）、◆ 0.503–0.667、▼／▲ 0.508–0.544、⚠ 0.467–0.600 全數擋下。實際 leader 圓點在 200dpi 高 25–39px、實測 fill 0.806–0.881，距上界 0.95 有 0.069 餘裕。
  三者相乘後：CB deck 8 個真圓點全留、Blueprint deck 零誤判（原本 5＋2 個誤判）。**連帶效應**：誤加的「• 」會讓該行不再是「2–4 字純 CJK」，`_vocab_correct` 因此跳過它 —— Blueprint p12 的「點擊」曾被讀成「點撃」而無法被同頁詞彙救回，修掉誤判後自動恢復。
- **淺色/描邊文字落在彩色表面 → 取飽和表面為底**（`style.py` 混合環帶分支開頭的統一判別，使用者回報 2026-06-14）：白字、或**黑字加厚白色描邊**落在彩色/紋理表面上時，混合環帶分支的 survival/膠囊啟發法會把厚白邊誤當底色、蓋板畫成白底黑字（p8「日曆與/依賴卡死」白邊黑字在紅磚牆、p14「資料授權請求清單」黑字白邊在深藍面板）。**統一判別**（放在混合分支最前、優先攔截）：全框 k=3 叢集若**同時有近白群**（min ≥200、share ≥0.15＝白字或白描邊）**與飽和表面群**（min <200、max−min ≥45、share ≥0.18＝磚/晶片/面板）→ 取最常見的飽和表面為底、近白為文字色。白字落在飽和晶片（無描邊）走同一條、結果一致。環帶太雜（紅磚 share 僅 0.24）也無妨，靠全框叢集而非環帶主色。閘門天生排除：黑字白底（無飽和表面）、灰機器面板上的白字（max−min <45、不飽和→不觸發、走原 survival 給白字）。p8 Guardrail 黑字（無近白群）不觸發。
- **黑字＋白暈在「淺色表面」→ 取近白為底、暗字為文字**（`style.py` 混合環帶 survival 分支前的判別，使用者回報 2026-06-14）：上一條的飽和表面判別需要有飽和叢集；但 p5「高耦合金流邏輯」是**黑字＋白色光暈在一條白色帶上**（環帶 [233,237,240] 近白、全框只有白＋黑兩群、無飽和表面）。survival 啟發法假設文字是細筆畫、會被侵蝕——但**粗黑字的筆畫比薄白暈更厚、反被當底**（→ 白字黑底，與使用者要的「深一點」相反）。判別：兩叢集為 {近白(min≥200), 暗} **且環帶近白(min≥180)** → 文字坐在淺色表面上、取近白為底、暗叢集核心為文字 → 黑字白底。白字深底的環帶是暗的、此分支跳過、survival 照常給白字。**仍未涵蓋的尺寸**：高耦合字級量成 28（應 ~24），因底部一條中性暗邊與字僅隔 5 列（<8 切組門檻）、又是 cov 0.01 的微弱列卻仍過 `MIN_INK_ROW_PX`、把帶撐高一階；「稀疏邊緣列裁切」CLAUDE 已記載試過並撤回（曾把 44pt 標題裁成 40），故字級暫留。
- **描邊文字在彩色晶片上：膠囊分支會把白邊當溢出膠囊底**（`style.py` pill/chip 分支，使用者回報 2026-06-14）：晶片色環帶 share ≥0.55、底色直接設對（金/綠），但**白色描邊夠厚使 ink.mean ≥0.45 觸發膠囊重推分支**，分支把「最實心叢集」＝白描邊當成溢出膠囊的底色 override 成白（p13「提速效果」在金晶片、「擴展與制度重構」在綠晶片 → 白底黑字）。守衛：重推底色 `bg_cand` **近白（min ≥200）而環帶 `bg_ref` 明顯較暗（min <170）** → 那是描邊文字的白邊、非膠囊 → 保留晶片色為底、把白邊設為文字色（不 override 成白盒）。真正的溢出膠囊底色是飽和晶片色（非近白），不受影響。全文 提速效果/擴展與制度重構 觸發、白字白底案例（白字深底晶片）零回歸。**仍未涵蓋**：白暈在**照片**上（p5「高耦合金流邏輯」黑字白暈在橘色電路球）——白暈太寬使環帶 share ≥0.55 直接當底、且 ink.mean 0.46 誤闖膠囊分支把橘照片當底，文字估成灰、字級被白暈灌高；密集粗體＋白暈＋照片三者互相干擾，動之風險高（會碰 p8 Guardrail／p15 校準），暫留原樣。
- **混合環帶的文字叢集可能是 AA 殼**（`style.py`）：環帶混雜分支選出的「文字叢集」對 p8 Guardrail 1 整個是反鋸齒殼+晶片外框（灰 ~190），真正的黑筆畫太稀疏沒成叢 — 文字被刷成淺灰。ink mask（以選定的 bg 重算）核心色若比叢集 override 離底色遠 ≥60 → 信任 ink 核心。此修連帶把 p8 時間軸五個月份標籤從 AA 灰救回黑色。
- **同列右鄰寬度夾制**（`blocks.py` 的 `clamp_row_neighbors`，調和器之後跑）：estimate_style 的寬度夾制只看自身字墨寬與寬容門檻，偵測器拆開的標題會渲染超寬、擠進右鄰（p14「AI 協作的黃金法則」40pt 渲染 ~336pt 但到「(流程與資產篇)」左緣只有 323pt）。以右鄰左緣為硬上限選最大可容字級；右鄰若是「同款式、同字級、近貼」的孿生（被拆開的同一標題）則同步縮 — 否則拆開的標題會一邊 36 一邊 40。
- **太小/模糊的行不轉文字、保留原圖**（`blocks.py` 的 `drop_illegible_lines`，浮水印過濾後、字級調和前跑；`--keep-tiny-text` 可關，使用者指示 2026-06-12）：插圖內文（p11 命盤/流程圖/終端機 46 行亂碼、p10 書脊、p7 渦輪機標籤）轉出來是錯字蓋板、比留原圖更糟。規則：≤9pt 預設丟、乾淨小字放行（純 CJK 2–4 字且分數 ≥0.85 → p5 金字塔籌片 0.89–0.93；其他 ≥0.94 → p9 時間戳）；≤14pt 且 <0.72 丟（p3 `<小>` 0.57、p9 斜置籌片 商業策略 0.69）；任意字級 ≤3 字無 CJK 且 <0.75 丟（p14 `di` 0.53、p10 `Al` 28pt 0.72）。**門檻判不動的高分孤立殘黨用「雜訊鄰域擴散」收**：弱行（≤9pt，或 ≤2 字無 CJK 且 ≤20pt）若 2×行高內有已丟行、又沒有強行（非弱的保留行）鄰居 → 一起丟，迭代到不動點 — 命盤的 m/Ⅱ/10 分數 0.96+ 純門檻看不出來、靠這個收掉；p8 `IP` 因緊貼 `Attacker` 活下來；p9 五個 8pt 時間戳是同一條弱行鏈、一個跌破 0.94 會整鏈沖掉（整條時間軸一致留原圖，可接受）。**第三遍「孿生一致性」**（使用者指示 2026-06-12）：同組籌片丟一半比全丟/全留都醜（p9 BSP 四層堆疊：商業策略/資訊策略 0.60–0.69 被丟、應用系統/技術基礎 0.77+ 活下來 → 半蓋板半原圖）— 保留中 ≤14pt 的行若與已丟行是孿生（同 snap 字級、同欄 x 重疊 ≥60%、高度差 ≤30%、垂直間距 ≤0.8 行高）就跟著丟，與擴散同一個迭代迴圈跑到不動點（也順手收掉 p10 書架上漏網的 System Design 書脊）。丟掉的行完全不產生形狀、原圖點陣字直接外露。
- **無法忠實還原的行保留原圖**（`blocks.py` 的 `drop_unreproducible`，浮水印過濾後、`drop_illegible_lines` 前跑、與 `--keep-tiny-text` 無關，使用者指示 2026-06-13）：三類視覺內容轉成可編輯文字必然失真，整行丟掉露出點陣。(a) **線稿圖示被讀成單字母**（p4 交叉箭頭 shuffle 圖示 → 40pt 的「X」）：單一拉丁字母、≥28pt、且該列無其他真文字（垂直不重疊任何行）即判圖示 — 真的單字母內容（X² 變數）一定有同列鄰字、不會在標題級孤立；`_looks_like_filled_icon` 只抓「填充+描邊」雙色圖示，線稿單色圖示靠這條。(b) **markup 示範的刪除線**（p13 `==螢光==` 的 光 上疊紅 ✗ 讀成 `==螢×==`）：含 `==` 且含 `×` → 刪除線疊字無法還原（p6 純 `==螢光筆==` 無 × 不丟，照常轉字）。(c) **上下標公式**（p12 H₂O X² 被 rec 壓平成 `H2OX2`）：word box 是詞層級、拿不到每字基線，只能靠點陣 — 短純英數行（3–8 字、含數字含字母、無 CJK）切欄量各字墨重心，散佈 >0.25 行高即判上下標。(d) **線稿圖示被讀成單一 CJK 字**（p14 CPU/IC 晶片圖示讀成「尚」）：把該字以 YaHei 渲染、與框內字墨各自緊裁縮放到 64×64 比 IoU（`style.glyph_char_iou`）——真字會與自身渲染重疊（0.35–0.65），圖示不會（尚 IoU 0.20）。**保守取 AND**：分數 <0.92 **且** IoU <0.30 才丟（正確讀出的單字分數高或 IoU 高、永不誤丟）。全文僅一個單 CJK 字行（就是這個圖示），其餘真字都是詞/句的一部分。(e) **未偵測到旋轉的圖表軸標籤**（`_is_rotated_axis_label`，p11 Y 軸「修復成本 (指數型攀升)」）：偵測器把 90° 旋轉的軸標籤回成 angle=0 的軸對齊框、框**高遠大於寬**（121×819，h/w=6.8），被當水平文字量成 8pt 小字、既不蓋住也不對齊側躺的點陣。此處旋轉/字級都不可靠，使用者裁定（2026-06-14）「這個字可以不轉換」→ 整行丟掉留原圖。判別：`h > 2.5×w` 且非 `style.vertical`（直排堆疊 CJK 由 `vert="eaVert"` 正常處理、保留）、非 angle/arc（偵測到的旋轉照樣旋轉渲染、保留）、含 CJK。水平行 w≫h 永不觸發。
- **同列被拆標題碎片合併成一行**（`blocks.py` 的 `merge_row_title_fragments`，drop 之後、字級調和前跑）：偵測器偶爾把一條標題打散成並排碎片、互相夾制 snap 成混級（p6「釐清/「方言」：/markdown/的規格體系」snap 成 36/28/28/36 — 中間兩塊框互疊把彼此夾到 28）。同列（垂直重疊 ≥0.6h）、橫向相鄰（右塊左緣距左塊右緣 ≤0.8h）、同粗體同色的 ≥28pt 碎片併成一行、取最大碎片字級。互疊框重讀同一個邊界標點（：然後 :）的重複字要去掉（`_norm_punct` 把全形 ： 摺成 :，p6 去掉重複的 :）；邊界字不同（p2 了 / 「）則保留兩者。也順手把 p9「語法速查 I：GFM 核心擴充」、p12「逃生門：合法內嵌 HTML」併回一行。
- **程式碼行的拉丁段別被反引號/底線灌高**（`blocks.py` 的 `harmonize_code_block_latin`，字級調和後跑）：純拉丁碼行的 ` 與 _ 撐滿整個 em 框、灌高估值（p4 `` `name: analyze_data` `` 量成 18pt，同款堆在下面的 `` `description：分析資料庫` `` 靠 CJK 字帶量成 14pt — 同字級卻差一階、上下重疊）。純拉丁且含 `_`/`` ` `` 的行若左對齊（x0 差 ≤0.5h）堆在同色同底、字級更小的含 CJK 行上 → 信任 CJK 量測、把拉丁行夾到 CJK 行字級。
- **障礙夾制留邊距、且傳遞給同欄孿生**（`style.py` 的 `OBSTACLE_MARGIN_PT`=3pt + `Style.clamp_pt`；`blocks.py` 的 `propagate_column_clamp`，使用者指示 2026-06-13）：餘裕掃描撞到障礙時，可用寬度要再扣 3pt 邊距（文字剛好貼到格線/卡片邊框看起來壅擠，使用者要降一級）、且取消 snap 容差與懸崖防護 — p2 John Gruber 卡片內文（16→14）、p8「VS Code 內建」「Working Copy」貼欄線（24→20）。障礙天花板存進 `clamp_pt`（**不是** `max_fit_pt`，後者只給投影片硬邊；CLAUDE.md 既有 p12 案例禁止障礙天花板進 max_fit）：字級調和器的折行組統一目標同時受 `clamp_pt` 夾制，否則 p2 卡片內文四行 2-2 平手會被中位數重 snap 回 16、再次衝出卡框。`propagate_column_clamp`（sync 之後跑）讓被障礙夾下來的標籤把**同欄、同自然 snap 字級、恰高一階、同色同底**的孿生一起拉下並繼承其粗細 — p8 首欄六個工具名原稿全是同級常規體，但 GitHub Web/Obsidian 較短不撞欄線、snap 上去 24 又被 ≥24 規則自動加粗，靠這條跟著 VS Code 內建 降回 20 常規、整欄一致。
- **被保留原圖的行隔開的段落尾，樣式對齊段落本文**（`blocks.py` 的 `harmonize_across_dropped`，粗體投票後跑，使用者指示 2026-06-13）：某段中間一行被留原圖時，上下兩個保留行不再相鄰，字級調和（只橋接緊鄰行）看不出它們同段、段尾被孤立成異級異粗（p13「Alerts 退化成…但特」16pt 常規，段尾「會變成干擾閱讀的字面文字。」誤量成 14pt 粗）。某個被丟行若落在「同欄（左緣 ≤0.6h）、同色、同底、est 差 ≤20%」的兩個保留行垂直間隙中，就把下行的字級與粗細統一到上行（使用者讀樣式的本文行）。cli 以 id 差集收集兩道 drop 丟掉的行傳入；左緣閘門擋掉 p12 H2OX2（上下是不同欄的程式碼示範與內文）。
- **小隊列孤身邊際模板粗體撤粗**（`blocks.py` 的 `harmonize_bold`，使用者指示 2026-06-13）：≤4 行的同字級小隊列中若只有 1 行粗、且它是模板判的邊際粗（`bold_r` 非 None 且 < `TPL_MARGINAL_R`=0.22）→ 撤粗。判別器在門檻附近擲硬幣（p3 三個 `[籌片]` 標籤：最大化可讀性 r=0.19 判粗、另兩個 0.07/0.09 判細，視覺其實同款）。只撤模板邊際粗 — 真強調（人類輸入 r=0.25）與筆畫判的標題（`bold_r` None）不動。

## 驗證

1. `python pdf2ppt.py "SOURCE.pdf" -o generated.pptx`
2. `python tools/compare_pptx.py generated.pptx "SOURCE.pptx"` — 真實內容行預期 100% 召回；被 `drop_illegible_lines` 刻意丟棄的插圖雜訊行（參考檔若未過濾會含有）列為 MISS 屬預期，逐條確認 MISS 都是雜訊/留圖行即可。⚠️ **`SOURCE.pptx` 只是「文字」基準，絕不可拿來校準樣式**：它的粗體／字級旗標是從我們自己更早的輸出繼承來的（使用者只修文字、不修樣式），2026-06-12 那批漏判的粗體**在參考檔裡同樣是錯的**；樣式的真值只有一個——PDF 渲染圖的目視比對。來歷見 `docs/dev/collaboration.md` §5
3. 視覺驗證：用 PowerPoint COM 把投影片匯出成 PNG（`$pp = New-Object -ComObject PowerPoint.Application; ...Slides.Item($i).Export(path,"PNG",1376,768)`），與 `pymupdf` 的頁面渲染圖上下並排比對，比對圖放 `verify/`。
4. **不要重新產生 `SOURCE.pptx`**（使用者指示 2026-06-12）：每輪修正只產生並保留 `generated.pptx` 給使用者自行驗證；`SOURCE.pptx` 由使用者管理、僅作為步驟 2 的比對基準。
5. **刪除其餘驗證產物**（使用者指示，⚠️ **這條失守過兩次**：2026-06-12、2026-06-14，兩次成因一模一樣）：**不要憑記憶手打片段的 `rm`**——兩次都是漏了 `--debug` 的疊圖 `*.debug.p*.png`，而且 `.gitignore` 已經涵蓋這些檔，所以 `git status` 看起來乾淨、**把殘留藏起來了**（**乾淨的 git status 不等於乾淨的工作目錄**）。每一輪的**最後一個動作**跑一次完整掃描：

   ```bash
   rm -rf verify/ *.debug.json *.debug.p*.png dbg*.pptx dbg.* _dbg_*.py nul.* *.tmp.png
   ls -1   # 強制：目視確認只剩原始碼 + SOURCE.pdf/SOURCE.pptx + 要交付的 .pptx
   ```

   `ls -1` 那步不可省——它才是抓得到「漏掉一個 glob」的那一步。永遠不要刪 `SOURCE.pdf`、`SOURCE.pptx` 與要交給使用者驗收的產出 `.pptx`；臨時檔一律寫到 scratchpad 目錄，不要落在專案裡。見 `docs/dev/collaboration.md` §6。

## 環境注意事項

- OCR 推論裝置：`--device` 預設 auto（DirectML > CUDA > CPU 依可用性）。本機裝的是 `onnxruntime-directml`（取代 CPU 版 onnxruntime）。實測 Intel Arc 140V：**DirectML 全份 50s vs CPU 210s（快 4.2×）**。注意**首次** DML 執行要付一次性的驅動 shader 編譯（該次 ~213s、看似與 CPU 同速）——編譯結果由 Intel 驅動存碟快取，之後每次執行都命中快取；不要被首跑數據騙了下「GPU 沒用」的結論（我們差點下錯）。rec_batch_num 加大反而變慢（16: 0.98s vs 6: 0.80s/頁），維持預設
- Windows 11、Python 3.14；主控台是 cp950 — 印中文的腳本需要 `python -X utf8`（cli.py 自己會重設 stdout）
- PowerShell 會吃掉 `python -c` 單行指令中的雙引號；這類情況請改用 Bash 工具或腳本檔。git commit 訊息中的雙引號也會被截斷 — 訊息避免使用雙引號
- 中文輸出字型預設 Microsoft YaHei（之前為 Noto Sans TC）；**所有拉丁字元一律輸出 Arial，但 ≥28pt 的大標題拉丁改用 Arial Narrow**（使用者指示 2026-06-12：來源字型拉丁較窄，p4「Layer 0: 三層架構分工與 Karpathy 模式知識庫」用 Arial 渲染明顯過長 — 改字型、不可降字級）：每個 run 設 `<a:latin>`=Arial/Arial Narrow（依 `NARROW_MIN_PT`） + `<a:ea>`=YaHei，PowerPoint 按字元類別自動選用，中英混排同行雙字型、不需拆 run；`--font` 只控制東亞字型。寬度量測用 `C:\Windows\Fonts\msyh.ttc`（純拉丁且 ≥3 字的行改用 arial.ttf、出生估字級 ≥28 的行拉丁段改用 ARIALN.TTF 與輸出一致 — 2 字短串的寬度夾制騎在 snap 平手點上，曾把 p8「98」籌片從 20pt 推成 24pt 粗體；`_cjk_band_height` 的單字映射一律維持 YaHei 度量、不可改）；本機另有 Noto Sans TC 在 `%LOCALAPPDATA%\Microsoft\Windows\Fonts\` 作為備援
- 提交與推送、回覆語言、字型選擇等協作規則見上方「協作方式」章節

## 量過而否決的路（別再做一次）

（章節格式自 `MP4-2-SRT` 移植 2026-08-23。）這些都做出來或量過了，結論寫在上面對應的那一條裡，重做之前先讀那一段——本檔很長，這裡只當索引：

- **把「逐字 CJK 共識」放寬到所有 CJK 行**——見「混合行（中英夾雜）字級取逐字 CJK 共識」。±3px 模糊雜訊讓其他臨界行掉一階、把原本一致的區塊拆散（p4 右註解 16→16/14/14），單 CJK 字的行（「2025 年」）更會塌到單字高度。
- **稀疏邊緣列裁切（< 0.25×中位數）**——同上一條，以及「黑字＋白暈在淺色表面」。曾把 44pt 標題裁成 40pt；`高耦合金流邏輯` 的字級因此暫留不動。
- **全文性的字墨足跡天花板**——見「寬度天花板分三級、snap 有懸崖防護」。來源 CJK 字距比 YaHei 均勻緊 ~10%，能抓住超寬標題的容差同時會勒死正常內文（1.098），曾一口氣降了 56 行。
- **餘裕掃描的「物件 vs 牆」分類**——同上。使用者裁定牽引線圓點也不可壓，被夾下來的標題改由 `sync_clamped_twins` 拖動全家族。
- **弧形文字用純影像逐欄追蹤法擬合弧度**、**輸出端用 PowerPoint 的 `prstTxWarp`**——見「弧形文字」。前者兩條平行弧線互相污染、連符號都會判反；後者字級/路徑縮放行為不透明，長文字會繞圈重疊。
- **行內 highlight 色塊照原稿保留**——見「行內多重背景填色」。輸出字型步進寬與原稿不同、中段字前面累積位移、色塊對不上文字（使用者否決），改為 `highlight_removed`。
- **偵測器換更大的 `max_side_len` / det server 模型救漏行**——見「同籌片漏行救援」。4096 與 server 模型都更糟。
- **靠局部窗 rec-only 重讀、YaHei 模板 XOR、滑動對位 IoU 分辨「只差部首」的誤讀字**——見「混淆雙字組修正表」。三條原則性路線都實測失敗，才收下辭典型修正。
- **`_glow_free_bg` 用蓋板的完整伸展量取樣**——見「底色要在投影之外重新取樣」與 2026-08-23 的 code review 修正。四份 deck 實測 16 行底色漂移 7–14 個單位，數行整個落到鄰近表面。
- **`sync_clamped_twins` 自己近似折行分組**——見「`sync_clamped_twins` 的折行組要整組一起拖」。只看直接鄰居會讓三行以上的段落只被拖走前兩行；邊迭代邊改 `font_pt` 還會讓結果跟著行的排列順序跑。
- **`propagate_row_clamp` 直接改用 `_wrap_partition` 的分組**（2026-08-23 `/simplify` 提案，未採納）：兩者述詞不同（多了 est 14% 與邊際粗體例外），換過去會改變分組結果。
