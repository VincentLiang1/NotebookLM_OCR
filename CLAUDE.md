# CLAUDE.md

本檔案為 Claude Code（claude.ai/code）在此倉庫工作時的指引。

## 專案簡介

`NotebookLM_PDF_2_PPT` — 將 NotebookLM 簡報 PDF 檔轉換成可編輯 PowerPoint 的工具（CLI `pdf2ppt.py` 為主，另有 Tkinter 圖形介面 `pdf2ppt_gui_2.py` 包在同一支 `cli.main()` 外面），每頁投影片以全解析度頁面渲染圖為背景，疊上實心填色矩形蓋住點陣文字，並承載可編輯、樣式比對過的文字（字級、粗體、文字色、蓋板色皆從影像估計）。

**完整規格書在 `docs/spec/`**（從零重建一次需要知道的一切，13 章），十分鐘版的架構導讀在 `docs/dev/architecture.md`；產品定位、使用者拍板過不得重開的決策、名詞定義見 `docs/spec/01-產品定位與硬性原則.md`。

⚠️ **spec 正文技術中立，模組檔名與私有函式名一律集中在 `docs/spec/12-附錄-現行實作對照.md`**（2026-08-24 起，判準見 `docs/dev/documentation.md`）：新增一條規則時，**規則本身**寫進對應章節，**指路**寫進那一章。門檻數值、實測數據、反例留在正文，那是規格唯一不可再生的部分。

## 常用指令

```powershell
uv sync                                # 建環境（雙擊「安裝.bat」也是跑這句）；rapidocr 首次轉檔才下載 ONNX 模型
uv run pytest                          # 文件與程式碼的一致性（tests/test_docs.py、tests/test_docs_index.py）
uv run python pdf2ppt.py "input.pdf"          # 完整轉換 -> input.pptx
uv run python pdf2ppt.py in.pdf -o out.pptx --pages 1-2 --debug   # 快速迭代：指定頁 + 疊框 PNG/JSON
uv run python tools/compare_pptx.py generated.pptx reference.pptx # 與參考簡報比對文字召回率
uv run python pdf2ppt_gui_2.py         # 圖形介面（或雙擊「啟動.vbs」，不開黑視窗）
```

⚠️ **GUI 的選項清單是手抄 `cli.py` 的 argparse 定義**（標籤、預設值、`--device` 的 choices、粗體模式的旗標對應）。在 `cli.py` 增刪旗標或改預設值時，`pdf2ppt_gui_2.py` 與 README 的選項表必須同一輪一起改 —— 已經漂移過一次（`--lang` 在 CLI 與 README 都有、GUI 完全沒有對應控制項，2026-08-16 補上）。

⚠️ **GUI 主畫面一個選項都不露出來**（使用者 2026-08-23／08-24 指示，見 `_toggle_advanced`）：只留輸入／輸出檔，其餘全部收進預設收合的「進階選項」區（理由：那些值日常一項都不必動，攤在主畫面上只是擋住主線）。**預設值三方一致**（2026-08-24 起）—— 色塊那一項曾經是主畫面上唯一的核取方塊、且刻意與 `cli.py` 相反以便做 A/B，量完之後 `cli.py` 的預設改成 `--no-cover`，GUI 也收進進階區並**反向**成「色塊獨立畫成矩形」（存 `self.cover`、預設不勾、勾了才送 `--cover`）。⚠️ **GUI 存的是反向旗標**，所以 `tests/test_docs.py` 的三方一致例外清單是 `{"--no-cover", "--output"}`（不是 `--cover`）。

自動化測試只有 `tests/test_docs.py`（文件與程式碼的一致性）與 `tests/test_docs_index.py`（指路不得斷掉、`CLAUDE.md` 字元上限，2026-08-24 隨文件分層規範加入）；**轉換品質沒有自動測試**，靠的是四份 deck 全跑加目視比對（見下方「驗證」）。

⚠️ **動到 `啟動.vbs`、`啟動（顯示訊息）.bat` 或 GUI 的啟動／錯誤處理路徑之前先讀 `docs/dev/gui-啟動與錯誤留底.md`**：藏掉主控台就等於藏掉所有錯誤，所以要有東西接住（決策層級的那一句在 `docs/spec/09-執行環境與效能.md` §9.3，那一份放綁著 Windows／VBScript／Tkinter 的實作細節）。

## 協作方式

以下**必須當場生效**，所以抄在這裡——完整理由、沿革與災情紀錄在 `docs/dev/collaboration.md`，只寫在那裡等於沒寫（要有人想到去讀才載得進來）。

- ⚠️ **使用者說「記住 / 記憶下來」= 寫進 repo，不是寫進 Claude Code 的記憶功能**（2026-08-17 自 `meeting-scribe` 移植）：記憶目錄在 `%USERPROFILE%\.claude\projects\<由專案路徑編出來的名字>\`、**在 repo 之外且 git 不追蹤**，而使用者換電腦的方式是**複製專案資料夾**——寫在那裡的東西一份都帶不走，光是把專案搬到別的資料夾，那個依路徑編出來的目錄名就對不上了。聽到時：**必須當場生效**的規則寫進**這一份**（只有 `CLAUDE.md` 每次自動載入）、門檻的理由與反例寫 `docs/spec/` 對應章節、協作方式的沿革與災情寫 `docs/dev/`，兩者都要在這裡留一句指路；然後**一併提交推送**，並**在回覆裡說清楚寫進哪個檔**，好讓他知道換機器之後還在。⚠️ 同理，全域的 `C:\Users\vli\.claude\CLAUDE.md` 也帶不走——跨專案規則若對本專案是關鍵的，就在這裡再寫一份。沿革見 `docs/dev/collaboration.md` §1。
- **文件分三層，決定「這段話該寫哪一層」或要新增文件之前先讀 `docs/dev/documentation.md`**（2026-08-24 定案）：`CLAUDE.md` 只放每次載入都值得的硬護欄與指路（有字元上限，測試守著）、`docs/spec/` 放**換一種語言重寫仍然成立**的問題與決策、`docs/dev/` 放綁著這份實作的細節。⚠️ **平台變體住在 `docs/spec/` 底下的平台子目錄**，頂層永遠只有 spec 與 dev 兩個。⚠️ **那份規範三個 repo（meeting-scribe / MP4-2-SRT / NotebookLM_OCR）各存一份、措辭一致，改了要一起改**。
- **回覆使用者一律用繁體中文**（台灣用語），不必等他提醒；專有名詞、API 名稱、程式碼、路徑、log 原文照原樣保留。使用者的**內容領域也是繁中**——這正是 OCR 必須用 PP-OCRv5 **server** 辨識模型而非預設 v4 mobile 的理由。
- **git commit 訊息一律繁體中文**（使用者 2026-06-11 指示）：技術名詞、函式名、常數、px 數值保留原文；訊息內**避免雙引號**（PowerShell 5.1 會截斷），結尾加 `Co-Authored-By`。
- **修正完成並驗證後自動 commit + push，不要再問**（使用者 2026-06-11 指示）；**功能新增或修改時 `README.md` 要在同一輪同步更新後一起提交**（判準：使用者看得到的行為變了沒有）。⚠️ **`CLAUDE.md` 是被 git 追蹤的**——`.gitignore` 只擋 `*.pdf`／`*.pptx`／`verify/`／`*.debug.*` 那幾類，提交時不要漏掉它。
- ⚠️ **自己下 Bash 指令時別寫成權限閘門攔得下的形狀**（自 `MP4-2-SRT` 移植 2026-08-23；原始查證 2026-08-15 讀 `claude.exe`）。全域的 `%USERPROFILE%\.claude\CLAUDE.md` 也有一份，但**那個檔換電腦帶不走**，所以照本章開頭的原則在這裡再寫一次。踩到就**跳過 Auto Mode 的分類器直接中斷去問使用者**，而且加 `allow` 白名單救不了（判為 `too-complex` 的指令，規則比對一律回 `unverifiable`）。三條：**①** 不在指令裡 `cd`（Bash 工具的工作目錄本來就在專案根目錄），要別的路徑就寫絕對路徑；**②** 重導向目標寫死絕對路徑，不可用 `$VAR` 或 `$(...)` 組（會判成 `runtime-determined`）；**③** `cd` 與輸出重導向不可出現在同一條複合指令（`cd-compound-redirect`）。能不重導向就不重導向，讓輸出直接回來。⚠️ 連帶效應：**工作區未被信任時，`.claude/settings*.json` 的權限規則會被整批忽略**（Claude Code 2.1.232 起信任不從上層資料夾繼承進 repo），症狀就是「每個工具都重新問一次」。
- **改了行為要同步更新文件，而且與程式碼同一筆提交**（自 `MP4-2-SRT` 移植 2026-08-23）：事後補的文件跟事後補的測試同一種病，沒人會回頭做，而規格一旦有一條是舊的，讀的人分不出哪幾條還能信。**這個 repo 已經吃過虧**——2026-08-23 那輪 code review 一次抓出本檔有五處與程式不符或自相矛盾（投影半徑公式、`nat_close` 門檻、chromatic 退回的新條件、已解決卻仍寫著「需使用者裁示」的限制、同一段裡一句說移除一句說還在的閘門）。落點：

  | 改了什麼 | 要更新 |
  |---|---|
  | 使用者看得到的行為（CLI 旗標、輸出規則、丟棄規則、限制） | `README.md` 對應章節 **＋ `pdf2ppt_gui_2.py` 的選項清單**（它是手抄 argparse 的，漂移過一次） |
  | 通用規則、不變量、門檻本身 | **本檔**的「不變量索引」（只有 `CLAUDE.md` 每次自動載入） |
  | 門檻的**理由、反例、被推翻過的前一版結論** | `docs/spec/` 對應章節（完整規格書）**＋**本檔的那一行要跟著改 |
  | 模組之間的關係、新增/拆分模組 | `docs/dev/architecture.md`（十分鐘架構導讀） |
  | 新增一章 spec | 同時改 `docs/spec/00-總覽與閱讀指南.md` 的文件地圖（`tests/test_docs.py` 雙向釘著） |
  | 沿革、災情紀錄、完整推導 | `docs/dev/` 並在本檔留一句可 grep 的指路 |
  | 常數的值或理由 | **該常數上方的註解**（那是正典，本檔指過去而已） |
  | 量過而否決的一條路 | 該處註解 **＋ 本檔最後的「量過而否決的路」索引** |

  判斷方式：**如果這個改動會讓本檔某一句話變成假的，那句話就要一起改**。
- **改壞了重試，成功的那一刻就把失敗的殘骸一起清掉**（自 `MP4-2-SRT` 移植 2026-08-23）：定義了沒人用的常數與函式、被取代卻沒刪的分支、指路到不存在的檔案 —— **跟著「最後一次改對」的那一批進同一個 commit**，不是留到下次想到再清。理由是殘骸跟「刻意留的東西」長得一模一樣，而**分得出來的只有剛改完的那個人**。⚠️ 本 repo 的實例：2026-08-23 用 `_grow` 取代 `_dilate` 之後，`_dilate` 零呼叫者卻留在檔案裡，直到當天稍晚 `/simplify` 才發現。
- **中文輸出字型是 Microsoft YaHei，不要「好心」換成 Microsoft JhengHei**（使用者 2026-06-11 明確選定，先前為 Noto Sans TC）：他是繁中使用者，但看的是 PowerPoint 裡的渲染結果。⚠️ 這個選擇有下游後果——`style.py` 的 `_measure_em()` 固定載入 `msyh.ttc` 量寬度，整套寬度夾制、snap 與標題足跡門檻都是照 YaHei 字寬校準的，換字型不會跟著換度量衡。

## 架構

每頁的處理管線（由 `pdf2ppt/cli.py` 調度）：
渲染（`render.py`，PyMuPDF @200dpi，同一張渲染圖同時供 OCR 與投影片背景使用）→ OCR（`ocr.py`，RapidOCR PP-OCRv5 + server 辨識模型 — 繁體中文準確度遠勝預設的 v4 mobile；`--fast` 可切回）→ 樣式估計（`style.py`）→ 選擇性行合併（`blocks.py`，預設與 DeckEdit 相同、每個 OCR 行一個形狀）→ PPTX 輸出（`builder.py`）。

**完整的 pass 順序**（順序本身是契約）見 `docs/spec/02-領域模型與資料契約.md` §2.6；模組之間的關係見 `docs/dev/architecture.md`。

### 最高原則：所有調整必須具通用性（使用者指示 2026-06-13）

**每一條規則／修改都必須對「未來任意新的 NotebookLM 簡報 PDF」有意義，不能只為了通過手上這幾份 deck。** 修正一個視覺問題時，要找出該問題背後**可一般化的成因與判別準則**（例如「線稿圖示被誤讀成單字」「背景有銳利明度階躍」「同段折行被 snap 雜訊拆階」），用通用條件去偵測與處置，並記錄其原理與反例。**嚴禁針對單一頁面/單一字串硬編碼**（如「若文字==某某 則…」「第 N 頁特別處理」）——那對新檔毫無價值。唯一例外是已知無誤殺空間的**辭典型修正**（如 `_CONFUSION_BIGRAMS` 只收非真實詞彙的穩定誤讀字對），且必須註明已目檢確認。**若某修正本質上只能針對某一份 PDF、無法一般化，必須先向使用者說明並取得確認，再決定是否寫入。**

### 不變量索引

⚠️ **以下 81 條規則的完整理由、門檻與反例已搬進 `docs/spec/`**（2026-08-23，使用者指示「CLAUDE.md 可搬移的內容移到 docs」）。這裡只留**規則本身、關鍵門檻與程式指路**，方便每次對話自動載入時就守得住；**要改任何一條門檻之前，必須先讀對應章節**——那裡有這條規則誤殺過什麼、前一版結論是什麼、哪些替代做法量過而否決。缺了那些反例，放寬門檻等於重演一次災情。

#### 渲染與 OCR 文字修正 — 詳見 `docs/spec/03-流程-渲染與OCR文字修正.md`

- **空格還原**（`ocr.py`）：辨識模型會丟空格。拉丁詞間空格由逐字 word box 復原（間隙 > 行內字寬中位的 1/3），再過兩道物理檢查——間隙中段墨水比例 < 0.15，**且完全空白欄帶寬 ≥ max(4px, 0.27×字寬中位)**（⚠️ 必須按字寬比例縮放、不可用固定下限）。中↔英交界一律補空格（盤古之白）。重組時**必須走訪原始文字**：間隙分析只能增加空格、不能弄丟模型自己輸出的空格。
- **表格分隔線 `|` 復原**（`ocr.py` 的 `_restore_pipes` + `_find_pipe_gaps`）：六道閘門合一才擋得住 CJK 字的直筆畫——跨整行高（barh/CJK ∈ [0.9,1.3]）、獨立 token（間隙 ≥0.85 字寬）、細（≤0.22 字寬）、寬度均勻（std/mean ≤0.3）、兩側各 ≥4px 空白。輸出半形 `|`，`char_boxes` 設 None。
- **行尾標點復原**（`ocr.py`）：框右有墨時右延 1.2×行高、以 **rec-only**（`use_det=False`）重讀，只接受補回 1–2 個 `TRAIL_PUNCT`。兩道防幻覺：行尾已是 。！？ 的不得再補 ：，；補回標點的墨水若在行距區有墨、而右移一個標點寬的**對照窗乾淨** → 判垂直邊框、拒收。行尾出現開引號視為幻覺（改重讀行尾 5 字高寬度的小段做後綴比對）。同函式另處理「行尾 CJK 詞被併讀」——**須有頁面詞彙佐證**才採用。⚠️ RapidOCR 的 `use_det/use_cls/use_rec` **跨呼叫殘留**，每次引擎呼叫都必須三個旗標全部顯式傳入。
- **s2tw 只准改「不可能是繁體」的字**（`ocr.py` 的 `_is_big5`／`_trad_only`）：判準是 **cp950（Big5）編不編得出來**——編得出的（划 升 台 注 征）可能本來就是使用者要的繁體，不准改；編不出的（恶 实 内 两 决）才是模型滑掉的證據。
- **「這頁的模型在滑」要收得很緊**（`ocr.py` 的 `_emits_simplified`）：三道閘門——跳過純簡體行、要求 s2tw 真的改寫了那個字、分數 ≥0.8。`_confirm_simplified_strays` 吃同一道閘門。
- **簡體混入修正**（`ocr.py`）：OpenCC **只套用在繁簡混雜的行**（純簡體行是刻意呈現的內容）。字典必須用 **s2tw 而非 s2t**（s2t 會把「吃」改寫成「喫」）。整行全滑成簡體時混雜測試不觸發，靠 `_confirm_simplified_strays` 的**頁面詞彙佐證**解套。
- **頁面詞彙校正**（`ocr.py` 的 `_vocab_correct`）：低分（<0.8）2–4 字純 CJK 行若與頁面詞彙恰差一字就校正；候選以頻次裁決、**嚴格最高頻**才採用（跨詞邊界子串會污染候選）。
- **同籌片漏行救援**（`ocr.py` 的 `_rescue_sibling_bands`）：2–4 字小 CJK 行上下探帶、rec-only 重讀，**只在頁面詞彙能驗證（精確或差一字）時採用**——未驗證就插入會蓋掉正確點陣卻顯示錯字，比漏掉更糟。
- **警告圖示修正**（`ocr.py`）：行首 ⚠ 會被讀成 A。判別是底部連續墨水帶佔框寬（圖示 0.93、真 A 0.24），門檻 0.6，只套用在「行首 A、後接空格或中文」。
- **實心圖示誤認單字母**（`ocr.py` 的 `_looks_like_filled_icon`）：圖示是「填充+描邊」兩個強色叢集（各 ≥25%、距離 ≥60），字形只有一種顏色。⚠️ 背景色**不可用環帶**（緊貼圖示的環帶會落在圖示上），要用框內主色；反鋸齒也會形成第二叢集，必須做**共線檢查**（AA 色落在背景↔字色連線上、殘差 <45）。
- **混淆雙字組修正表**（`ocr.py` 的 `_CONFUSION_BIGRAMS`）：**只收非真實詞彙**的穩定誤讀字對，且已 5× 放大目檢確認。三條原則性路線（局部 rec-only 重讀、YaHei 模板 XOR、滑動對位 IoU）都實測失敗才收表。
- **行首圓點統一**（`ocr.py` 的 `_normalize_bullet`）：·‧・∙ 一律正規化為「• 」（含補空格），`char_boxes` 設 None。
- **行首圓點復原**（`ocr.py` 的 `_restore_leading_bullet`）：以**固定視窗** `[x0−0.9lh, x0+1.0lh]` 搜最左合格圓盤，命中就補「• 」並回傳 `new_x0` 把 bbox 左緣延伸到圓點左邊。⚠️ **「實心」不夠、必須是「圓盤」**：`0.72 ≤ fill ≤ 0.95`（**雙側區間**——只設下界時實心方塊 ■ 的 fill=1.00 三關全過）、`w/h ≥ 0.72`、**上下對稱 min(上半,下半) ≥ 0.70×max**（唯一擋得住 ▼ 與 ⚠ 的判別）。另加三道防誤判：dh ≤0.40lh、左側也要 ≥0.15h 空白、右緣 ≤x0+0.7lh。

#### 字級估計與寬度夾制 — 詳見 `docs/spec/04-流程-字級估計與寬度夾制.md`

- **兩套座標系**：PDF 頁面可以是任何尺寸，但投影片固定 960 pt 寬。`px_to_slide_pt = 960 / image_width`。
- **字級來自字墨而非 OCR 框**：RapidOCR 偵測框的留白不一致。量框內緊貼字形墨水的高度，再除以 `CJK_INK_RATIO`（PowerPoint COM 實際渲染回測所得）。字墨列要**分群取最重的群**（空隙 > 8px 即切群）——跨進框內的外來墨水會灌高字級、讓蓋板塗掉鄰近內容。
- **密集粗體字墨帶不可被 `MAX_INK_ROW_FRAC`(0.85) 切碎**（`style.py`）：近實心列仍從帶的 row set 剔除，但**分組只在「>8 列真空白縫」切、橋接被 cap 剔除的近實心列**。密集粗體在最擠的中段列會超過 0.85，被切碎會量成 6–10pt。
- **同卡堆疊標籤統一字級**（`blocks.py` 的 `harmonize_stacked_overlap_size`，**必須在 `harmonize_font_sizes` 之前跑**）：兩種失效形狀（框 y 重疊 >0.12×min 高／長行被寬度夾而短行未夾）皆拉到較小的可靠字級，「自然字級相近」門檻 **10%**。⚠️ **自然字級要用字墨帶高比較、不可用 `est_pt`**（寬度夾制會壓低 est、把換行孿生誤判成不同級）。
- **拆開的標題碎片取「最多 CJK 字」碎片的字級**（`blocks.py` 的 `merge_row_title_fragments`）：**不可取最大字級**——被全形括號撐高的短碎片會高估。平手取較寬者。
- **行首高圖示要從字墨帶剔除**（`style.py`）：行首間隙內的字墨若 h_lead > 1.25×h_text 即判圖示，從字墨帶清掉（字級與蓋板高都據此），並設 `cover_x0_px` 讓圖示**不被蓋掉**。定蓋板左界時要向左走到真正字緣（char box 內縮幾 px）。
- **混合行（中英夾雜）字級取逐字 CJK 共識**（`style.py` 的 `_cjk_band_height`）：拉丁降部會伸到表意字帶底下 ~0.15em、灌高整行。過半字落在中位數 **±4%** 內才採叢集最大值，否則退回全字最大值。⚠️ 欄窗由逐字 advance 寬度映射（CTC word box 太抖）、每字窗內縮 0.2em、低墨全形標點整窗排除、窗內列範圍同樣套「>8 切群取最重」；**窄欄窗不可套 `MAX_INK_ROW_FRAC`**（CJK 橫筆會貫穿整窗、誤刪成 8pt）。門檻：含拉丁降部字元的行，或含拉丁字母且 ≥2 個表意字票。結果取 `min(完整字墨高, 共識高)`；蓋板與定位仍用完整字墨範圍。
- **同段折行字級調和**（`blocks.py` 的 `harmonize_font_sizes`）：垂直緊鄰（−0.6h~0.45h）、x 重疊 ≥40%、粗體/底色/文字色相同、**snap 前估值差 ≤14%**（⚠️ 底色差門檻是 **25 不是 16**）歸為一組；組內字級散在**相鄰**階就多數決統一。⚠️ 統一目標**不可超過組內任一成員的 `max_fit_pt`**；判斷必須用 `est_pt`（snap 前、寬度夾制後）不可用 snap 後值。純字級觸發的自動粗體（`_autobold_only`）不得擋住歸組，組字級掉到 24 以下時要撤粗。
- **偵測框切到字形時要沿著字墨走出框外**（`style.py` 的 `_ink_run_out`）：框自己的邊緣列有墨（≥0.15 框寬，⚠️ **用 `present` 不是 `rows`**）、且與字墨帶之間**沒有任何空白列**。四道防誤判缺一不可：環帶必須是平整背景（share ≥`BG_MIN_SHARE`）、只對 `ink_h_px ≥ MIN_CUT_BAND_PX`(60) 的行做、走到 cap（0.6×字墨帶高）還沒遇到空白列就整個放棄、**兩側邊距（`CUT_MARGIN_PX`=12）都有墨的列是畫出來的線不是字**。
- **同列標題碎片不可跨越欄線合併**（`blocks.py` 的 `_rule_between`）：表格同列的兩個儲存格幾何上與「被打散的標題碎片」一模一樣。間隙欄裡只要有一欄的墨水覆蓋 ≥0.8 列高就是分隔線、不合併。
- **並排卡片同列共用字級，且必須是多數決**（`blocks.py` 的 `propagate_row_clamp`，在 `propagate_column_clamp` 之後、粗體重判之前跑）：以**堆疊為單位、用組內 est 中位數**比對（不是逐行，否則會把卡片自己拆成兩級）；**被夾的字級必須是這一列家族的多數**（否則一次誤夾會放大成一整列）。⚠️ **不可要求「並排」**——同欄的堆疊也要能被拉動，多數決本身已經足以擋住少數拖多數。
- **被夾制的標題拖動同款孿生**（`blocks.py` 的 `sync_clamped_twins`，字級調和後、粗體投票前跑）：限粗體、est 差 ≤10%、文字色 ≤45、底色 ≤25、em 寬比 0.6–1.6、恰差一個 snap 階；**方向只往下**。折行組要**整組一起拖**、孿生比對不看行長；分組必須用 `harmonize_font_sizes` 的真實分組（`_wrap_partition`）而不是自己近似，且**在任何 mutation 之前算一次**（邊算邊改會讓結果取決於走訪順序）。
- **障礙夾制留邊距、且傳遞給同欄孿生**（`style.py` 的 `OBSTACLE_MARGIN_PT`=3pt + `Style.clamp_pt`；`blocks.py` 的 `propagate_column_clamp`）：撞到障礙時可用寬度再扣 3pt，並取消 snap 容差與懸崖防護。⚠️ 障礙天花板存進 **`clamp_pt` 而不是 `max_fit_pt`**（後者只給投影片硬邊）。`propagate_column_clamp` 讓被夾下來的標籤把同欄、同自然字級、恰高一階的孿生一起拉下並繼承其粗細。
- **`harmonize_across_dropped` 不可越過字級天花板**（`blocks.py`）：統一目標要以 `snap_font_size(target, max_pt=cap, tol=1.0)` 夾住 `max_fit_pt`／`clamp_pt`。
- **被保留原圖的行隔開的段落尾，樣式對齊段落本文**（`blocks.py` 的 `harmonize_across_dropped`，粗體投票後跑）：某個被丟行若落在「同欄（左緣 ≤0.6h）、同色、同底、est 差 ≤20%」的兩個保留行垂直間隙中，就把下行的字級與粗細統一到上行。
- **寬度量測按字種分字型**（`style.py` 的 `_measure_em`）：拉丁段用 Arial、CJK 段用 YaHei 分段加總（全用 YaHei 會高估 ~9%）。單字呼叫與 2 字短串維持原規則。
- **寬度天花板分三級、snap 有懸崖防護**（`style.py`）：**投影片右緣**（硬，`(960−x0−3)/em`，存進 `max_fit_pt`，字級調和的組統一尊重）／**障礙**（近硬，容差只給絕對 3.5pt、**停用懸崖防護**；掃描全程無障礙回傳 inf、完全不設天花板）／**字墨寬**（軟）。懸崖防護僅走字墨寬路徑：超天花板 ≤3% **且**絕對超寬 ≤3pt **且**回退會掉 >10% 才貼上去。**標題足跡檢查**（snap 後，≥`NARROW_MIN_PT` 且 em ≥10）渲染寬 > 字墨 ×**1.07** 且降一階仍 ≥ ×0.90 → 取小；**內文用 1.13**（粗體筆畫加厚字墨帶 ~6%、把真實級推過 snap 中點）。
- **右側餘裕掃描可穿透「細且淡」的障礙**（`_chip_room_right`）：寬 ≤8px **且** Chebyshev <75 才放行；深色障礙（牽引線圓點、卡片邊框）不論多細都停下。
- **同列右鄰寬度夾制**（`blocks.py` 的 `clamp_row_neighbors`，調和器之後跑）：以右鄰左緣為硬上限選最大可容字級；右鄰若是「同款式、同字級、近貼」的孿生則同步縮。
- **同列被拆標題碎片合併成一行**（`blocks.py` 的 `merge_row_title_fragments`，drop 之後、字級調和前跑）：同列（垂直重疊 ≥0.6h）、橫向相鄰（≤0.8h）、同粗體同色的 ≥28pt 碎片併成一行。互疊框重讀同一個邊界標點的重複字要去掉（`_norm_punct` 把全形 ： 摺成 :）；邊界字不同則兩者都保留。
- **程式碼行的拉丁段別被反引號/底線灌高**（`blocks.py` 的 `harmonize_code_block_latin`，字級調和後跑）：純拉丁且含 `_`／`` ` `` 的行若左對齊堆在同色同底、字級更小的含 CJK 行上 → 信任 CJK 量測、夾到 CJK 行字級。

#### 粗體判定 — 詳見 `docs/spec/05-流程-粗體判定.md`

- **粗體判定：≥24pt 一律粗體；≥16pt 用「模板比對」；更小用筆畫門檻**（`style.py`）：固定筆畫門檻（stroke_rel ≥ 0.13）在 16pt 以上完全重疊，救不了。模板比對（`_template_bold_r`）用 YaHei Regular/Bold 各渲染一次、σ=1.2 高斯模糊、以**該行自己的對比度相對門檻**切 ink mask，r =（實測−Regular)/(Bold−Regular)，`BOLD_R_THRESH=0.13`。⚠️ **門檻與 σ=1.2 綁定、不可單獨動其中一個**。適用條件：≥16pt、bg 已知、對比 ≥75、非弧形、濾掉 .notdef 後 ≥2 字；`w_reg > 2.5×實測` 判模板崩壞放棄。14pt 以下模糊噪聲支配 r，維持筆畫規則。
- **鮮豔深色文字（chromatic）退回筆畫規則**（`CHROMA_MAX=15`）：文字 chroma >15 **且文字比底色暗**時改用筆畫 0.13 規則。⚠️ **這條退回只覆蓋模板的「邊際帶」**（`CHROMA_TRUST_R`=0.35）：實測誤判上限是 r=0.27，`bold_r ≥ 0.35` 時模板照樣算數。`blocks.py` 的 `reeval_clamped_bold` 有同一份判別式，**兩邊要一起改**。
- **被夾到 <24pt 後重判粗細**（`blocks.py` 的 `reeval_clamped_bold`，clamp 之後、隊列投票之前跑）：≥24pt 的「一律粗體」只是假設，字級被壓到 24 以下後前提消失、改信量測。對本就 <24 的行是冪等；筆畫判的粗體（無模板分數）不動。
- **粗體隊列投票**（`blocks.py` 的 `harmonize_bold`，**在字級調和之後跑**）：同頁同字級組隊投票，**補粗需 ≥2/3 多數、撤粗需 ≤1/5**。可翻動帶按主判指標分流：模板判的補粗需 r ≥ −0.05、撤粗限 r < `TPL_MARGINAL_R`(0.22)；筆畫判的維持 [0.115, 0.15] 帶，但 r ≥ 0.28 否決撤粗。隊列鍵**不含文字色**。
- **邊緣模板粗體不可鎖死折行組**（`blocks.py` 的 `harmonize_font_sizes`）：r < `TPL_MARGINAL_R` 的模板粗體不阻擋歸組；組內粗體屬**嚴格少數**時邊緣粗體跟隨多數撤掉（1v1 平手不動）。
- **小隊列孤身邊際模板粗體撤粗**（`blocks.py` 的 `harmonize_bold`）：≤4 行的同字級小隊列中若只有 1 行粗、且它是模板判的邊際粗（`bold_r` 非 None 且 < 0.22）→ 撤粗。只撤模板邊際粗，真強調與筆畫判的標題不動。

#### 顏色、蓋板與裁切 — 詳見 `docs/spec/06-流程-顏色、蓋板與裁切.md`

- **光暈取樣要「往外走到顏色穩定」，不能只取一圈**（`style.py` 的 `_surface_around_ink`）：這類簡報的文字帶 10–20px 柔和暗色投影，3–6px 的環會整圈落在投影裡。從字墨往外一次走一段 3px，**只有下一段與這一段一致（Chebyshev ≤`HALO_SETTLED`=5）才承認那是表面**；平坦表面在第一段就成立（與舊行為等價），投影會一路漂移。五段走完仍不穩定就回報「找不到表面」、環帶色照舊生效（≥20 才覆蓋的閘門不動）。⚠️ 這條路徑必須用不繞回的 `_grow`（`np.roll` 會繞到對邊）。
- **投影半徑要取「確認帶」的外緣**（`style.py` 的 `_surface_around_ink`）：settle index i → `3 + 3(i+2)` px；**第一段就穩定＝沒有投影＝0**。存進 `Style.glow_px`。
- **底色要在投影之外重新取樣，取離投影最遠的那一段邊**（`style.py` 的 `_glow_free_bg`）：`RING_PX`=4 貼著框、框又貼著字，**環帶自己也在投影裡**。量到投影時把環帶整個外推再讀四個邊，取**離投影色最遠**的那一段。兩道閘門：與原環帶差 ≤`GLOW_BG_DIST`(20)、share ≥`BG_MIN_SHARE`。⚠️ **取樣半徑要比蓋板的伸展量少一段**（用 `clean_px` 不是 `reach_px`），否則會取到鄰近表面。
- **蓋板要吃掉文字自己的投影，堆疊的同色蓋板之間不可留縫**（`Style.glow_px`、`builder.py` 的 `_cover_band`／`_glow_reach_x`／`_trim_stacked_overlaps`）：底色修對之後殘留的投影反而現形。**垂直** `pad_v = max(4, 0.08h, glow_px)`；**水平** `_glow_reach_x` 從蓋板邊往外走 `glow_px` 欄、**碰到任何真墨水就停**；**行間縫**由 `_trim_stacked_overlaps` 在接縫中點會合，四道閘門——至少一方 glow>0（黑字白底 glow=0，這條規則對它完全不作用）、兩塊底色差 ≤20、縫寬 ≤ max(8px, 0.5×較小字墨高)、縫內整段無墨水。
- **蓋板/文字色的分層情境**（`style.py`）：環帶取樣有兩種失效——文字在灰帶間的近白橫條（用光暈修正；⚠️ 緊貼筆畫的 3px 是被字色染色的反鋸齒區）、框溢出實心膠囊（`ink.mean ≥0.45 且 survival ≥0.45` 偵測，k=3 重推，⚠️ **重推底色必須與環帶色差 ≥40 才採用**）。文字色一律只平均「筆畫核心」（離底色最遠的前 30% 墨水像素）。
- **筆畫核心跨 bg 兩側要選單側**（`style.py` 的 `_core_color`）：核心像素同時含比 bg 暗與亮的兩群時（暗邊框＋白字），平均會得到無意義的中灰。有夠多亮像素（≥0.2）即判淺色文字取亮側，否則取暗側。對「全部同側」是 no-op。
- **淺色/描邊文字落在彩色表面 → 取飽和表面為底**（`style.py` 混合環帶分支開頭的統一判別）：全框 k=3 若**同時有近白群**（min ≥200、share ≥0.15）**與飽和表面群**（min <200、max−min ≥45、share ≥0.18）→ 取飽和表面為底、近白為文字色。
- **黑字＋白暈在「淺色表面」→ 取近白為底、暗字為文字**（`style.py` survival 分支前）：兩叢集為 {近白, 暗} **且環帶近白（min ≥180）** 時成立。粗黑字的筆畫比薄白暈更厚、會被 survival 啟發法誤當底。
- **描邊文字在彩色晶片上：膠囊分支會把白邊當溢出膠囊底**（`style.py` pill/chip 分支）：重推底色 `bg_cand` 近白（min ≥200）而環帶 `bg_ref` 明顯較暗（min <170）→ 保留晶片色為底、把白邊設為文字色。
- **混合環帶的文字叢集可能是 AA 殼**（`style.py`）：ink mask 核心色若比叢集 override 離底色遠 ≥60 → 信任 ink 核心。
- **緞帶/色塊偵測**（`style.py` 的 fallback 分支）：環帶不是單一平面色時，取框內中段水平帶做雙色聚類並對各遮罩**侵蝕**——細筆畫會消失、實心緞帶會存活。單純的主色佔比判斷在這裡會失敗。
- **無蓋板救援**（`style.py`）：斜置籌片的軸對齊框同時罩到多個表面時取字墨帶光暈主色，佔比 ≥0.65 才當蓋板色；漸層/插畫區主色不純、維持無蓋板（透明文字疊點陣會重影）。
- **同晶片堆疊文字共用底色**（`blocks.py` 的 `harmonize_chip_bg`）：垂直緊鄰、x 重疊、同文字色、bg 相近（Chebyshev ≤40）的行歸組，各行 bg 取組內逐通道中位數。排除 `bg_segments` 與不同填色。
- **多色行**（`style.py` 的 `_split_color_runs` + `builder.py` 的 `_split_text_runs`）：逐字 word box 取色分組（合併門檻 ≤45、色差 ≥60 才真正分段——不對稱門檻防模糊雜訊拆散單色行）。**夾在兩段同色 run 之間的單字 run 是 CTC 字框抖動**，併回鄰段。
- **行內多重背景填色（雙色橫幅＋行內 highlight）**（`style.py` 的 `_detect_bg_segments` + `_split_color_runs_segmented`、`builder.py` 的 segment 蓋板）：用**框上下緣列**逐欄取背景色再游程編碼切塊。通用閘門排除漸層/照片：任何塊自身欄色 std > `BG_SEG_STD`(16) 即整行作廢、相鄰塊色差需 ≥`BG_SEG_DIST`(32)。內部邊界精修到**逐通道中位交界中心**，⚠️ **不可貼齊 char box 空隙**（char box 有 padding）。分界兩側文字色不同 → **保留色塊**（builder 先鋪一整條最右填色的全寬底板再疊左段）；文字同色的純行內 highlight → **移除色塊**（輸出字型步進寬與原稿不同、對不上），改用符合環帶的底色整片蓋掉，涵蓋整框高 ±max(8,0.12h)。
- **蓋板裁掉框緣外溢的彩色圖示**（`style.py` 估 `cover_x0_px`/`cover_x1_px`、`builder.py` 套用）：判別用**色彩飽和度**（max−min >60）。四道閘門：行首/尾字須是真字母或 CJK、圖示與文字間須有空白欄縫、裁後文字區仍 >20px、**文字本身飽和（>60）時整條停用**（鮮豔文字每欄都飽和，掃描會停在字內筆畫、把真字裁掉）。
- **同列重疊框要裁切**（`builder.py` 的 `_trim_row_overlaps`）：在渲染圖上找兩段字墨間最寬的空白欄縫，兩框各裁到縫中線兩側（各退 `COVER_PAD_PX`）。⚠️ **嵌套框必須整對跳過**（`bx1 ≤ ax1`），且邊界要**鉗回兩框之內**——否則會寫出負寬度、PowerPoint 判檔案損毀。
- **垂直堆疊框要裁切**（`builder.py` 的 `_trim_stacked_overlaps`，緊接 `_trim_row_overlaps` 後跑）：在 x 重疊區找兩字墨帶間的空白列縫。⚠️ **要裁的是「實際塗出來的色帶」而不是字墨界**——寫進 `Style.cover_band_px`（單一 tuple），否則 builder 會把剛買到的間隙又還回去；覆寫之後**仍要再套一次「自帶底色的 leading 延伸」**。邊界必須**鉗制到 ≤ 下行字頂 − `COVER_PAD`**（下行字頂是可靠分界、上行字墨帶才是被灌爆的那個）。
- **`--cover` / `--no-cover` 只決定「色塊畫在哪」，不是精緻度開關**（`builder.py` 的 `self.cover`）：`--no-cover`（**2026-08-24 起是預設**）是讓底色長在文字方塊上（移動文字時底色跟著走）。三件事**不可**掛在 `self.cover` 上：同列／堆疊裁切（幾何修正）、**雙色橫幅的分段蓋板**（一個形狀承載不了兩種填色）、**弧形切線段**（否則成了唯一的可編輯文字疊點陣重影）。`_arc_geometry` 必須在 `self.cover` 閘門**之後**才呼叫。
- **預設是 `--no-cover`，理由是可維護性不是外觀**（2026-08-24 在 `guard` 十五頁上量過）：兩模式的視覺差異在肉眼閾值以下（最大一頁 0.22% 像素差 >8 灰階，全落在色塊上緣的背景線條，因為 no-cover 的色帶高 0.20em），所以取捨全在編輯性——形狀數 255 vs 463，且 227 行**行為一致**；`--cover` 下仍有 19 行自帶填色（雙色橫幅、行內 highlight、弧形），使用者無從分辨哪一行要點兩層才改得到底色。⚠️ **代價**：`--no-cover` 下刪掉文字方塊會連底色一起刪、露出背景圖的點陣原字（再也刪不掉）。正確做法是**清空文字、保留形狀**，README 的「編輯輸出的 PPTX」有寫給使用者。完整量測見 `docs/spec/06`。
- **`--no-cover` 的色帶比字墨高 0.20em，裁切必須知道**（`builder.py` 的 `_cover_band`）：文字方塊頂端是 `ink_top − LEADING_COMP(0.20)×字級`。`nudge` 恆大於 `pad_v`(≈0.09em)，所以 cover 模式下**每一行**都會拆成獨立色塊＋透明文字框（形狀數約兩倍，這是設計不是 bug）。⚠️ `--no-cover` 時下方色塊的頂端**不可被推下去**（被字形位置釘死）。
- **文字垂直定位**（`builder.py`）：形狀同時是蓋板（須涵蓋整個 OCR 框）與文字容器（字形須落在點陣字墨上）。小字時兩者衝突——用 `margin_top` 內距解耦，**絕不要移動形狀頂端**。

#### 特殊版面與丟棄規則 — 詳見 `docs/spec/07-流程-特殊版面與丟棄規則.md`

- **弧形文字**（緞帶橫幅）：弧度只能從**救援片段中心**擬合（`_arc_from_fragments`），逐欄追蹤法與 PowerPoint 的 `prstTxWarp` 都已試過並放棄。輸出改用 **3–6 段切線文字方塊**沿拋物線排列，色塊用 12 條沿弧條帶（條帶高 1.5×字高）。拋物線錨點**不能用 bbox**（救援片段會灌高），`_arc_geometry` 在中心與 ±0.65 翼側量字墨 run 重新錨定。垂直範圍以 `_ribbon_limits` 夾進緞帶本體（只縮不放）；相鄰條帶要加 `strip_h × Δslope` 的重疊量補楔形縫。
- **直排 CJK 文字**（`style.py` 的 `_is_vertical_cjk` + `_estimate_vertical`、`builder.py` 的 `vert="eaVert"`）：框高窄、≥2 個純 CJK 字、每字格近正方（框高/字數 ÷ 框寬 ∈ [0.55,1.7]）。字級取**字欄寬**而非堆疊高。
- **傾斜需要基線：單一字形的角度一律丟棄**（`ocr.py` 的 `_tilt_has_baseline`）：只有一個字就沒有基線，min-area quad 會鎖到筆畫方向（實測單字「1」回了 26.31°、分數卻 0.956）。閘門放在偵測器 quad 與旋轉救援兩處；救援的**文字照樣採用**，只丟角度。⚠️ **不可改用「去斜後 w/h 狹長」當閘門**（兩字的斜置籌片 w/h 只有 ~1.1）。
- **傾斜文字，兩種情況**：(1) 偵測器回傳旋轉 quad → `style.py` 用 PIL `Image.QUAD` **轉正後的貼片**取樣（用軸對齊外接框會混入周圍顏色）、`builder.py` 輸出旋轉形狀；(2) 偵測器合併成軸對齊框（弧形緞帶）→ 分數 <0.75 觸發旋轉救援，以 ±3/6/9° 重新 OCR，只留「自身高度帶跨過裁切中心」的片段，取最長且分數最高的組合。高度要用片段高度的**中位數**（quad 聯集會灌爆）。
- **浮水印品名是清單、不是單一字串**（`cli.py` 的 `WATERMARK_MARKS`）：產品會改名而舊 PDF 仍帶舊標記，字尾比對要同時收 `notebooklm` 與 `gemininotebook`（去空格、casefold）。⚠️ **位置閘門補不了這一刀**（底右角會命中橫跨頁底的引言帶），只有品名能區分。前綴雜字上限「扣掉標記後 ≤6 字」。
- **浮水印遮擋範圍要量、不能用框高的倍數猜**（`cli.py` 的 `watermark_wipe`）：在遮擋盒自己的欄帶內掃描列墨、加 `WIPE_PAD_PX`=2，最後**鉗進 [tight, wide]**——不得比使用者調過的緊盒更緊，也不得超過 0.35h/0.40h。掃描失敗就退回已知可用的行為。
- **掃描要「從浮水印往外走、遇空白就停」，不是取視窗內最外側的墨**（`cli.py` 的 `_contiguous_ink`）：`min/max` 版本會把碰巧靠近角落的頁面元素一起吞進來再放大塗掉。從文字帶取 seed 向外走，連續 `gap` 列全空白就停（浮水印自己的零件之間沒有空白列）。三個 gap 不同（皆下限 4px）：垂直 0.15h、**左 0.35h**、**右 0.10h**。⚠️ **列門檻 `WIPE_INK_FRAC` 是 0.05 不是 0.01**（貫穿的頁框直線只佔 1–2% 欄，卻會讓掃描永遠走不到頭）。水平也要量，且允許往框內縮 `WIPE_SHRINK_FRAC`=0.3h（實測兩份 deck 的 logo **都在框內**）。
- **太小/模糊的行不轉文字、保留原圖**（`blocks.py` 的 `drop_illegible_lines`，浮水印過濾後、字級調和前跑；`--keep-tiny-text` 可關）：≤9pt 預設丟（乾淨小字放行：純 CJK 2–4 字且 ≥0.85，其他 ≥0.94）；≤14pt 且 <0.72 丟；≤3 字無 CJK 且 <0.75 丟。門檻判不動的高分孤立殘黨用**雜訊鄰域擴散**收（弱行 2×行高內有已丟行、又沒有強行鄰居 → 一起丟，迭代到不動點），第三遍**孿生一致性**（同組籌片丟一半比全丟/全留都醜）與擴散同一迴圈。
- **無法忠實還原的行保留原圖**（`blocks.py` 的 `drop_unreproducible`，`drop_illegible_lines` 前跑、與 `--keep-tiny-text` 無關）：(a) 線稿圖示被讀成單字母（單一拉丁字母、≥28pt、該列無其他真文字）；(b) markup 示範的刪除線（含 `==` 且含 `×`）；(c) 上下標公式（短純英數行切欄量字墨重心、散佈 >0.25 行高）；(d) 線稿圖示被讀成單一 CJK 字（YaHei 渲染比 IoU，**保守取 AND**：分數 <0.92 **且** IoU <0.30）；(e) 未偵測到旋轉的圖表軸標籤（`h > 2.5×w` 且非 vertical/angle/arc、含 CJK）。
- **書封/告示牌式斜排裝飾字成對丟棄**（`blocks.py` 的 `_rotated_decor_pair`）：兩行 |角度| ≥15°、皆無 CJK、旋轉矩形互相交疊（雙方坐標系都檢查）→ 整對留原圖。CJK 豁免（斜置金字塔籌片是真內容）。

#### PPTX 建構 — 詳見 `docs/spec/08-輸出-PPTX建構.md`

- **單行形狀關閉自動換行**（`builder.py`）：字級估計只要略大一點就會把整行折行、毀掉版面。
- **CJK 字型需要直接寫 XML**：python-pptx 的 `font.name` 只設 `<a:latin>`；必須用 `_set_east_asian_font()` 注入 `<a:ea>`，否則中文會悄悄回落到佈景主題字型。
- **上標註腳標記**（`style.py` 估 `Style.superscript_tail`、`builder.py` 的 `_mark_superscript` 設 `baseline="30000"` + 0.65 倍字級）：text 結尾符合 `\[\^?\d+\]$`、且該標記字墨**明顯上移**（底部離本文底 ≥0.2 本文高）**又較矮**（≤0.92 本文高）。程式碼框內的字面 `[^1]` 不上移 → 排除。
- **刪除線**（`style.py` 估 `Style.strikethrough`、`builder.py` 設 `strike="sngStrike"`）：純覆蓋率分不開「畫出來的線」與「碰巧對齊的 CJK 橫筆」。判別（cov ≥0.70 的連續列為「帶」）：**恰好一個帶**、帶厚 **≤0.10×字高**、峰 cov ≥0.80、帶中心落 0.30–0.70h、峰 ≥1.5×全列中位、帶外字身中位 ≤0.6×峰。⚠️ **厚度不可再放寬**——「牽一髮動全身」四個前置閘門全過，厚度是它唯一的判別器，餘裕只剩 0.10-vs-0.13。⚠️ builder 必須加 `len(block.lines) == 1` 閘門（與 `runs`／`superscript_tail` 一致）。

### 尚未處理的結構性風險（查證過、決定不動）

2026-08-23 code review 留下三個，完整說明在 `docs/spec/11-附錄-已知風險與否決的路.md` §11.1：`_surface_around_ink` 對投影深度的反應不是單調的（深到灰滿前兩段時會把投影色當成表面、`glow_px=0`）、`harmonize_bold` 會用 `bold_r` 推翻 `CHROMA_TRUST_R` 剛做的判斷（同一個 `bold_r` 在檔案裡有三個互相矛盾的信任門檻）、`_cover_band` 的垂直 `pad_v` 沒有墨水測試（同半徑的水平版 `_glow_reach_x` 有）。**動之前先讀那一節的理由與反例。**

## 驗證

⚠️ **`SOURCE.pdf` 與 `SOURCE.pptx` 已於 2026-08 由使用者刪除，救不回來**（`.gitignore` 從第一個 commit 起就擋 `*.pdf`／`*.pptx`，它們**從未進過版控**；回收桶與全機掃描也都沒有）。連帶後果要記住：本檔「架構」章裡引用**早期 p-編號**的案例（p5 Git Hook 家族、p8 單一/主專案、p13 第三階段、p14 啟動【技術解法】…）**再也無法回歸測試**，只剩文字記錄；2026-08-23 那輪 code review 就因此無法驗證 `nat_close` 門檻的餘裕。⚠️ **p-編號有兩種來源**：p11–p15 那批新的（藍籌片投影、橘色橫幅、五張並排卡片、步驟徽章「2」）指的是 `AI_Quality_Guardrails`，不是 SOURCE.pdf。

現行語料是磁碟上這四份 deck（比單一 deck 更廣，2026-08-23 的每一次驗證都跑在它們上面）：

| 代稱 | 檔案 |
|---|---|
| `guard` | `C:\SOURCE5\AI協作開發的軟體品質護欄\AI_Quality_Guardrails.pdf`（p11–p15 案例的來源；同資料夾的 `.pptx` 是使用者自己的存檔，**不是基準**） |
| `trans` | `C:\SOURCE5\Raw_Sources\大模型架構\Transformer_演進地圖_NotebookLM簡報.pdf` |
| `gptbp` | `C:\SOURCE5\Raw_Sources\大模型架構\GPT图解 大模型是怎样构建的\blueprint\The_GPT_Blueprint.pdf` |
| `rlbp` | `C:\SOURCE5\FUTURES\AI\Reinforcement_Learning_Blueprint.pdf` |

驗證步驟（**為什麼這樣驗**見 `docs/spec/10-驗收準則與測試策略.md`；**照著打就能跑的指令、語料路徑與清掃 glob** 見 `docs/dev/verification.md`）：

1. `uv run pytest` — 文件與程式碼的一致性（常數值、指路、CLI 旗標三方一致、否決索引）。**這一支只驗機器驗得到的**，語意的真值仍然只有步驟 3。
2. **四份 deck 全跑、逐行對照 `--debug` 的 JSON**：改動前先跑一輪存成基準，改完再跑一輪，比對每一行的 `text`／`font_pt`／`bold`／`bg_rgb`／`text_rgb`／`est_pt`。**預期是零差異**——有差異的每一行都要能說出為什麼，說不出來就是回歸。這比「跑一份看起來對」強得多：2026-08-23 靠它抓到「一個 +3px 的取樣半徑讓 16 行底色漂移 7–14 個單位、數行整個落到鄰近表面」。
3. **視覺驗證（樣式的唯一真值）**：用 PowerPoint COM 把投影片匯出成 PNG（`$pp = New-Object -ComObject PowerPoint.Application; ...Slides.Item($i).Export(path,"PNG",1376,768)`），與 `pymupdf` 的頁面渲染圖上下並排比對，比對圖放 `verify/`。
4. `uv run python tools/compare_pptx.py generated.pptx reference.pptx` — 文字召回率比對。⚠️ **參考檔絕不可拿來校準樣式**：舊的 `SOURCE.pptx` 的粗體／字級旗標是從我們自己更早的輸出繼承來的（使用者只修文字、不修樣式），2026-06-12 那批漏判的粗體**在參考檔裡同樣是錯的**。來歷見 `docs/dev/collaboration.md` §5。目前沒有現成的參考檔，這一步只在使用者提供時做。
5. **刪除其餘驗證產物**（使用者指示，⚠️ **這條失守過兩次**：2026-06-12、2026-06-14，兩次成因一模一樣）：**不要憑記憶手打片段的 `rm`**——兩次都是漏了 `--debug` 的疊圖 `*.debug.p*.png`，而且 `.gitignore` 已經涵蓋這些檔，所以 `git status` 看起來乾淨、**把殘留藏起來了**（**乾淨的 git status 不等於乾淨的工作目錄**）。每一輪的**最後一個動作**跑一次完整掃描：

   ```bash
   rm -rf verify/ *.debug.json *.debug.p*.png dbg*.pptx dbg.* _dbg_*.py nul.* *.tmp.png
   ls -1   # 強制：目視確認只剩原始碼、設定檔、.bat 與要交付的 .pptx
   ```

   `ls -1` 那步不可省——它才是抓得到「漏掉一個 glob」的那一步。**永遠不要刪要交給使用者驗收的產出 `.pptx`**；臨時檔一律寫到 scratchpad 目錄，不要落在專案裡。見 `docs/dev/collaboration.md` §6。

## 環境注意事項

- OCR 推論裝置：`--device` 預設 auto（DirectML > CUDA > CPU 依可用性）。本機裝的是 `onnxruntime-directml`（取代 CPU 版 onnxruntime）。實測 Intel Arc 140V：**DirectML 全份 50s vs CPU 210s（快 4.2×）**。注意**首次** DML 執行要付一次性的驅動 shader 編譯（該次 ~213s、看似與 CPU 同速）——編譯結果由 Intel 驅動存碟快取，之後每次執行都命中快取；不要被首跑數據騙了下「GPU 沒用」的結論（我們差點下錯）。rec_batch_num 加大反而變慢（16: 0.98s vs 6: 0.80s/頁），維持預設
- Windows 11、Python 3.14；主控台是 cp950 — 印中文的腳本需要 `python -X utf8`（cli.py 自己會重設 stdout）
- PowerShell 會吃掉 `python -c` 單行指令中的雙引號；這類情況請改用 Bash 工具或腳本檔。git commit 訊息中的雙引號也會被截斷 — 訊息避免使用雙引號
- 中文輸出字型預設 Microsoft YaHei（之前為 Noto Sans TC）；**所有拉丁字元一律輸出 Arial，但 ≥28pt 的大標題拉丁改用 Arial Narrow**（使用者指示 2026-06-12：來源字型拉丁較窄，p4「Layer 0: 三層架構分工與 Karpathy 模式知識庫」用 Arial 渲染明顯過長 — 改字型、不可降字級）：每個 run 設 `<a:latin>`=Arial/Arial Narrow（依 `NARROW_MIN_PT`） + `<a:ea>`=YaHei，PowerPoint 按字元類別自動選用，中英混排同行雙字型、不需拆 run；`--font` 只控制東亞字型。寬度量測用 `C:\Windows\Fonts\msyh.ttc`（純拉丁且 ≥3 字的行改用 arial.ttf、出生估字級 ≥28 的行拉丁段改用 ARIALN.TTF 與輸出一致 — 2 字短串的寬度夾制騎在 snap 平手點上，曾把 p8「98」籌片從 20pt 推成 24pt 粗體；`_cjk_band_height` 的單字映射一律維持 YaHei 度量、不可改）；本機另有 Noto Sans TC 在 `%LOCALAPPDATA%\Microsoft\Windows\Fonts\` 作為備援
- 提交與推送、回覆語言、字型選擇等協作規則見上方「協作方式」章節
- 相依鎖定、字型檔路徑、cp950／PowerShell、四個入口與 GUI 手抄清單的細節見 `docs/dev/windows-環境與入口.md`；決策層（為什麼要 GPU build、首跑編譯不可當效能結論、換字型要重跑回歸）在 `docs/spec/09-執行環境與效能.md`
- ⚠️ **`啟動.vbs`（無黑視窗）藏掉主控台＝藏掉所有錯誤**，所以要有兩個落點：**啟動期的失敗當場跳訊息框**（攔訊息的暫存檔在 `%TEMP%`、用完即刪，**專案資料夾不留 `啟動.log`**），**執行期寫進 `logs\<啟動時間>.log`**（一次執行一個檔、檔頭記版本、逐次 flush、保留 30 天，形狀照 `meeting-scribe`）。**動到啟動或錯誤處理路徑之前先讀 `docs/dev/gui-啟動與錯誤留底.md`**（決策層在 `docs/spec/09` §9.3）。三個違反了才會發現的陷阱：GUI 留底要寫 `App.__init__` **存起來的** `self._boot_stderr`，**不可讀當下的 `sys.stderr`**（轉檔期間那是 `QueueWriter`，只到日誌區、關窗就沒）；寫紀錄檔**要上鎖**（畫面是主執行緒寫的、轉檔 traceback 是背景執行緒寫的）；`.vbs`／`.bat` 的註解**不能用 ⚠**（cp950 編不出 `U+26A0`，寫檔當場 `UnicodeEncodeError`）

## 量過而否決的路（別再做一次）

（章節格式自 `MP4-2-SRT` 移植 2026-08-23。）這些都做出來或量過了，**完整結論在 `docs/spec/` 對應的那一條裡**（也收進 `docs/spec/11-附錄-已知風險與否決的路.md` §11.2），重做之前先讀那一段——這裡只當索引：

- **把「逐字 CJK 共識」放寬到所有 CJK 行**——見「混合行（中英夾雜）字級取逐字 CJK 共識」。±3px 模糊雜訊讓其他臨界行掉一階、把原本一致的區塊拆散（p4 右註解 16→16/14/14），單 CJK 字的行（「2025 年」）更會塌到單字高度。
- **稀疏邊緣列裁切（< 0.25×中位數）**——同上一條，以及「黑字＋白暈在淺色表面」。曾把 44pt 標題裁成 40pt；`高耦合金流邏輯` 的字級因此暫留不動。
- **全文性的字墨足跡天花板**——見「寬度天花板分三級、snap 有懸崖防護」。來源 CJK 字距比 YaHei 均勻緊 ~10%，能抓住超寬標題的容差同時會勒死正常內文（1.098），曾一口氣降了 56 行。
- **餘裕掃描的「物件 vs 牆」分類**——同上。使用者裁定牽引線圓點也不可壓，被夾下來的標題改由 `sync_clamped_twins` 拖動全家族。
- **弧形文字用純影像逐欄追蹤法擬合弧度**、**輸出端用 PowerPoint 的 `prstTxWarp`**——見「弧形文字」。前者兩條平行弧線互相污染、連符號都會判反；後者字級/路徑縮放行為不透明，長文字會繞圈重疊。
- **行內 highlight 色塊照原稿保留**——見「行內多重背景填色」。輸出字型步進寬與原稿不同、中段字前面累積位移、色塊對不上文字（使用者否決），改為 `highlight_removed`。
- **偵測器換更大的 `max_side_len` / det server 模型救漏行**——見「同籌片漏行救援」。4096 與 server 模型都更糟。
- **靠局部窗 rec-only 重讀、YaHei 模板 XOR、滑動對位 IoU 分辨「只差部首」的誤讀字**——見「混淆雙字組修正表」。三條原則性路線都實測失敗，才收下辭典型修正。
- **`_glow_free_bg` 用蓋板的完整伸展量取樣**——見「底色要在投影之外重新取樣」與 2026-08-23 的 code review 修正。四份 deck 實測 16 行底色漂移 7–14 個單位，數行整個落到鄰近表面。
- **`sync_clamped_twins` 自己近似折行分組**——見「`sync_clamped_twins` 的折行組要整組一起拖」。只看直接鄰居會讓三行以上的段落只被拖走前兩行；邊迭代邊改 `font_pt` 還會讓結果跟著行的排列順序跑。
- **`propagate_row_clamp` 直接改用 `_wrap_partition` 的分組**（2026-08-23 `/simplify` 提案，未採納）：兩者述詞不同（多了 est 14% 與邊際粗體例外），換過去會改變分組結果。
