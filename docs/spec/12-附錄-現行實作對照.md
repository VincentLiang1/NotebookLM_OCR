# 12. 附錄：現行實作對照

> 前 11 章是技術中立的規格。這一章是**現行實作**（Python 3.14 + PyMuPDF + RapidOCR + python-pptx）的對照表——想沿用同一條路的人可以直接對照；想換技術棧的人請把它當成「這些坑長什麼樣」的參考。
>
> ⚠️ **這一章是隔離區。** 規格正文不出現模組檔名與私有函式名，全部集中在這裡。新增一條規則時：**規則本身**寫進對應章節，**指路**寫進這裡。這個安排讓正文換一種語言重寫時可以整章拿走，而這一章是唯一要重寫的。
>
> ⚠️ **常數的正典永遠是程式碼**，這裡與 `CLAUDE.md` 都是整理版（`tests/test_docs.py` 釘著值要一致）。

## 12.1 技術棧

| 層 | 選用 | 為什麼 |
| --- | --- | --- |
| 語言／環境 | Python 3.14，`uv` 管理 | |
| PDF 渲染 | PyMuPDF @200dpi | 同一張渲染圖**同時**供 OCR、樣式估計與投影片背景使用，全程不重繪 |
| OCR | RapidOCR，PP-OCRv5 + **server** 辨識模型 | 繁中準確度遠勝預設的 v4 mobile；`--fast` 可切回 |
| 推論後端 | ONNX Runtime **DirectML** | 見 §9；本機裝的是 `onnxruntime-directml`，取代 CPU 版 |
| PPTX 輸出 | python-pptx（部分直接寫 XML） | 東亞字型必須自己注入 `<a:ea>`，見 §8 |
| 簡繁 | OpenCC，**s2tw**（不是 s2t） | s2t 會把「吃」改寫成「喫」，見 §3 |
| 字寬量測 | Pillow 讀 `msyh.ttc`／`arial.ttf`／`ARIALN.TTF` | 換字型不會跟著換度量衡，見 §4 |

## 12.2 模組地圖

| 檔案 | 職責 | 規格章節 |
| --- | --- | --- |
| `pdf2ppt.py` | CLI 薄殼 | — |
| `pdf2ppt_gui_2.py` | Tkinter 圖形介面。⚠️ **選項清單是手抄 argparse 的** | §9 |
| `cli.py` | 逐頁管線調度、浮水印偵測與遮擋 | §2.6、§7 |
| `render.py` | PyMuPDF @200dpi 渲染 | §2.1 |
| `ocr.py` | RapidOCR 呼叫 + 十四條文字修正 | §3 |
| `style.py` | 字級、顏色、粗體、幾何的逐行估計；寬度量測；字級階梯 | §4、§5、§6 |
| `blocks.py` | 十六個調和／丟棄 pass | §4、§5、§6、§7 |
| `builder.py` | 裁切、畫形狀、寫 XML | §6、§8 |
| `models.py` | `Line`／`Style`／`TextBlock`／對齊常數 | §2 |

依賴方向：`cli` → 管線 → `models`，不回頭。兩條刻意的邊：

- **`blocks` import `style`**（`_measure_em`／`snap_font_size`／`text_width_em`）：調和 pass 要重算「這個字級放不放得下」，而寬度量測只能有一份。反向不成立。
- **`builder` 不 import `style`**：它只吃 `Style` 的欄位。⚠️ 這條邊正是 §2.3 說「裁切必須寫 `cover_band_px` 而不是改字墨界」的原因。

## 12.3 §2 領域模型的名詞對照

| 規格正文的說法 | 現行實作 |
| --- | --- |
| 三個資料結構 | `pdf2ppt/models.py` 的 `Line`、`Style`、`TextBlock` |
| 字級階梯（§2.5） | `FONT_SIZES`。⚠️ **定義在 `style.py`，不在 `models.py`** |
| 樣式估計（§2.3） | `style.py` 的 `estimate_style()` |
| 輸出階段（§2.3、§2.4） | `builder.py` |
| 對齊 | `models.py` 的 `ALIGN_LEFT`／`ALIGN_CENTER`／`ALIGN_RIGHT` |
| `TextBlock.bbox` 的覆寫欄位（§2.4） | `_bbox` |
| 「除非要求合併行」（§2.4） | `--merge-lines` |
| 頁面詞彙校正（§2.2 的 score 0.8） | `ocr.py` 的 `_vocab_correct`，詞表由 `_page_vocab` 產 |
| 「這頁的模型在滑」的確認（§2.2） | `ocr.py` 的 `_emits_simplified` |
| 丟棄規則的分級（§2.2） | `blocks.py` 的 `drop_illegible_lines` |
| 同列裁切／堆疊裁切（§2.6） | `builder.py` 的 `_trim_row_overlaps`／`_trim_stacked_overlaps` |

## 12.4 §2.6 管線 pass 對照

依 `cli.py` 的實際呼叫順序：

| 規格正文的說法 | 函式 |
| --- | --- |
| 逐行樣式估計 | `estimate_style` |
| 浮水印過濾 | `watermark_wipe` |
| 丟棄無法忠實還原的行 | `drop_unreproducible` |
| 丟棄太小／太模糊的行 | `drop_illegible_lines` |
| 合併同列被拆開的標題碎片 | `merge_row_title_fragments` |
| 統一同卡堆疊標籤的字級 | `harmonize_stacked_overlap_size` |
| 折行字級調和 | `harmonize_font_sizes` |
| 程式碼行的拉丁段夾制 | `harmonize_code_block_latin` |
| 被夾制的標題拖動同款孿生 | `sync_clamped_twins` |
| 同欄字級傳遞 | `propagate_column_clamp` |
| 同列字級傳遞（多數決） | `propagate_row_clamp` |
| 被夾後重判粗細 | `reeval_clamped_bold` |
| 粗體隊列投票 | `harmonize_bold` |
| 跨丟棄行的樣式對齊 | `harmonize_across_dropped` |
| 同列右鄰寬度夾制 | `clamp_row_neighbors` |
| 同晶片堆疊共用底色 | `harmonize_chip_bg` |
| 行 → 形狀 | `lines_to_blocks` |
| 產生投影片 | `builder.add_slide` |

---

⚠️ **這一章會隨各章清掃逐步長大。** 目前只收了 §2 的對照；§3～§8、§9～§10 的指路仍留在各章正文，尚未搬過來。
