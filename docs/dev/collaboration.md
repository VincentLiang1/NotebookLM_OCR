# 協作方式（完整版）

`CLAUDE.md` 的「協作方式」只抄了**必須當場生效**的部分（只有 `CLAUDE.md` 每次自動載入）。這一份放理由、沿革與實測災情——動到相關行為之前先讀這裡。

## 1. 「記住 / 記憶下來」= 寫進 repo，不是寫進 Claude Code 的記憶功能

2026-08-17 移植自 `C:\SOURCE5\Python\meeting-scribe`（該規則 2026-08-06 在 meeting-scribe 與 `MP4-2-SRT` 定案，2026-08-10 推及 `C:\SOURCE5\WIKI` 與 `C:\SOURCE5\FWIKI`，措辭一致；本專案是第五個）。

**Why**：記憶功能的目錄在 `%USERPROFILE%\.claude\projects\<由專案路徑編出來的名字>\`，**在 repo 之外、git 不追蹤**，而使用者換電腦的方式是**複製專案資料夾**——寫在那裡的東西一份都帶不走。更麻煩的是那個目錄名是從絕對路徑編出來的：光是把專案搬到別的資料夾，名字就對不上，記憶等於憑空消失。同理，`C:\Users\vli\.claude\CLAUDE.md`（全域規則）也帶不走，所以跨專案規則若對本專案是關鍵的，要在這裡再寫一份。

**How to apply**：聽到「記住 / 記憶下來 / 以後都要…」時——

1. **必須當場生效**的規則 → 寫進 repo 根目錄的 `CLAUDE.md`（只有它每次自動載入）。
2. **領域細節、沿革、災情紀錄** → 寫進 `docs/dev/`，並在 `CLAUDE.md` 留一句指路。**只寫在 `docs/dev/` 等於沒寫**——那要有人想到去讀才載得進來。
3. 然後**一併提交推送**（本專案本來就是自動 commit + push，見 §3）。
4. **在回覆裡說清楚寫進哪個檔**，讓使用者知道換機器之後還在。

`C:\Users\vli\.claude\projects\C--SOURCE5-Python-NotebookLM-OCR\memory\` 已退役，原地只留一份墓碑指回這裡。

## 2. 回覆語言

**回覆使用者一律用繁體中文（台灣用語），不必等他提醒。** 專有名詞、API 名稱、程式碼、路徑、log 原文照原樣保留。

使用者本身以繁中書寫，且**內容領域也是繁中**——這不只是溝通偏好，它直接決定了技術選擇：OCR 必須用 PP-OCRv5 **server** 辨識模型而不是預設的 v4 mobile（繁中準確度差很多），`--fast` 才切回 mobile。

## 3. commit 訊息、自動提交與 README 同步

**commit 訊息一律繁體中文**（標題與內文，使用者 2026-06-11 指示；技術名詞、函式名、常數、px 數值保留原文）。理由：專案文件本身就是繁中，兩邊一致。

**修正完成並驗證後自動 commit + push，不要再問**（使用者 2026-06-11 指示）。使用者把 GitHub repo（`VincentLiang1/NotebookLM_OCR`）當成即時紀錄，不想每輪下 git 指令。

**功能新增或修改時，`README.md` 要在同一輪同步更新後一起提交**——過時的 README 讓 repo 失去意義。判準是「使用者看得到的行為變了沒有」：新 CLI 選項、新行為、改掉的預設值都算；純內部重構不算。

⚠️ 訊息裡**避免雙引號**（PowerShell 5.1 會截斷），結尾加 `Co-Authored-By`。

⚠️ **`CLAUDE.md` 是被 git 追蹤的**——舊記憶裡寫「CLAUDE.md 被 gitignore」是**錯的**（`.gitignore` 只擋 `*.pdf` / `*.pptx` / `verify/` / `*.debug.*` 那幾類），提交時不要漏掉它。

## 4. 輸出字型是已經拍板的選擇

中文輸出字型是 **Microsoft YaHei**（使用者 2026-06-11 指示，先前為 Noto Sans TC）。

⚠️ **不要「好心」換成 Microsoft JhengHei**：使用者是繁中使用者，但他**明確選了 YaHei**，看的是 PowerPoint 裡的渲染結果。這個選擇還有下游後果——`style.py` 的 `_measure_em()` 固定載入 `C:\Windows\Fonts\msyh.ttc` 量寬度，整套寬度夾制、snap、標題足跡門檻都是以 YaHei 字寬校準的，換字型不會跟著換度量衡（GUI 的字型下拉選單旁有註明）。

## 5. 參考檔不可拿來校準樣式

比對用的參考 PPTX 只能當**文字**基準（`tools/compare_pptx.py` 的召回率），**絕不可拿來校準樣式**。理由是它的粗體／字級旗標往往是從我們自己更早的輸出繼承來的——使用者只修文字、不修樣式——所以我們漏判的粗體在參考檔裡**同樣是錯的**（2026-06-12 那批就是這樣）。**樣式的真值只有一個：PDF 渲染圖的目視比對。**

專案目標（2026-06-11 起）是做 deckedit.com 的本地替代品：把純影像的簡報 PDF 轉成可編輯 PPTX。已確認的決策：RapidOCR 本地離線、完整 DeckEdit 式輸出（背景圖 + 色塊 + 樣式比對過的可編輯文字）、CLI 介面（後來另加桌面圖形介面）。現行的回歸語料見 `CLAUDE.md` 的「驗證」章。

## 6. 收尾清掃：用一條完整的掃描指令，不要憑記憶手打

**這條失守過兩次**（2026-06-12、2026-06-14，使用者兩次都不高興）。兩次的成因**一模一樣**：我手打一條片段的 `rm`，漏掉 `--debug` 的疊圖 PNG（`*.debug.p*.png`）與臨時腳本。

⚠️ **根因不是文件缺漏**（`CLAUDE.md` 的驗證章節早就列了這些檔），而是兩件事疊在一起：手打指令會漏，而 `.gitignore` 已經涵蓋這些檔案，所以 `git status` 看起來是乾淨的、**把殘留藏起來了**。**乾淨的 git status 不等於乾淨的工作目錄。**

**How to apply**：每一輪修正的**最後一個動作**跑一次完整掃描，不要用眼睛掃：

```bash
rm -rf verify/ *.debug.json *.debug.p*.png dbg*.pptx dbg.* _dbg_*.py nul.* *.tmp.png
ls -1   # 目視確認只剩原始碼、設定檔、.bat 與產出的 .pptx
```

`ls -1` 那步是**強制**的——它才是抓得到「漏了一個 glob」的那一步。

⚠️ 永遠不要刪要交給使用者驗收的產出 `.pptx`。臨時檔一律寫到 scratchpad 目錄，不要落在專案裡。

## 7. 規則搬進 `docs/spec/`（2026-08-23）

使用者指示「CLAUDE.md 裡面可以搬移的內容，移到 `docs/dev` 去」，同一輪還要求「比照 `MP4-2-SRT` 的 `docs` 產出架構導讀與 spec 目錄」。兩件事其實是同一件：**那 78 條不變量本來就是規格書的內容，只是住錯地方。**

**搬之前的問題**：`CLAUDE.md` 113KB、每次對話整份載入，而其中絕大多數規則在任何一次對話裡都用不到。壓縮它的誘惑一直在，但那些句子的價值**全在細節**——門檻是多少、誤殺過什麼、前一版結論是什麼、哪三條替代路線量過而失敗。改寫成摘要等於毀掉它們，下一個人放寬門檻時就再也讀不到「這個值是這樣來的」。

**做法**：81 條頂層條目**逐字**搬進 `docs/spec/03`–`08`（規則 80 條）與 `11`（結構性風險那一條）（一個字都沒改，機器驗過每一條在且只在一個章節裡），`CLAUDE.md` 改留「規則本身 + 關鍵門檻 + 程式指路 + 詳見連結」的索引。CLAUDE.md 因此從 113KB 降到 49KB（−57%），而理由一句都沒有消失。

**為什麼索引仍要帶門檻數字**：只寫「見 spec 04」的索引擋不住任何事——agent 讀到規則名稱不會去翻章節，只會照直覺改。門檻留在自動載入的那一份，才擋得住「順手把 0.72 放寬成 0.6」這種改動；理由留在 spec，是給真的要動它的人讀的。

**釘法**：`tests/test_docs.py` 從 8 條擴到 12 條——常數值與符號指路改成掃 `CLAUDE.md` ∪ 架構導讀 ∪ `docs/spec/*.md`（規則搬家之後任何一份漂掉都一樣害人），另加章號連號、H1 與檔號一致、`00` 的文件地圖與目錄雙向一致、CLAUDE.md 指得到每一章。⚠️ 最後這條是整個安排的支點：**CLAUDE.md 是唯一自動載入的檔，它沒指到的章節等於不存在。**

### 7.1 不要為了跟 `meeting-scribe` 一致而重排（使用者裁定 2026-08-23）

讀過 `meeting-scribe`（本系列的原始專案）之後很容易想對齊，**但它的形狀是它的處境長出來的，不是通則**。它的 `docs/spec/` 技術中立、`docs/dev/` 依領域切成九份；本專案的 spec 綁現行實作、dev 只放協作沿革。兩種都對，判準只有一條：

> **同一批知識要不要餵給第二個實作。**

`meeting-scribe` 有 `docs/ios-app/`（Swift，程式碼一行都不共用），中立化的成本被攤掉了；它 2026-08-23 決定 macOS 版走**同一份 repo**，答案也不是把 spec 中立化，而是**另開 `docs/mac/` 只寫平台差異、一句領域知識都不重述**。本專案沒有第二個實作，**macOS 版也不打算做**（使用者 2026-08-23：「只是想，但是應該不會做」）。

分兩層的代價是**同一批實測數據存兩份**，而這個 repo 家族已經三次證明副本會漂（`meeting-scribe` 與 `MP4-2-SRT` 都寫在自己的 §0.6；本專案的 `CJK_INK_RATIO` 在**只有一份副本**時就漂了兩個多月）。現在的 `docs/spec/` 是逐字搬、機器驗過的**零副本**狀態，不要拿去換整齊。

⚠️ 中立版規格只在**換語言重寫**時才划算，而且那時是**另開一批**（像 `docs/ios-app/`），不是把現有 spec 改中立。

從 `meeting-scribe` 值得抄的只有一樣，已經抄了：每章開頭那句**「動到 X 之前請先讀完這一章」**——把「什麼時候該讀」寫死在文件裡，而不是指望讀的人自己判斷（`tests/test_docs.py` 釘著）。

## 8. 改了行為，文件要一起改（落點對照表）

（自 `MP4-2-SRT` 移植 2026-08-23；2026-08-25 從 `CLAUDE.md` 搬進來，那邊只留判準與三個最常撞到的落點。）

事後補的文件跟事後補的測試同一種病：**沒人會回頭做**，而規格一旦有一條是舊的，讀的人就分不出哪幾條還能信。**這個 repo 已經吃過虧**——2026-08-23 那輪 code review 一次抓出 `CLAUDE.md` 有五處與程式不符或自相矛盾：投影半徑公式、`nat_close` 門檻、chromatic 退回的新條件、已解決卻仍寫著「需使用者裁示」的限制、同一段裡一句說移除一句說還在的閘門。全部是人工發現的。

| 改了什麼 | 要更新 |
| --- | --- |
| 使用者看得到的行為（CLI 旗標、輸出規則、丟棄規則、限制） | `README.md` 對應章節 **＋ `pdf2ppt_gui_2.py` 的選項清單**（它是手抄 argparse 的，漂移過一次，見 `docs/dev/windows-環境與入口.md` §5） |
| 通用規則、不變量、門檻本身 | `CLAUDE.md` 的「不變量索引」（只有它每次自動載入） |
| 門檻的**理由、反例、被推翻過的前一版結論** | `docs/spec/` 對應章節 **＋** `CLAUDE.md` 的那一行要跟著改 |
| 模組之間的關係、新增／拆分模組 | `docs/dev/architecture.md`（十分鐘架構導讀） |
| 新增一章 spec | 同時改 `docs/spec/00-總覽與閱讀指南.md` 的文件地圖（`tests/test_docs.py` 雙向釘著） |
| 沿革、災情紀錄、完整推導 | `docs/dev/`，並在 `CLAUDE.md` 留一句可 grep 的指路 |
| 常數的值或理由 | **該常數上方的註解**（那是正典，其他地方都只是指過去） |
| 量過而否決的一條路 | 該處註解 **＋** `CLAUDE.md` 最後的「量過而否決的路」索引 **＋** `docs/spec/11` §11.2 |

判斷方式：**如果這個改動會讓 `CLAUDE.md` 某一句話變成假的，那句話就要一起改。**

## 9. 自己下 Bash 指令的形狀：別寫成權限閘門攔得下的樣子

（自 `MP4-2-SRT` 移植 2026-08-23；原始查證 2026-08-15 讀 `claude.exe`。2026-08-25 從 `CLAUDE.md` 搬進來，那邊只留三條規則本身。）

Auto Mode 的自動核准是把指令交給分類器判斷，但**分類器只吃能被靜態解析的指令**。指令一旦被判為 `too-complex`，程式會設 `classifierApprovable: false`，**跳過分類器直接中斷去問使用者**——而且在 `settings.local.json` 加 `allow` 白名單**救不了**，因為對 `too-complex` 的指令，規則比對一律回傳 `unverifiable`。

三條規則：

1. **不在指令裡 `cd`** —— Bash 工具的工作目錄本來就在專案根目錄，需要別的路徑就寫絕對路徑。
2. **重導向目標寫死絕對路徑**，不可用 `$VAR` 或 `$(...)` 組出來（會被判 `Redirect target contains $(cmd) output — path is runtime-determined`）。
3. **`cd` 與輸出重導向不可出現在同一條複合指令**（`cd-compound-redirect`；程式碼註解寫的是「防止靠 cd 繞過路徑檢查」）。

能不重導向就不重導向，讓輸出直接回到對話裡。

⚠️ **連帶效應：工作區未被信任時，`.claude/settings*.json` 裡的權限規則會被整批忽略**（Claude Code 2.1.232 起，信任不再從上層資料夾繼承進 git repo，每個 repo 要各自確認一次）。症狀就是「每個工具都重新問一次」。

## 10. 改壞了重試：成功的那一刻就把殘骸一起清掉

（自 `MP4-2-SRT` 移植 2026-08-23。）

定義了沒人用的常數與函式、被取代卻沒刪的分支、指路到不存在的檔案 —— **跟著「最後一次改對」的那一批進同一個 commit**，不是留到下次想到再清。

理由是殘骸跟「刻意留的東西」長得一模一樣，而**分得出來的只有剛改完的那個人**。⚠️ 本 repo 的實例：2026-08-23 用 `_grow` 取代 `_dilate` 之後，`_dilate` 零呼叫者卻留在檔案裡，直到當天稍晚 `/simplify` 才發現。

## 11. 第二次瘦身：`CLAUDE.md` 觸到上限時搬哪一段（2026-08-25）

`tests/test_docs_index.py::test_claude_md_stays_small` 的上限**當時**是 28,000 字元（⚠️ 2026-08-26 起是 **30,000**、2026-08-27 起是 **32,000**，兩次放寬的理由都見那支測試自己的 docstring；下面這一段講的是 08-25 那天的事，數字照當時記），當天連續兩筆改動之後它到了 **27,765——只剩 235 個字元**，使用者指示「幫我搬一段到 docs，騰出空間」。搬完是 **26,768（剩 1,232）**。⚠️ **數字要照測試的數法量**：`Path.read_text()` 走 universal newlines，CRLF 併成一個字元，所以用位元組或 `newline=""` 量會多出「行數」那麼多（本檔約 200），憑那個數字判斷離上限多遠會嚇到自己。

⚠️ **不可以動的是「不變量索引」那六段**（2026-08-26 實測 **17,067 字元、佔 61%**）：上限訂在 32,000 而不是姊妹專案的 18,000，理由就是那六段（見那支測試自己的 docstring）。要騰空間只能從**其餘約 10,900 字元**裡找——⚠️ 那一半三天下來幾乎沒動（10,943 → 10,921），**長的一直是不變量索引本身**（15,168 → 17,067），所以「再瘦一次身」對它沒有用，該做的是重新校準上限。

**判準：看這條規則的「觸發點」在哪個檔。**

- **觸發點在別的檔 → 留在 `CLAUDE.md`。** 例：「GUI 的選項清單是手抄 `cli.py` 的 argparse」——你在改 `cli.py`，不會想到去讀 GUI 的文件；「介面字型與 `--font` 是兩回事」——順手統一的動機出現在 `style.py` 那一側。
- **觸發點就在那個檔本身 → 搬走，留一句「動之前先讀」。** 例：`Accent.TButton` 的樣式名、收合還原高度、DPI 與 `App.px()`、`<<ThemeChanged>>`、圖示「文」骨架的三條判別、捷徑的四個路徑陷阱——這些只在你已經打開 `pdf2ppt_gui_2.py`／`tools/make_icon.py`／`make_shortcut.py` 時才需要，而那時「先讀 §5–§5.3」這句指路一定會先被讀到。
- **實測數字 → 搬，結論留下。** 例：DirectML 的 50s/210s/213s 搬回 `docs/spec/09` §9.1，`CLAUDE.md` 只留「快 4.2×」與**不可拿首跑數據下結論**這個判準。

當天搬走的內容全部進 `docs/dev/windows-環境與入口.md` §5–§5.3（其中「介面字型 ≠ `--font`」是**新寫進去的**——它本來只住在 `CLAUDE.md` 與 GUI 的註解裡，搬之前先確認每一條在 dev 文件裡都有家，否則就是在刪東西而不是搬東西）。
